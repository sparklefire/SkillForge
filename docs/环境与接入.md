# 环境与接入

更新时间：2026-07-17（Asia/Shanghai）

## 1. Step Coding Plan - Flash Pro

官方文档：<https://platform.stepfun.com/docs/zh/step-plan/integrations>

### 推理接口

| 用途 | 地址 |
|---|---|
| OpenAI SDK Base URL | `https://api.stepfun.com/step_plan/v1` |
| Chat Completions | `https://api.stepfun.com/step_plan/v1/chat/completions` |
| Messages | `https://api.stepfun.com/step_plan/v1/messages` |
| Anthropic SDK Base URL | `https://api.stepfun.com/step_plan` |
| StepAudio TTS | `https://api.stepfun.com/step_plan/v1/audio/speech` |

注意：Anthropic SDK 会自动追加 `/v1/messages`，所以其 Base URL 不能带末尾 `/v1`。

### 当前可用模型

| 类别 | 模型 | SkillForge 用途 |
|---|---|---|
| 多模态推理 | `step-3.7-flash` | 视频/图片理解、SOP规划、证据复核、复杂质检 |
| 智能路由 | `step-router-v1` | 推理服务回退或复杂请求路由 |
| 快速推理 | `step-3.5-flash-2603` | 批量抽取、改写、低成本结构化处理 |
| 快速推理 | `step-3.5-flash` | 通用Agent与代码任务 |
| 语音对话 | `stepaudio-2.5-chat` | 需要语气与人设的文本交互 |
| 语音合成 | `stepaudio-2.5-tts` | 培训视频旁白 |
| 语音识别 | `stepaudio-2.5-asr` | 专家口述和操作视频转录 |
| 实时语音 | `stepaudio-2.5-realtime` | P2实时专家访谈能力 |
| 图像生成/编辑 | `step-image-edit-2` | 海报辅助图、关键帧清理和视觉标注 |

### 专用端点

| 能力 | 端点 |
|---|---|
| TTS | `/step_plan/v1/audio/speech` |
| ASR（HTTP + SSE） | `/step_plan/v1/audio/asr/sse` |
| 实时语音 | `wss://api.stepfun.com/step_plan/v1/realtime` |
| 文生图 | `/step_plan/v1/images/generations` |
| 图像编辑 | `/step_plan/v1/images/edits` |

### 密钥管理

- 真实密钥保存在项目根目录 `.env`。
- `.env` 已加入 `.gitignore`，权限应为 `600`。
- `.env.example` 只保留字段名和非敏感地址。
- 任何日志、异常、截图和演示材料均不得打印 Authorization Header。
- 当前密钥曾通过对话明文传递；若仓库要公开或人员扩大，提交前应轮换一次。

## 2. DGX Spark

### SSH

```bash
ssh -p 6052 Developer@106.13.186.155
```

连接参数也已保存在本地 `.env`。验证命令：

```bash
bash scripts/check_dgx.sh
```

### 2026-07-17 已验证环境

| 项目 | 结果 |
|---|---|
| 主机名 | `spark-1130` |
| 用户 | `Developer` |
| 系统架构 | `aarch64` |
| 内核 | Ubuntu/NVIDIA 6.14 系列内核 |
| GPU | NVIDIA GB10 |
| 驱动 | 580.126.09 |
| 系统内存 | 约 119 GiB |
| 可用内存 | 约 115 GiB（检查时） |
| 磁盘 | 3.7T，总剩余约 2.6T |
| Python | 3.12.3 |
| Git | 2.43.0 |
| curl | 8.5.0 |
| Docker CLI | 28.5.1 |
| CUDA Toolkit / nvcc | CUDA 13.0；`nvcc` 位于 `/usr/local/cuda-13.0/bin/nvcc`，未加入默认 PATH |
| Docker daemon | 服务正常；`Developer` 不在 `docker` 组，因此当前账号不可访问 |
| NVIDIA Container Toolkit | `1.18.2` 已安装，NVIDIA runtime 已配置，CLI 可识别 GB10 |
| FFmpeg | 用户级 `7.0.2-static` 已安装到 `~/skillforge/bin/ffmpeg` |
| FFprobe | 当前缺失；应用已验证使用 FFmpeg 回退读取媒体信息 |
| FFplay | DGX非必需；本机引导式人工观看使用 Homebrew `ffplay`，找不到时可用手工审核兜底 |
| PDF | 系统 `pdftotext` 可用；P0 运行环境使用 PyMuPDF 逐页提取和渲染 |
| 中文字体 | Noto Sans CJK 已安装 |

### 2026-07-14 远端目录

已建立：

```text
~/skillforge/bin
~/skillforge/data/input
~/skillforge/data/derived
~/skillforge/outputs
~/skillforge/logs
~/.cache/skillforge/models
~/.cache/skillforge/huggingface
~/.cache/skillforge/torch
```

`~/skillforge/bin/ffmpeg` 通过用户级 `imageio-ffmpeg 0.6.0` 提供，不修改系统包。`scripts/check_dgx.sh` 会同时检查系统 FFmpeg 和该用户级路径。

原生应用部署在 `~/skillforge/app`，虚拟环境为 `~/skillforge/app/.venv`。依赖通过本地准备的 `aarch64` wheelhouse 安装，实际测试不依赖 Docker 或远端 PyPI 下载速度。执行输出保存在 `~/skillforge/outputs`。

从macOS以tar同步文件时必须设置 `COPYFILE_DISABLE=1` 或使用不携带扩展属性的归档方式；远端测试前应确认项目内没有 `._*` AppleDouble辅助文件。此类文件若进入 `schemas/`，会被 `*.schema.json` 扫描误判为JSON。

自摄安全派生视频的 DGX 专用目录为 `~/skillforge/data/n31_safe/video`，目录权限 `700`、文件权限 `600`；这里只同步案例清单中6段 `LOCAL_QA_PASSED` 视频。逐文件SHA-256已与本机核对一致。第三方教程、厂商手册、真实面单、私有照片和 `_private_review` 原片均未上传。场景候选帧位于 `~/skillforge/data/n31_safe/frames`，不进入Git。

原生 CUDA 工具由 `native/dgx_frame_features.cu` 在目标机编译为 `~/skillforge/bin/dgx_frame_features`，目标架构为 `sm_121`。2026-07-17实测处理6段视频、420帧，CUDA核耗时55.062毫秒、端到端13.134秒，选择50个候选时间点。该结果只做场景候选筛选，不产生语义事实。

Docker 服务和 containerd 均为 `active`，Socket 为 `root:docker`、权限 `660`；现有 `docker` 组只有其他账号，`Developer` 无法访问。把账号加入 `docker` 组等同于授予主机 root 级能力，必须由远端管理员明确执行并在重新登录后再验证 GPU 容器。

### 用户级离线演示服务

DGX 已安装 `skillforge-demo.service` 用户服务：

- 单元位置：`~/.config/systemd/user/skillforge-demo.service`。
- 工作目录：`~/skillforge/app`。
- 运行模式：仓库内离线 Gold 演示包，不调用外部模型。
- 监听地址：仅 `127.0.0.1:7860`，不暴露公网。
- 安全边界：`NoNewPrivileges=true`、`PrivateTmp=true`、`UMask=0077`。
- 恢复策略：`Restart=on-failure`，2026-07-17 已两次通过 SIGKILL 故障注入验证，每次都恢复为新的主进程并重新通过业务检查。
- 启动策略：服务为 `enabled`，用户 `Linger=yes`；具备随用户管理器启动的配置。未执行高风险整机重启测试。

DGX 本机验证：

```bash
cd ~/skillforge/app
bash scripts/manage_dgx_demo_service.sh verify
bash scripts/manage_dgx_demo_service.sh restart-test
```

开发机通过 SSH 隧道访问：

```bash
bash scripts/dgx_demo_tunnel.sh --smoke
bash scripts/dgx_demo_tunnel.sh
```

默认浏览器地址为 `http://127.0.0.1:17860`。隧道烟测已验证 `GOLD / FINAL / COMPLETED`、严重问题5→0和4项局部修订；测试退出后本地端口自动关闭，远端服务继续保持回环监听。

### 运行时基准

本机与DGX使用同一入口，默认2次预热、20次正式测量：

```bash
# 本机
bash scripts/run_runtime_benchmark.sh local

# DGX：~/skillforge/app
bash scripts/run_runtime_benchmark.sh dgx
```

2026-07-17 当前DGX报告：直接Python Gold闭环中位37.626毫秒、P95 37.758毫秒；进程内Web现场重算中位44.800毫秒、P95 56.040毫秒；基准进程高水位RSS 86,491,136字节。报告不读取 `.env`，不处理原始媒体，不调用外部模型，也不发起网络请求。该口径不能用于宣称原始多模态摄取或GPU语义推理耗时。

### 提交预检边界

正式提交预检必须在本地Git仓库运行：

```bash
bash scripts/check_submission.sh
```

DGX的 `~/skillforge/app` 是不携带 `.git` 的安全部署副本，只能用以下命令验证运行时部分：

```bash
bash scripts/check_submission.sh --allow-missing-git --allow-dirty
```

远端结果必须是 `DEVELOPMENT_CHECK`，其中Git工作树、跟踪敏感路径和本地 `.env` 检查标为 `SKIPPED`；不能用它替代本地正式预检。本地正式预检不允许 `--allow-dirty` 或 `--allow-missing-git`。

退出码为：`0=READY_FOR_SUBMISSION`、`1=NOT_READY`、`2=DEVELOPMENT_CHECK或READY_WITH_HUMAN_GATES`。即使返回非零，结构化报告也会先写入 `outputs/submission/submission_preflight_latest.json`，可据此逐项关闭门禁。

每日提交节奏由 `bash scripts/check_project_board.sh` 检查。公开配置固定10项P0、六类责任角色、7项风险和8个日检命令；只使用 `COMPLETED / READY / AWAITING_HUMAN / AWAITING_EXTERNAL`，不使用含混的 `BLOCKED`。7月19日状态为 `ON_TRACK`，下一截止日7月20日；过期后返回 `ATTENTION_REQUIRED`，7月22日后仍未完成则返回 `DEADLINE_MISSED`。这两种状态会阻止正式预检，但报告仍固定 `implementation_goal_blocked=false`，表示可以继续做安全范围内的实现和收尾。

第8个日检命令是 `bash scripts/check_demo_mode_parity.sh --verify-only`，用于确认现场重算、已准备预处理结果和无素材离线包的核心语义仍一致。素材、依赖或代码变化后先运行 `bash scripts/check_demo_mode_parity.sh --refresh-preprocessed`；刷新会设置严格离线OCR模式，缓存缺失或固定哈希不匹配时直接停止，不会在演示准备阶段联网下载。

`bash scripts/check_submission_closeout.sh` 把技术包、五项门禁、最终干净预检、上传、三个公开入口和回执固定为10阶段依赖链。它只读取当前本地状态并写入Git忽略的0600安全摘要，不发网络请求、不创建人工记录、不确认门禁、不上传文件；等待阶段返回2，证据失效返回1，只有提交回执再次验真后 `READY_FOR_ARCHIVE` 才返回0。DGX可运行同一命令检查技术副本，但远端没有私有门禁证据，不能代替本地正式收尾。

人工门禁通过 `bash scripts/manage_human_gates.sh status` 查看。实际完成某项工作后，用同一脚本的 `confirm` 子命令绑定当前运行单和本地证据SHA-256或安全HTTPS链接；确认不能由自动测试生成。私有记录目录权限700、文件600且由Git忽略，预检报告不会复制确认人、说明、证据定位或证据内容。证据变化、缺失、运行单哈希变化、门禁文案变化或记录权限放宽都会使确认无效并阻止正式提交。最终有声录屏必须先运行 `bash scripts/check_final_recording.sh`，再完整观看并通过 `bash scripts/check_final_recording_review.sh`；该门禁只接受固定本地MP4，同时要求完整观看记录和匿名QA绑定当前媒体、机器QA、构建报告、故事板及内部策略。

本机推荐使用 `bash scripts/run_guided_human_review.sh status` 查看三项人工工作的私有状态，使用 `prepare` 只初始化或迁移合法空白草稿；随后在交互式终端分别运行 `training-video`、`final-rehearsal` 和 `final-recording`。视频由本机 `ffplay` 全屏播放，子进程环境只保留播放器所需的基础变量，不传递Step密钥；命令不使用Shell拼接，不提供自动同意参数。任一人工回答为否或计时不合格时均不写入通过记录和QA，成功生成QA后仍须参赛者用人工门禁确认器显式绑定。DGX部署副本没有私有媒体和草稿，不应运行 `prepare`，只可运行 `status` 验证为 `ABSENT`。

18项成果的角色、独立终检人角色、哈希和发布渠道由 `bash scripts/build_release_manifest.sh --verify-only` 校验，并已接入提交预检。真实姓名不进入仓库。作品页、代码仓库和最终录屏链接在上传后通过 `bash scripts/check_publication_links.sh --init` 创建私有模板，再由同一脚本匿名检查；输入和报告保存在Git忽略的 `outputs/submission/`，目录0700、文件0600，报告不会保存真实网址、域名、IP、响应正文、Authorization或Cookie。带凭证、敏感签名参数、片段、私有主机、非HTTPS跳转或最终落到非公网IP的网址直接拒绝。

`bash scripts/build_public_release_bundle.sh` 会把当前18项冻结成果、发布冻结清单和包内清单打为20成员的确定性ZIP，输出到Git忽略的 `outputs/release/`。再次运行 `--verify-only` 会反向核对精确成员集合、来源与包内哈希、固定ZIP元数据、安全相对路径和本地密钥边界。目录权限700、ZIP及QA权限600；包内不含 `.env`、原始输入、私有派生素材、团队名单、人工确认、作品页/仓库/最终录屏等私有提交网址或提交回执，但证据原文可能保留厂商手册中的公开参考地址。该包可作本地交付和录屏兜底，但在官方规则确认前不能当作指定上传格式。

提交成功后使用 `bash scripts/check_submission_receipt.sh --init` 建立私有回执审核表，并以 `--attach-receipt` 复制成功截图或PDF。机器QA要求固定的 `submission_preflight_final.json` 已达到 `READY_FOR_SUBMISSION`、发布冻结清单仍为当前18项、三个公开入口在提交后匿名复核通过，并绑定回执截图和提交编号哈希。实际截图、编号、网址和个人信息只留在Git忽略目录；QA不复制这些内容，也不能替人判断页面是否显示成功。

团队成员和单位属于私有信息。最终名单确定后运行 `bash scripts/check_team_roster.sh --init`，仅在 `outputs/submission/team_roster.json` 填写2–5人和六类职责，目录0700、输入/QA文件0600且受Git忽略；再次运行脚本后，QA和提交预检只输出名单哈希、人数、角色数和布尔结论，不复制姓名、单位或成员编号。`READY_FOR_HUMAN_CONFIRMATION` 不会自动关闭 `TEAM_ELIGIBILITY_CONFIRMED`；确认操作会绑定当前名单SHA-256和QA状态，名单缺失、权限漂移、人数/声明/角色不完整或确认后内容变化都会使门禁失效。

### P0 冻结路线

1. 使用 DGX 原生 Python 虚拟环境运行摄取、证据处理、工作流和 Web Demo。
2. 使用 `~/skillforge/bin/ffmpeg` 完成抽帧、转码、音轨提取和成片。
3. Docker 权限和 GPU 容器验证降为非阻塞项。
4. 只有明确部署本地 GPU 模型或 NVIDIA VSS 时才处理 Docker 权限。
5. 不额外安装或升级 CUDA；仅复用机器已有的 CUDA 13 `nvcc` 编译可审计的场景特征内核。
6. 使用用户级 systemd 服务完成失败自动重启，并通过 SSH 隧道提供稳定现场访问；不开放 DGX 公网端口。

Gold对齐连续动作候选窗口可在本机或DGX部署副本中确定性重建：

```bash
bash scripts/build_n31_temporal_windows.sh
```

该命令只读取公开安全的Gold、视觉复核和DGX结构化报告，不读取原始媒体或 `.env`，也不调用外部模型。

N31手册结构、中文OCR和检索报告在保存原始手册的本机重建：

```bash
bash scripts/setup_ocr_languages.sh
bash scripts/build_n31_pdf_structure.sh
```

OCR安装脚本只下载固定 `tessdata_fast` 提交中的 `chi_sim` 和 `eng` 数据，并分别核对SHA-256；缓存目录为 `outputs/cache/tessdata`，目录权限700、文件权限600且由Git忽略。构建过程只在本地读取两份手册，手册正文、页面图和含正文索引写入 `cases/n31/output/pdf_structure_v1/`，公开报告只保存哈希、统计和检索页码。DGX安全部署副本没有原始手册，因此只验证公开报告Schema与Web展示，不重跑私有PDF摄取。

## 3. 本地项目实际位置

交接信息记录的旧路径 `/Users/qiushile/WorkStation/ai/agent/skillforge` 当前不存在。本会话挂载并核实的同一 Git 仓库实际位于：

```text
/Users/qiushile/WorkStation/yq/skillforge
```

其分支、提交 `dae65e4`、文档和配置与交接信息完全一致。为避免重复项目，后续工作均在该实际目录进行。

## 4. 推荐混合架构

- DGX Spark：原生 Python 工作流、视频转码、抽帧、OCR/Embedding、敏感素材与缓存；本地模型或 VSS 为可选增强。
- Step 3.7 Flash API：多模态理解、SOP规划、证据复核和复杂质检。
- StepAudio ASR/TTS：专家录音转写和培训旁白。
- Step Image Edit：关键帧编辑、海报辅助图，不生成关键操作事实。
- 原始敏感素材默认留在DGX Spark；调用外部API前必须确认赛事与素材授权边界。

培训视频旁白固定使用 `stepaudio-2.5-tts` 和 `zhixingjiejie` 音色。生成器只发送合并后的旁白文本、音色、语音指令和采样率；视频、手册、专家录音、真实面单和Evidence正文不会进入TTS请求。响应音频缓存在Git忽略目录并保持`600`权限，最终成片、生成清单和不含原始媒体的证据包输出到 `output/video/`。
