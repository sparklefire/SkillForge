# 执行状态

更新时间：2026-07-19

## 当前仓库事实

- 实际项目目录：`/Users/qiushile/WorkStation/yq/skillforge`。
- 交接记录中的旧目录 `/Users/qiushile/WorkStation/ai/agent/skillforge` 当前不存在；未创建重复项目。
- 当前分支：`main`。
- 本批开始提交：`ad530cb docs: record release bundle DGX validation`。
- 未配置远端，未推送。
- `.env` 权限为 `600`，并由 `.gitignore` 排除。

## 已完成：接入与 DGX 环境

- [x] Step Plan 健康检查再次通过：HTTP 成功，模型 `step-3.7-flash`，回复 `STEP_PLAN_OK`。
- [x] 新 Step Plan 客户端使用 `step-3.5-flash-2603` 完成真实结构化 Evidence 输出并通过 Schema 校验。
- [x] DGX SSH、GB10、驱动、内存和磁盘状态再次核实。
- [x] Docker 与 containerd 服务确认为运行状态。
- [x] NVIDIA Container Toolkit `1.18.2` 和 NVIDIA runtime 配置已核实；容器 CLI 可识别 GB10。
- [x] 定位 Docker 阻塞为 `Developer` 不属于 `docker` 组，不是 daemon 停止。
- [x] 在用户目录安装 FFmpeg `7.0.2-static`，路径为 `~/skillforge/bin/ffmpeg`。
- [x] 建立远端项目、输入、派生数据、输出、日志和模型缓存目录。
- [x] 扩充 `scripts/check_dgx.sh`，可重复检查服务、Socket、Toolkit、用户级 FFmpeg 和项目目录。
- [x] 明确当前不需要为预构建容器安装 `nvcc`。
- [x] 冻结 P0 路线为原生 Python + 用户级 FFmpeg；Docker 权限降为非阻塞项。
- [x] 核实 CUDA 13 `nvcc` 实际位于 `/usr/local/cuda-13.0/bin/nvcc`，只是未加入默认PATH；不升级系统CUDA。
- [x] 用 `sm_121` 原生CUDA探针在GB10上正确计算黑白帧场景变化1.0，确认GPU内核可编译、可执行。

## 已完成：P0 工程闭环第一批

- [x] 建立 SOP、Evidence、Conflict 和 Revision Audit JSON Schema。
- [x] 建立集中模型路由层和 Step Plan 客户端。
- [x] 模型请求使用 `600` 权限临时 Header 文件，不把密钥放入参数或日志。
- [x] 实现模型 JSON 提取、保守修复、Schema 校验和最多三次重试。
- [x] 实现结构化 JSONL 日志与凭证脱敏。
- [x] 实现显式工作流状态机及非法迁移拦截。
- [x] 建立明确标注的无版权模拟案例和受控错误注入。
- [x] 实现缺步骤、缺前置、错误顺序、无依据工具、无依据参数和无效证据检测。
- [x] 实现证据驱动的插入、重排和删除式局部修订。
- [x] 保存首轮、问题证据、修订审计、修订后版本和工作流历史。
- [x] 从修订后 SOP 生成手机检查清单数据和带证据的培训测验数据。
- [x] 实现必要步骤覆盖率、证据覆盖率、严重错误数和冲突数指标。

## 已完成：原生 Python + FFmpeg 第二批

- [x] 实现 FFmpeg 自动发现，并在 DGX 缺少 `ffprobe` 时使用 FFmpeg 元数据回退。
- [x] 实现视频编码探测、H.264/AAC 标准化、固定间隔关键帧、音轨提取和时间戳映射。
- [x] 实现独立专家录音转为 16 kHz、单声道 PCM WAV。
- [x] 实现 PDF 逐页文本提取、原始页码保留、页面预览和 OCR 候选标记。
- [x] 按官方 HTTP + SSE 格式实现 `stepaudio-2.5-asr` 客户端和逐段时间戳解析。
- [x] 用无版权合成中文语音完成真实 StepAudio ASR 验证。
- [x] 将视频关键帧、PDF页和ASR片段统一转为 Evidence 候选并执行 Schema 校验。
- [x] 实现 Step 3.7 关键帧感知；证据ID、来源和时间定位由程序强制锁定，模型不能改写。
- [x] 实现 Evidence Catalog 到8–15步 SOP 的规划层，并拒绝未知证据引用和无证据必要步骤。
- [x] 实现无需 Docker 的 FastAPI Web Demo，展示问题、证据、修订前后和修订审计。
- [x] Web 支持视频、PDF、录音上传和原生预处理，原始上传文件不对外提供下载。
- [x] ASR 默认关闭；用户已确认本人自摄、本人录制的 N31 素材可用于项目处理及赛事提交/展示，后续只对该范围内的规范化安全素材启用。第三方教程和真实面单原件继续禁止外发。
- [x] 新增原生环境检查和一键启动脚本。
- [x] 将 `setuptools 80.9.0` 固定到依赖锁，兼容不再默认携带构建后端的Python 3.14虚拟环境；命令行入口重新安装验证通过。
- [x] 在 DGX Python 3.12 虚拟环境完成离线依赖安装、自动测试和原生摄取验证。

## 已完成：真实主案例准备

- [x] 放弃 SIM 卡方案，将唯一真实主案例锁定为“汉印 N31 电子面单打印机更换标签纸、介质学习与试印验收”。
- [x] 确认已有一台 N31 和配套可粘贴物流标签纸。
- [x] 核对官方 N31 用户手册 Rev.1.0 和快速安装指南 Rev.1.5，并记录来源 URL、版本和 SHA-256。
- [x] 定位手册关键证据：介质规格为 PDF 第12页、装纸为第14页、卡纸处理为第15页、走纸与标签学习为第20页。
- [x] 建立真实案例目录、现场采集执行单和预填答案版专家口述与 Gold SOP 审核表；操作者只需补真实经验、确认关键结论并录音。
- [x] 用户确认本人既是参赛选手也是自摄素材提供者，自摄视频、照片、口述及其脱敏衍生成果无需另签素材授权书；授权模板降为未来第三方/单位素材的可选留档。
- [x] 明确受控错误只注入 SOP 草稿，不在真实机器上故意制造卡纸或错误操作。
- [x] 原始厂商手册保存到被 Git 忽略的私有输入目录，仓库只保留来源和哈希记录。
- [x] 接收并检查 `汉印N31开机装纸教程.mp4`：20.72秒、1080x1920、25fps、H.264/AAC；登记为辅助参考视频，不替代连续实机主视频。
- [x] 存档汉印 N31 官方产品页长截图并登记 URL、访问时间、截图尺寸和 SHA-256；核对型号、介质范围、传感器和智能吸纸信息。
- [x] 确认官方产品页当前未提供该20秒教程，产品页只能证明型号和参数，不能替代视频原始来源与复用授权。
- [x] 用户确认20秒教程为购买 N31 后由京东订单客服私发的售后指导素材；登记为本地参考，未获得公开展示或第三方 AI 处理授权。
- [x] 完成目标5分钟的一镜到底拍摄脚本，逐步固定机位、动作、台词、完成标志以及缝标/黑标二选一分支。
- [x] 自动核对13段时间轴连续无重叠、总计300秒；逐段台词长度为48-60个主要字符，保留了实际动作和停顿时间。
- [x] 将一份历史真实面单归档到私有输入目录，仅本地提取页面几何：1页、75.85 × 129.82 mm；不读取或外发客户与运单正文。
- [x] 用户通过实物和照片确认标签纸为折叠纸、单张 72.0 × 130.0 mm；含真实物流内容的试印照片仅存私有输入目录，不提交 Git、不进入演示。
- [x] 将全虚构 N31 测试面单改为实物 72.0 × 130.0 mm，明确标注非真实运单和禁止物流流转；历史 PDF 页面几何只保留为历史记录。
- [x] 测试面单生成器使用嵌入式中文字体和固定PDF元数据；相同环境重复生成的 SHA-256 一致，ReportLab 依赖已固定版本。
- [x] 用户决定当前实机试印直接使用私有历史面单，不采用虚构测试面单；私有文件只允许本机打印，禁止外部处理、Git提交和可辨识地进入拍摄画面。
- [x] 根据私有 PDF 75.85 mm 页面宽与 72.0 mm 实物纸宽的差异，将打印设置改为“适合可打印区域”或 94%-95% 缩放，不使用 100% 原尺寸。
- [x] 私有保存标签纸背面交界处照片；连续标签背面未见周期性黑标，介质定位方式锁定为缝标，正式拍摄只执行第二次闪灯释放的缝标学习。
- [x] 用户放入 `output/pdf/` 的真实面单与既有私有副本 SHA-256 完全一致；保留 `cases/n31/input/reference/N31_previous_shipping_label_private.pdf` 唯一私有副本，并清理公开输出目录中的敏感重复件。
- [x] 接收首版5分56秒横屏主视频和七条短视频；检查分辨率、编码、音轨、旋转元数据和关键帧后完成语义化重命名，原始素材继续由 Git 忽略。
- [x] 确认主视频覆盖开盖、内部导纸、进纸、按键/状态灯、走纸和打印结果等主要动作，机位稳定且照明可用。
- [x] 发现主视频、介质类型特写和结果特写存在真实面单正文可辨识问题，已加 `_private_review` 后缀并禁止进入公开演示或外部模型。
- [x] 主视频音频综合响度约 -31.87 LUFS、响度范围约20.30 LU，判定为音量偏小且波动较大；未调用外部 ASR。
- [x] 保存不含真实面单正文的 `cases/n31/materials/视频验收记录_20260714.md`，列出重命名映射、技术结果和最小补拍清单。
- [x] 接收并检查四条2026-07-15重拍素材；第二版主视频为5分27秒横屏1080p，时长、构图和主要操作覆盖通过。
- [x] 第二版导纸夹特写清楚展示双手实际调节，重命名为正式 `n31_closeup_guides.mp4`，无需再次拍摄。
- [x] 第二版介质类型和结果特写均改为横屏且时长合格；测试面单已遮住直接身份字段，仍有残余唯一标识和编码，分别标记为 `_private_review_v2`，待本地补遮。
- [x] 第二版主视频约05:05至结束出现已部分脱敏的测试面单，标记为 `_private_review_v2`；尾段位置稳定，可本地补遮残余编码，不要求第三次整条重拍。
- [x] 第二版主视频音频响度改善至约 -27.21 LUFS、响度范围14.50 LU；正式派生版仍需本地响度规范化。
- [x] 保存 `cases/n31/materials/视频验收记录_20260715.md` 并更新全部第二版文件哈希和复检状态。
- [x] 用户确认第二版视频中的测试面单已经实体脱敏；复核确认姓名、电话和详细地址等直接字段已遮住，将结论从“未脱敏面单”修正为“已部分脱敏但仍有唯一标识、条码和二维码残留”，后续只补遮残余编码区域。
- [x] 新增可重复运行的本地视频隐私处理配置、脚本和测试，原始 `_private_review_v2` 文件不覆盖，正式输出文件不使用私有复盘后缀。
- [x] 生成 `n31_operation_full.mp4`：约 05:03 起对出纸区域应用不透明遮挡，并将音频规范化到约 -17.49 LUFS、峰值 -1.79 dBTP、48 kHz。
- [x] 生成 `n31_closeup_media_type.mp4`：只保留背面和接缝安全片段；经 5 fps 全片抽检修正一次切点后，不再有正面条码闪入。
- [x] 生成 `n31_closeup_result.mp4`：四个不透明遮挡区域覆盖所有残余编码，人工抽检发现并修复中央实体遮挡下沿的边界泄漏。
- [x] 三段正式视频均通过时长、H.264/AAC、遮挡亮度和音频峰值机器检查；机器报告保存在 Git 忽略目录，自摄安全派生视频的单独授权阻塞已由用户确认移除。
- [x] 新增案例清单驱动的多视频、多PDF本地摄取器，强制拒绝 `_private_review` 文件、未批准来源和任何未明确设为 `external_processing_authorized=false` 的清单。
- [x] 用6段安全视频和两份手册完成 `N31_LOCAL_INGEST_V1`：8个来源、84张视频关键帧、58张PDF页预览、142条 Evidence 候选，模型调用为0。
- [x] 142条 Evidence ID 全部唯一并通过 Schema；两份手册58/58页保留页码。后续精细图像覆盖率门禁识别并完成9页中文OCR，最终待处理页为0；结构化日志权限为600。
- [x] 基于真实 Evidence Catalog 确定性生成13步候选 SOP，绑定25条不同证据引用，无未知 Evidence ID，全部步骤标为 `NEEDS_REVIEW`，整体标为 `NOT_GOLD`。
- [x] 自动生成人工审核队列和可填写 Markdown 审核表，逐步列出 PDF 页码、视频时间点、参数缺证据项和未确认关键帧。
- [x] 候选审核正确暴露两项尺寸参数证据缺口、19个视频关键帧引用待确认，不把未运行的视觉理解当作已验证事实。
- [x] 使用真实素材候选基准执行受控错误彩排：发现缺少缝标学习、依赖断裂、顺序错误、3个无依据参数和1个无依据工具，共7个高严重度问题。
- [x] 基于真实证据完成6处局部修订，复检严重问题7→0、必要步骤覆盖11/12→12/12；生成13项候选检查清单和5题候选测验。
- [x] 彩排输出强制标记 `CANDIDATE_NOT_GOLD`、`PROVISIONAL_ONLY`、`human_review_required=true`，不得作为最终真实评测数字。
- [x] 接收并规范命名6分29秒专家口述，原始文件权限设为600；音频为AAC、48kHz、双声道，平均音量约-25 dB、峰值约-4 dB，无需重录。
- [x] 调用 `stepaudio-2.5-asr` 完成真实口述转写：1301个SSE事件、1300个逐字时间戳片段、1399字最终文本；规范化ASR源为16kHz单声道。
- [x] 将逐字片段确定性合并为12个问题级时间段，保留原始ASR和时间戳流，用受控术语表校正N31、缝标、导纸夹、胶辊和72×130毫米等识别项；全部必要术语检查通过。
- [x] 新增12条 `EXPERT_ADVICE / VERIFIED` 口述证据（E143–E154），Evidence Catalog由142条扩充到154条。
- [x] 生成13步 `OPERATOR_REVIEWED_GOLD / Gold v1`：全部步骤为 `VERIFIED`，10步必需，S05、S06和S11为条件执行；72×130毫米参数获得口述证据，删除无明确来源的“短按0.5秒”参数。
- [x] 使用Gold约束重跑受控错误集：发现缺步骤、依赖断裂、顺序错误、无依据参数和无依据工具共5个高严重度问题；4项局部修订后复检为0，必要步骤与证据覆盖均由9/10恢复到10/10。
- [x] 生成Gold版13项检查清单、5题测验、证据时间点、审核记录、约束和评测正确答案；指标标记为 `GOLD / FINAL / human_review_required=false`。
- [x] Web Demo 新增 N31 真实彩排接口并优先展示真实来源结果；页面醒目标注“候选、非Gold”，缺失预处理结果时自动回退到无版权模拟案例。
- [x] Web Demo 支持实际操作者审核的 `GOLD / FINAL` 结果并自动优先展示，同时兼容 `NOT_GOLD / PROVISIONAL_ONLY` 和模拟回退。
- [x] Web 证据卡只展示 Evidence ID、来源和 PDF 页码/视频时间点，不直接展开手册正文；不提供原始素材下载。
- [x] 本机真实进程再次验证 `/health`、`/api/n31` 和首页均返回 HTTP 200；接口返回 `GOLD / FINAL`、5个初始冲突、4项局部修订和0个最终冲突。
- [x] 新增 `scripts/run_n31_local.sh`：默认复用安全成片快速重跑，也可用 `--with-video-processing` 从本地隐私派生开始完整重跑；两种模式模型调用均为0。
- [x] 新增 `scripts/run_n31_expert.sh`：一键执行专家录音ASR、术语核对、Gold固化和最终闭环评测；`run_n31_local.sh` 检测到Gold后自动运行最终评测。
- [x] 只将Git已跟踪文件同步到 DGX `~/skillforge/app`，未上传 `.env`、N31视频、PDF、关键帧、Evidence Catalog、候选输出或授权材料。
- [x] 修正 DGX 离线安装方式：可编辑安装增加 `--no-build-isolation`，避免 pip 尝试联网下载构建依赖。
- [x] DGX 原生 Web 在远端回环地址完成健康检查和模拟闭环烟测；健康状态为 `ok`、`docker_required=false`、模拟复检严重错误为0。
- [x] 新增 Visual Assessment、Visual Review Report 和 Multisource Evaluation Schema；模型只返回视觉判断，程序锁定步骤、Evidence ID、来源、时间点和关键帧路径。
- [x] 用 Step 3.7 对13个 Gold 步骤执行真实视觉序列复核：12步 `PARTIAL`、S04 `NOT_VISIBLE`、0步 `SUPPORTED`、0步 `CONTRADICTED`；13步整体可观察率12/13，10个必要步骤中9个至少部分可观察。
- [x] 视觉复核加入最多6帧窗口、4096 token 输出预算、非法JSON重试和逐步检查点；失败输出不发布，成功后删除检查点。
- [x] 完成多源消融：手册单源8/10、专家口述单源9/10、视频严格语义支持0/10、视频至少部分可观察9/10、至少两种来源联合10/10。
- [x] 保留视觉模型的11步潜在隐私标记作为保守复核队列；既有安全派生视频机器检查和人工抽检为 `PASSED`，模型标记不自动推翻人工结论。
- [x] Web 新增多源对比面板和 Gold 现场重跑控制；本机接口稳定复现严重问题5→0，并继续展示视觉复核与多源评测。
- [x] 新增现场重算、完整预处理和无素材离线三种一键演示模式；离线演示包不含视频、关键帧、录音、PDF、真实面单或凭证。
- [x] 本批Git跟踪文件同步到DGX后，Python 3.12完成35项测试；远端离线Web、视觉/多源接口和Gold现场重跑均返回200，验证服务已停止。
- [x] Web 成果区接入13项手机检查清单和5题培训测验；API同时返回工作流记录。
- [x] 新增最终SOP、检查清单、测验和修订审计四项结构化下载白名单；真实素材和任意未允许名称均无法通过该接口下载。
- [x] DGX复验成果区：13项清单、5题测验和 `COMPLETED` 工作流可读；4项白名单下载均为200，未允许私有视频名为404，服务自动停止。
- [x] 新增通用 Gold SOP → 单页A4海报生成器；N31海报包含13步、条件步骤、异常处理、完成标准和隐私提示。
- [x] 海报PDF使用固定元数据确定性生成；标准A4单页、130442字节、150 dpi渲染无裁切、重叠、乱码或越界文字。
- [x] Web成果白名单新增A4海报下载；本机真实进程返回 `application/pdf`、HTTP 200，下载字节与生成文件一致。
- [x] DGX旧虚拟环境缺少ReportLab导致海报测试首次在收集阶段停止；已将ReportLab、Pillow和charset-normalizer的aarch64 wheel加入既有远端wheelhouse并离线安装。
- [x] 为保证Python 3.12 aarch64可复现安装，将无对应aarch64 wheel的 `Pillow 12.3.0` 锁定值调整为 `12.2.0`；不修改系统Python。
- [x] DGX重新生成单页A4海报：595.28 × 841.89 pt、文本含S01和S13；Web返回HTTP 200、`application/pdf`和有效PDF文件头。
- [x] 新增 `DGX Visual Compute Report` Schema、原生CUDA特征工具、Python编排、隐私门禁、场景候选选择器和专项测试。
- [x] 只将案例清单中6段 `LOCAL_QA_PASSED` 自摄安全派生视频同步到 `~/skillforge/data/n31_safe/video`；目录700、文件600，逐文件SHA-256与本机一致。第三方教程、手册、真实面单、私有照片和原始复盘视频未上传。
- [x] DGX GB10实际处理6段视频420帧：CUDA核耗时55.062毫秒、端到端13.134秒、吞吐31.978帧/秒，筛出50个场景候选时间点；外部API处理授权仍为false。
- [x] GPU输出只用于候选筛选，固定为 `CANDIDATE_SELECTION_ONLY`，不把亮度/边缘/变化分数误写成SOP动作证据。
- [x] Web新增DGX计算面板和5步Agent决策/工具轨迹，API加载前再次执行Schema校验。

## 已完成：连续动作候选窗口

- [x] 新增连续动作候选窗口Schema、生成器、一键脚本、Web面板、白名单下载和专项测试。
- [x] 对13个Gold步骤的51次已审核帧引用按同源时间线合并，生成19个带开始/结束时间、时长和Evidence ID的候选窗口，覆盖6段视频。
- [x] 将窗口附近的DGX场景候选时间点绑定到报告：12个窗口命中，去重后共41个候选时间点；窗口生成阶段外部模型调用为0。
- [x] 语义范围固定为 `GOLD_ALIGNED_CANDIDATE_WINDOW_ONLY`，不冒充无Gold引导的通用动作识别；S04窗口为60–75秒并绑定3个DGX候选时间点，但严格视觉结论仍保留 `NOT_VISIBLE`。
- [x] 路演验收将该报告列为第11项必需成果，并自动核对范围、数量、来源和S04保守结论。

## 已完成：PDF结构化解析、中文OCR与检索

- [x] 重构PDF摄取：保留原始页码和预览，同时抽取标题、段落、列表、表格、警告与图注；当布局块遗漏正文时回退到页级文本分段，不静默丢失内容。
- [x] 新增页级OCR质量分数、替换字符比例、图像覆盖率和 `disabled / auto / required` 三种门禁；图片型页面使用本地Tesseract `chi_sim+eng`，不调用外部模型。
- [x] 新增固定提交与SHA-256校验的中文/英文OCR数据安装脚本，缓存目录权限700、语言文件600且由Git忽略。
- [x] 两份N31手册共58页、28,172字符、607个结构块；9页执行OCR后待OCR页为0，结构包含49个标题、87个列表、448个段落、5个表格和18个警告。
- [x] 建立含来源、PDF页码、块ID和结构类型的本地检索索引；三个关键查询全部精确命中，用户手册定位为第14页和第20页。
- [x] N31摄取清单启用自动中文OCR；完整预处理仍保持8个来源、142条Evidence和既有Evidence ID选择器，候选SOP 13步及Gold 5→0闭环未发生漂移。
- [x] 公开 `pdf_structure_v1.json` 只保存输入哈希、统计和命中页码，不含手册正文、页面图、私有索引、凭证或绝对路径；Web展示前再次Schema校验。
- [x] 路演验收将PDF结构报告列为第12项必需成果，并核对58页、607块、9页OCR、0页待处理和三项检索结果。

## 已完成：三来源候选步骤抽取、合并与排序

- [x] 新增来源候选输入和合成报告两个JSON Schema、原生Python实现、命令行入口、一键脚本与专项测试。
- [x] 从真实N31 Evidence Catalog分别保存18条视频候选、7条手册候选和8条操作者口述候选；每条锁定来源类型、来源引用和Evidence ID，禁止跨来源借证据。
- [x] 候选合成不读取Gold步骤文本、不调用外部模型；使用审核后的来源投影和既有候选语义规范，完整保留输入措辞、候选成员和合并理由。
- [x] 自动识别8条过粗候选并拆成18个语义片段，识别8条过细候选，执行同义归一和精确重复去重，最终合成为13步。
- [x] 13步依赖图通过拓扑排序和阶段单调检查：准备3步、执行6步、验证4步；成功路径无独立复位步骤并显式记录 `RESET / NOT_APPLICABLE`。
- [x] 标记打印自检页和测试标签两项耗材不可逆步骤，12步配置明确的重试、返回前序或停止复核策略。
- [x] 12/13步骤具备至少两类来源，10/13步骤覆盖视频、手册和口述三类来源；S04因视觉复核为 `NOT_VISIBLE`，合成层只保留PDF依据且不引用E014。
- [x] Web新增候选来源、粗细粒度和合并组面板及白名单下载；路演与提交预检将报告列为必需成果之一。
- [x] `run_n31_local.sh` 已接入候选合成阶段，实测仍保持Gold严重问题5→0、4项局部修订且外部模型调用为0。

## 已完成：可解释置信度与审核路由

- [x] 置信度不再直接采用单一模型或人工分数；对每种来源选择最佳候选，并按 `SOURCE_FACT=1.0`、`EXPERT_ADVICE=0.9`、`MODEL_INFERENCE=0.65` 的证据权威性加权。
- [x] `VERIFIED` 与 `UNREVIEWED` 审核状态、`SUPPORTED` 与 `PARTIAL` 支持状态分别进入分数分解；两源和三源佐证最多增加0.08。
- [x] 建立 `NOT_VISIBLE`、`CONTRADICTED`、`MISSING_SOURCE` 和 `SOURCE_CONFLICT` 四类负面观察惩罚，并锁定其Evidence ID、来源和置信度。
- [x] 输出每步来源组件、权威权重、支持/审核系数、佐证奖励、观察惩罚、最终分段、路由和解释文本，通过新增Schema约束。
- [x] 当前N31为6步 `HIGH / AUTO_VERIFY`、6步 `MEDIUM / VERIFIER_QUEUE`、1步 `LOW / HUMAN_REVIEW_REQUIRED`。
- [x] S04只含PDF候选，视频 `NO001 / NOT_VISIBLE` 产生0.108惩罚，最终为0.691并自动进入人工确认；路演断言固定检查该保守结果。
- [x] Web候选面板展示人工复核数量，并在合并组中显示分数、置信度等级和审核路由。

## 已完成：80秒培训视频与成果交付

- [x] 新增培训视频 Storyboard、Manifest 和 Evidence Pack 三份JSON Schema，所有模型/媒体输出进入Web前再次校验。
- [x] 建立15镜头、总计80秒的N31分镜；覆盖13/13 Gold步骤、10/10必要步骤和30次Evidence引用，每个步骤镜头至少绑定一个同源重叠视频证据。
- [x] 只使用案例清单中6段 `LOCAL_QA_PASSED` 自摄安全派生视频；第三方教程、厂商手册原件、专家录音、真实面单和私有照片均未参与成片处理。
- [x] 使用 StepAudio `stepaudio-2.5-tts`、`zhixingjiejie` 音色生成371字旁白；只发送文本，缓存音频与临时Header保持`600`，日志不记录文本、密钥或Authorization Header。
- [x] 使用原生FFmpeg确定性重剪真实镜头并叠加步骤、旁白、Evidence ID、条件步骤和进度；不生成或伪造关键操作画面。
- [x] 生成80秒、1920×1080、30fps、H.264/AAC、48kHz成片；CRF 22版本为35194451字节，SHA-256为 `61ec74826befc89889a6025a9acdb217adbd32af589a9c1f4bb4be1958ff695f`。
- [x] 自动检查通过：时长、尺寸、帧率、音轨、Gold覆盖、Evidence边界和来源隐私门禁均为true；综合响度-16.18 LUFS、真峰值-1.43 dBTP。
- [x] 复核16帧联系表和5张1080p重点抽帧：字幕安全区、设备辨识、S11裁切和S12-S13遮挡有效；S04只展示状态并明确标注“接电见手册P2”。
- [x] 生成独立视频证据包：15镜头、25条去重Evidence，保留PDF页码、视频/录音时间点和来源类型，不嵌入原始媒体、绝对路径或凭证。
- [x] Web成果页新增内嵌播放器、视频/生成清单/证据包三项白名单下载；展示前同时核对视频、证据包和清单SHA-256，Range请求可读取MP4。
- [ ] 成片状态保持 `READY_FOR_HUMAN_REVIEW`；最终参赛者必须完整观看并确认旁白节奏后才能改为 `FINAL_APPROVED`。

## 已完成：三分钟路演运行层

- [x] 新增可机器校验的 N31 路演运行单：7个阶段从0秒连续覆盖到180秒，无空档、重叠或超时。
- [x] 冻结讲解顺序为痛点20秒、输入20秒、本地提取30秒、质检修订40秒、成果30秒、指标20秒、平台价值20秒。
- [x] 明确现场演示边界：重算的是实际操作者审核Gold的受控错误集，用于稳定验证质检与局部修订，不冒充原始素材实时模型推理。
- [x] 新增路演自动验收器，检查时间轴、18项必需成果、五类Agent工具追踪、Gold 5→0、覆盖90%→100%、四场景无来源内容门禁、高推理语义复核、选择性重建边界、三种SOP视图、三来源候选合并、DGX 6段/420帧/50候选、19个连续动作候选窗口、58页PDF结构/OCR报告、运行时基准、视频哈希和Web兜底。
- [x] 生成8页正式路演PPT，使用真实安全N31画面、Web证据截图和DGX计算报告；未嵌入第三方教程、真实面单、密钥或绝对路径。
- [x] PPT逐页完成原始渲染检查和PPTX兼容渲染，越界检测为0；文件约1.46MB，可编辑并可离线打开。
- [x] 生成精确讲解词、切屏动作、超时裁剪顺序、录屏设置和现场故障恢复表。
- [x] 实际逐个启动 `live`、`preprocessed`、`offline` 三个入口，健康检查均为原生Python、无需Docker，Web均返回 `GOLD / FINAL`、严重错误5→0和4项局部修订。
- [x] 自动验收状态为 `READY_WITH_HUMAN_GATES`；机器检查全部通过，完整观看、真人彩排、最终录屏、团队资格和官方规则五项人工/外部门禁保持 `PENDING`。

## 已完成：可复现运行时基准

- [x] 新增 `Runtime Benchmark` Schema、Python运行器、一键脚本、命令行入口和专项测试；报告只保存结构化指标，不含原始媒体、凭证或绝对路径。
- [x] 基准固定执行2次预热和20次正式测量，分别覆盖直接Python Gold闭环和进程内Web现场重算；每次都断言 `GOLD / FINAL / COMPLETED`、严重问题5→0、4项局部修订和外部模型调用0次。
- [x] 本机实测：直接Python中位38.755毫秒、P95 39.317毫秒；Web现场重算中位44.523毫秒、P95 49.086毫秒；进程高水位RSS 100,057,088字节。
- [x] DGX Python 3.12.3 / aarch64 / NVIDIA GB10当前报告：直接Python中位37.626毫秒、P95 37.758毫秒；Web现场重算中位44.800毫秒、P95 56.040毫秒；进程高水位RSS 86,491,136字节。
- [x] 路演验收将DGX运行时报告列为第10项必需成果，并检查执行环境、20个样本、Gold断言、资源记录和安全测量范围。
- [x] 性能口径限定为已审核结构化Gold的确定性质检、局部修订和输出；不包含原始视频、PDF、录音预处理，不代表GPU推理速度。

## 已完成：提交预检与参赛材料

- [x] 新增提交预检Schema、Python检查器、命令行入口和一键脚本；正式报告写入Git忽略的 `outputs/submission/`，不记录绝对路径、凭证或原始媒体。
- [x] 自动检查项目名称/定位/案例/截止日、9份说明文档、18项路演成果、Git工作树、跟踪敏感路径、`.env`忽略与600权限、实际本地密钥值泄漏和公开成果绝对路径。
- [x] 全量测试作为预检必选项；开发时显式允许脏工作树只返回 `DEVELOPMENT_CHECK`，不能被误报为正式就绪。
- [x] 提交预检退出码收紧为：只有 `READY_FOR_SUBMISSION` 返回0；自动失败返回1，开发检查或未关闭人工门禁返回2。
- [x] 将路演人工门禁从2项补全为5项：成片完整观看、180秒彩排、最终有声字幕录屏、2–5人团队资格、官方规则讲义核对。
- [x] 新增 `docs/架构与数据边界.md` 和 `docs/参赛提交材料.md`，包含架构图、数据分级、事实边界、20秒/约100字/一页介绍、指标、已知限制和待填写字段。

## 已完成验证

- [x] 自动测试：视觉、多源评测和演示兜底新增后本机与DGX均为 `35 passed`；A4海报新增后为 `36 passed`；DGX视觉计算新增后为 `38 passed`；培训视频、Web证据交付和AppleDouble防护新增后本机和DGX Python 3.12均为 `45 passed`；路演时间轴和人工门禁新增后为 `49 passed`；DGX服务安全边界和部署脚本新增后为 `51 passed`；运行时基准新增后为 `55 passed`；提交预检新增后为 `60 passed`；连续动作候选窗口新增后为 `66 passed`；PDF结构/OCR新增后本机与DGX均为 `72 passed`；三来源候选合成新增后本机为 `77 passed`；三种SOP视图和交互检查清单新增后本机为 `82 passed`；五类证据测验和无来源门禁新增后依次为90和96项；高推理语义复核与选择性重建加入后本机为 `105 passed`；离线包发布前Schema门禁加入后本机为 `106 passed`。
- [x] P0 模拟闭环状态：`COMPLETED`。
- [x] 模拟严重错误：修订前 5 项，修订后 0 项。
- [x] 模拟必要步骤覆盖率：修订前 `8/9`，修订后 `9/9`。
- [x] 模拟必要步骤证据覆盖率：修订前 `8/9`，修订后 `9/9`。
- [x] 自动局部变更：4 项，包括插入、重排、删除工具和删除参数。
- [x] 本机原生摄取：8秒视频、2页PDF、5秒录音，生成4帧、2页预览和6条证据候选。
- [x] StepAudio ASR：合成中文句子完整识别，返回逐段时间戳事件。
- [x] N31真实专家口述ASR：6分29秒、1399字、12个问题级时间段、12条已验证口述Evidence。
- [x] N31 Gold v1：13步全部 `VERIFIED`，10个必要步骤均有证据，评测严重问题5→0、必要步骤覆盖90%→100%、证据覆盖90%→100%。
- [x] Step 3.7 视觉：真实分析合成关键帧并返回通过 Evidence Schema 的画面描述。
- [x] N31 Step 3.7 视觉序列复核：13步均完成模型调用和 Schema 校验，12步部分可见、1步不可见、0步矛盾；未将稀疏关键帧误标为完整支持。
- [x] N31连续动作候选窗口：13步、19个同源窗口、51次已审核帧引用、12个窗口命中41个去重DGX候选；S04仍保留 `NOT_VISIBLE`。
- [x] N31 PDF结构/OCR：两份手册58页、607个结构块、9页本地中文OCR、0页待处理；三个关键查询精确命中用户手册第14页和第20页。
- [x] N31三来源候选合成：33条候选合并为13步，8条粗粒度候选拆成18段、8条细粒度候选进入合并，12步多源、10步三源、依赖图无环，S04仅保留手册支持。
- [x] N31 多源对比：手册80%、专家口述90%、视频部分可观察90%、两种以上来源100%；Gold确定性修订仍为5→0。
- [x] N31 本机演示兜底：离线包 `/health` 和 `/api/n31` 返回200，现场重跑接口返回 `GOLD / FINAL`、5→0、4项局部修订。
- [x] N31 本机成果接口：13项检查清单、5题测验和 `COMPLETED` 工作流可读；4项白名单下载返回200，私有素材名称返回404。
- [x] N31 A4海报：一页标准A4，确定性SHA-256为 `d87dd874dc332d8057f0a1106b5b42085f6127f87ddeaa8f4b8a79eee08a8fba`，页面无越界文本块。
- [x] DGX A4海报：远端生成页数1、595.28 × 841.89 pt、可抽取1067字符，S01和S13均存在；成果下载为有效PDF。
- [x] Step 3.7 SOP规划：从13条合成证据生成10个必要步骤，13个引用全部来自给定目录。
- [x] DGX 原生摄取：`probe_backend=ffmpeg`、4帧、2页、6条证据候选。
- [x] DGX Web 健康检查：`runtime=native-python`、`docker_required=false`。
- [x] DGX Gold闭环：`GOLD / FINAL / COMPLETED`，严重问题5→0，必要步骤与证据覆盖90%→100%。
- [x] DGX Gold Web：`/health`、`/api/n31`和首页均返回200，`n31_rehearsal_available=true`，未上传原始N31素材。
- [x] DGX 无素材演示兜底：`/health`、`/api/n31`和`/api/n31/run`均返回200；现场重算5→0，重算后视觉与多源结构化结果仍可用。
- [x] DGX 结构化成果下载：最终SOP、检查清单、测验和修订审计返回200，非白名单素材请求返回404。
- [x] DGX原生GPU视觉：6个安全视频、420帧、50个候选时间点，报告通过Schema且不含本机/远端绝对路径、密钥或素材内容。
- [x] DGX培训视频：成片35194451字节且SHA-256与本机一致；生成清单、25条去重Evidence证据包、`READY_FOR_HUMAN_REVIEW`状态和32字节Range播放均通过回环Web复验。
- [x] 路演自动验收：180秒连续时间轴、14项必需成果、三种SOP视图与交互检查清单、其他核心主张、6项运行时基准断言和6项演示兜底断言全部通过；PPTX兼容渲染无越界。
- [x] DGX路演验收：状态同为 `READY_WITH_HUMAN_GATES`，PPTX SHA-256与本机一致，传输产生的AppleDouble元数据已清理为0。

## 已完成：赛事对齐与 DGX 访问复验

- [x] 对照 NVIDIA AI 技术专区公开赛事说明和 NVIDIA 训练营官方页，建立 `docs/赛事要求对齐矩阵.md`。
- [x] 确认 SkillForge 直接覆盖赛事公开要求中的多模态感知、理解、自主创作、持续优化、内容生产和自动化工作流，没有方向性偏离。
- [x] 区分规则门槛与竞争力风险：2–5人组队状态尚未确认；DGX语义推理分量偏轻属于潜在评分风险，不是当前已确认违规。
- [x] 核实训练营页存在赛事规则讲义入口；匿名访问需要用户校验，公开页面仍未给出评分权重、提交字段、外部API和开源要求，继续标为待官方原文确认。
- [x] DGX离线Web以 `0.0.0.0:7860` 启动，远端回环 `/health` 和 `/api/n31` 返回200；Gold状态为 `GOLD / FINAL / COMPLETED`，严重问题5→0、局部修订4项。
- [x] 停止首个Web进程后以新PID重新启动并复验成功，确认应用进程级重启恢复；验证完成后服务已停止。
- [x] 从本机访问DGX公网 `:7860`：普通请求返回502，绕过本机代理后连接建立但收到空响应；Uvicorn日志没有收到该请求，定位到公网入口或中间网络层。未修改系统防火墙和云安全组。

## 已完成：DGX 用户服务与安全隧道

- [x] 新增用户级 `skillforge-demo.service`、服务管理脚本、SSH隧道脚本、部署说明和专项测试；全链路不依赖root、Docker或公网端口。
- [x] 服务固定使用离线Gold演示包并只监听 `127.0.0.1:7860`；启用 `NoNewPrivileges`、私有临时目录和 `0077` UMask。
- [x] DGX `systemd-analyze --user verify` 通过；服务安装为 `enabled`，用户 `Linger=yes`，当前状态 `active/running`。
- [x] 服务健康检查和N31接口返回 `native-python`、`docker_required=false`、`GOLD / FINAL / COMPLETED`、严重问题5→0和4项局部修订。
- [x] 对服务主进程两次执行SIGKILL故障注入，systemd均按 `Restart=on-failure` 恢复为新PID；恢复后全部业务断言再次通过。
- [x] 本机 `bash scripts/dgx_demo_tunnel.sh --smoke` 通过：SSH将本机 `127.0.0.1:17860` 映射到DGX回环服务，退出后隧道进程自动清理。
- [x] 未执行整机重启，也未修改云安全组、系统防火墙或Docker权限。

模拟案例数字仅用于工程回归；N31 `GOLD / FINAL` 数字来自实际操作者口述审核后的Gold v1，可作为当前真实案例评测结果。

## 已完成：三种SOP视图与交互检查清单

- [x] 新增简洁版、详细版和带证据版SOP；三种视图均为13步，并从同一份修订后SOP确定性生成。
- [x] 三种视图的每一步均含动作、原因、完成标志、风险和来源；带证据版补充证据分类、审核状态、PDF页码或音视频时间点。
- [x] 手机检查清单升级为一步一屏，支持关键帧、风险提示、Evidence展开和完成勾选；S04严格显示 `NOT_VISIBLE`，不把定位帧声明为动作支持。
- [x] 新增完成、重开和四类问题反馈的本地会话记录；会话目录权限700、文件权限600，输出位于Git忽略目录，不进入离线公开包。
- [x] Web新增三视图切换/下载、上一步/下一步、完成状态和反馈入口；完成记录重新读取和本地关键帧白名单验证均通过。
- [x] 从已审核80秒培训成片确定性生成13张640像素宽安全预览及哈希清单；Web默认展示这些公开预览，不再依赖仅本机存在的私有派生帧。
- [x] Gold现场重跑、候选彩排和模拟闭环均接入新成果生成；离线Gold包新增 `sop_views.json` 并保持不含原始媒体和凭证。
- [x] 路演必需成果增至14项，新增 `training_package_traceable` 断言；自动状态仍为 `READY_WITH_HUMAN_GATES`，只剩既有五项人工/外部门禁。
- [x] DGX Python 3.12全量83项测试通过；用户服务重启后返回三种视图、13张安全预览、S04 `NOT_VISIBLE`、本地完成记录和14项路演成果，会话文件权限为600；S01预览接口返回有效JPEG。

## 已完成：五类证据培训测验

- [x] 将原有5道同构判断题升级为排序、工具多选、风险单选、完成状态识别和错误判断五类题，每类恰好一题。
- [x] 排序答案校验SOP依赖；工具、风险和状态答案分别与目标步骤的 `tools`、`warnings` 和 `success_check` 精确一致；错误判断固定拒绝忽略有来源风险。
- [x] 干扰项只取自其他已绑定Evidence的步骤；每个选项独立保存Evidence ID，不引入虚构工具、风险或状态。
- [x] 每题保存答案来源、解析来源和完整Evidence详情；Schema与完整性校验拒绝未知引用、来源越界、重复选项、答案不在选项和错误依赖顺序。
- [x] Gold、候选、模拟闭环和离线包统一生成新版测验；Web展示选项、格式化答案、答案/解析来源和页码或时间点展开。
- [x] 路演新增 `training_quiz_grounded` 自动断言；本机全量测试为90项，当前自动状态仍为 `READY_WITH_HUMAN_GATES`。
- [x] DGX Python 3.12全量90项测试通过；用户服务重启后N31接口和测验下载均返回五类题，答案与解析来源非空，路演断言通过。

## 已完成：确定性无来源内容拒绝门禁

- [x] 工具质检从“全局允许”收紧为“当前步骤有来源”；把S01加入其他步骤允许使用的标签纸时，规则仍会拒绝，并引用S01的E001、E096和E144作为当前步骤Evidence边界。
- [x] 参数质检同时校验名称、数值、单位和Evidence集合；把有来源的72毫米宽度篡改为999毫米时，不删除整个参数列表，而是只把受影响参数恢复为Evidence支持的72毫米。
- [x] 所有额外安全提示均需与当前步骤来源一致；标题、动作或完成标志中的“100%安全”“零风险”等绝对承诺会被拒绝并恢复为Gold字段。
- [x] 新增四个隔离篡改场景和严格Schema；每个场景必须恰好产生预期冲突、完成一次局部修订、恢复参考值且复检残留为0，否则门禁报告标记为 `FAILED`。
- [x] Web新增 `4场景 / 4检出 / 4恢复 / 0残留 / 0模型调用` 面板、修订前后展开和白名单下载；无素材离线包同步包含该安全结构化报告。
- [x] 路演必需成果增至15项并新增 `grounding_gate_closed` 断言；本机全量96项测试、JavaScript语法检查、路演验收和开发模式提交预检全部通过，状态保持 `READY_WITH_HUMAN_GATES`。
- [x] 仅用当前提交27个Git跟踪文件生成327,680字节增量安全归档并同步DGX，远端SHA-256一致；两次不完整的全仓归档都在解压前被大小/哈希校验拦截，旧部署未被覆盖。
- [x] DGX Python 3.12全量96项测试通过；门禁脚本返回4场景、4检出、4恢复、0残留、0模型调用，生成报告SHA-256与本机一致。
- [x] DGX路演15项成果和 `grounding_gate_closed` 断言通过；用户服务重启后保持 `active/enabled` 和 `127.0.0.1:7860`，N31数据与门禁白名单下载均通过，Gold指标仍为5→0且五类测验保持可用。
- [x] DGX运行时预检为预期的 `DEVELOPMENT_CHECK`：15项成果和96项测试通过，因部署副本不含Git和`.env`，对应检查按设计标记 `SKIPPED`；未处理Docker权限。

## 已完成：高推理语义复核与选择性重建边界

- [x] 新增语义复核响应与公开报告Schema，使用 `step-3.7-flash / high` 检查来源曲解、来源冲突、顺序风险和异常遗漏。
- [x] 外部请求只包含13步结构化Gold、36条步骤内Evidence陈述和约束；不发送原始媒体、完整转写、手册页面、本地路径或凭证，并要求操作者显式授权。
- [x] 响应必须逐步覆盖且只能引用所属步骤Evidence；来源冲突至少双来源，顺序风险至少跨两步。模型结果固定归类为 `MODEL_INFERENCE`，不能自动覆盖Gold。
- [x] 本次真实调用累计5次传输尝试：前三次Schema拒绝，一次长度截断，最后一次通过Schema和本地Evidence边界；最终13步均为 `SUPPORTED`，四类发现项均为0，自动Gold修改0。
- [x] 新增Revision Audit确定性重放与选择性重建报告：受位置或内容影响的是S07–S13，内容实际变化3步；只重建Q02和V07–V13，其余6步、4题和8镜头逐对象保持相同。
- [x] A4海报为固定单页原子成果，步骤插入、顺序或海报可见字段变化时才整页重建；全部选择逻辑在本地执行，外部模型调用0。
- [x] Web新增高推理复核与选择性重建面板、白名单下载；无素材离线包增至13个JSON且仍声明不含原始媒体或凭证。
- [x] 路演必需成果增至17项，新增 `semantic_review_grounded` 和 `selective_rebuild_bounded` 断言；本机106项全量测试、JavaScript语法、17项路演成果和三种演示兜底全部通过，状态保持 `READY_WITH_HUMAN_GATES`。
- [x] 用仅含当前提交34个Git文件的112,129字节增量归档同步DGX；归档SHA-256为 `ff9b8769738059a0214f53a23d21f30a67de68c69b1a208e28b6efca4a7ba721`，传输前后一致，且不含 `.env`、原始输入、私有派生目录或AppleDouble文件。
- [x] DGX首次全量105项测试通过；随后离线包重建暴露被Git忽略的Gold运行缓存仍是旧检查清单格式，验收器按Schema拒绝，没有发布错误包。使用当前结构化Gold在DGX本地重新生成缓存后，离线包恢复为13个安全JSON。
- [x] 将这次发现固化为打包前门禁：离线包构建器现在先校验三种SOP视图、手机清单、测验、无来源门禁、语义复核和选择性重建Schema及状态；旧版或损坏成果在复制进公开包前即失败。
- [x] 用38,072字节第二增量归档同步离线包门禁，SHA-256为 `e37d652d27e263b5b92c5b6ee9dd4fad31eff8c2e8e01438a71fab9f47c88b05`，传输前后一致且安全路径检查通过。
- [x] DGX最终106项全量测试和17项路演成果通过，`semantic_review_grounded=true`、`selective_rebuild_bounded=true`，状态为 `READY_WITH_HUMAN_GATES`。
- [x] 重启用户服务后仍为 `active/enabled` 且只监听 `127.0.0.1:7860`；健康检查、`GOLD / FINAL / 5→0 / 4项修订`、语义复核和选择性重建下载均通过，伪造私有下载返回404。
- [x] DGX部署副本提交预检为预期的 `DEVELOPMENT_CHECK`：17项成果、公开边界和106项测试通过，Git与`.env`检查因部署副本缺失对应状态而明确 `SKIPPED`。未处理Docker权限、云安全组或系统防火墙。

## 已完成：操作者审核台与Evidence安全定位

- [x] 新增 `SOP_REVIEW_SESSION` Schema和本地会话存储；目录权限700、会话JSON权限600，全部位于Git忽略的 `outputs/sop_review_sessions/`。
- [x] 审核会话绑定当前Gold最终SOP的SHA-256；SOP版本或内容变化后拒绝继续使用旧会话，避免把审核状态误套到新版本。
- [x] 步骤支持锁定、撤销锁定、人工确认、撤回确认和安全重排；确认必须先锁定，已确认步骤不能直接解锁，所有事件记录 `actor=OPERATOR / automatic=false`。
- [x] 重排同时校验前置依赖和所有已锁定步骤的位置；S11与S12可按依赖安全互换，把S09移到S08之前会被明确拒绝。
- [x] 单步重建只生成目标步骤的简洁/详细/证据三种视图、一个手机检查卡和直接关联测验题；其余12步保持不变，Gold不修改，外部模型调用0。
- [x] Evidence安全定位接口删除私有 `keyframe` 路径，只返回PDF页码或音视频起止时间；视频可关联已审核安全预览，`raw_source_url`固定为空。
- [x] Web新增操作者审核台、锁定/确认/上下移动/单步重建控件和Evidence点击面板；Gold以外结果不能创建审核会话。
- [x] 路演运行验收新增 `evidence_locator_safe` 与 `operator_review_controls`，实测Evidence定位、13步会话、依赖拒绝、锁定确认和S12单步重建全部通过。
- [x] 本机专项25项、全量112项测试和Web JavaScript语法通过；17项路演成果保持 `READY_WITH_HUMAN_GATES`。
- [x] 提交 `2794711` 的19文件增量归档为100,455字节，SHA-256为 `fd6d90c91329c43f9a7e59d504d9bbd2b492dbab0def9926b7bc3a8000ca2934`；本机和DGX一致，归档不含 `.env`、原始输入、私有派生目录或AppleDouble文件。
- [x] DGX Python 3.12全量112项测试和17项路演成果通过；回环Web实测E144安全定位、13步会话、S12单步重建、S11/S12重排、S09非法重排拒绝和S01组合锁定确认均符合约束。
- [x] 重启DGX用户服务后为 `active/enabled` 且只监听 `127.0.0.1:7860`；部署副本预检为预期的 `DEVELOPMENT_CHECK`，112项测试和公开边界通过，Git与`.env`检查明确跳过。未处理Docker权限、云安全组或系统防火墙。

## 已完成：传输容错与可恢复工作流检查点

- [x] Step Plan传输层区分 `TIMEOUT`、`RATE_LIMIT`、`SERVICE_UNAVAILABLE`、`CONNECTION`、`HTTP_CLIENT_ERROR` 和非法HTTP JSON；超时、429、5xx与连接故障最多在总计3次尝试内按0.5/1.0秒退避，确定性4xx不重试。
- [x] 传输错误日志只记录模型、Schema、尝试次数、分类、状态码和退避；测试把Authorization值放入异常消息，最终JSONL没有泄漏该值。HTTP错误正文也不进入异常或日志。
- [x] 新增 `WORKFLOW_RUN` Schema：保存11种状态、每阶段尝试次数、迁移/失败/重跑事件、下游失效范围和最后一次失败；检查点采用原子写入，目录700、JSON权限600。
- [x] 可重试失败可以从原阶段恢复；不可重试失败拒绝自动恢复，只有操作者修复后才能显式重跑。未执行过的阶段不能伪装成“重跑”。
- [x] Gold、候选和模拟闭环统一生成新检查点；当前Gold为9个事件、`COMPLETED`，质检阶段真实执行2次，最后失败为空。
- [x] 离线包发布门禁新增工作流Schema和跨文件摘要绑定：Before SOP、After SOP、Revision Audit语义内容必须与选择性重建报告一致；运行起止时间不影响确定性重跑，但修改动作、路径、前后值、原因或Evidence会在发布前被拒绝。
- [x] 重新生成Gold审计和选择性重建报告，修复旧包中单参数删除路径与当前修订器不一致的问题；重建后的三个摘要绑定和全部选择性验证均通过。
- [x] Web新增“可恢复工作流检查点”面板，路演新增 `workflow_checkpoint_safe=true`；本机关键专项、全量122项、JavaScript语法和17项路演成果通过，状态保持 `READY_WITH_HUMAN_GATES`。
- [x] 从DGX现有部署基线生成26文件、113,145字节主增量归档，SHA-256为 `e681d207deeb05597f1c49157221d0aaa1bc80f6828ac0a29e78d7eebdd19904`；修复测试夹具和语义绑定后再同步12文件、62,783字节增量，SHA-256为 `defbd061efee0163efa18deead25283c0eaf8624fd711628f351c724b3c5cf07`。两次本机/远端哈希一致，均不含敏感路径或AppleDouble文件。
- [x] DGX首次回归发现一项测试错误依赖被Git忽略的旧Gold缓存；产品接口未失败。测试改用Git跟踪的Schema门禁夹具后，DGX Python 3.12全量122项通过。
- [x] DGX真实Gold重跑生成 `COMPLETED / 9事件 / VERIFYING=2` 检查点；不同运行时间下Revision Audit语义摘要仍为 `165936d3641a2742c97502ca140f2c2b4813072623dd129b8968374fee3e0404`，与选择性重建报告一致。
- [x] 重启DGX用户服务后首页显示工作流面板，HTTP现场重跑返回 `5→0 / 4项修订`；服务保持 `active/enabled` 且只监听 `127.0.0.1:7860`。
- [x] DGX路演17项成果和 `workflow_checkpoint_safe=true` 通过；部署副本预检为预期 `DEVELOPMENT_CHECK`，122项测试和公开边界通过，Git与`.env`检查明确跳过，数据策略为无凭证、无原始媒体。未处理Docker权限、系统防火墙或云安全组。
- [x] 八个实际Gold产物阶段已接入统一执行器；完整运行全部重建，指定阶段重跑逐文件验证并复用上游、实际重建所选阶段和下游，不再把状态重置误报为产物重建。

## 已完成：真实产物阶段重跑与原子发布

- [x] 新增 `GOLD_STAGE_RUN` Schema和私有运行存储：每次运行有独立UUID、八阶段执行记录、产物文件名/字节数/SHA-256、工作流摘要、来源运行和无原始媒体/凭证/绝对路径数据策略。
- [x] 完整Gold运行实际执行 `INGESTING → EXTRACTING → PLANNING → CREATING → VERIFYING_INITIAL → REVISING → VERIFYING_FINAL → RENDERING`，稳定复现严重问题5→0、4项局部修订和13步培训成果。
- [x] 从 `RENDERING` 重跑时，前七阶段逐文件哈希校验后标记 `REUSED`，只运行培训成果构建器；从首轮质检重跑时，前四阶段复用、质检至渲染五阶段实际重建。
- [x] 运行先写700权限暂存目录和600权限JSON；全部成功后原子移动到版本目录并替换 `current.json`。当前指针只保存相对路径，读取时再次校验边界、Schema、阶段顺序、全部产物哈希、工作流哈希和发布摘要。
- [x] 注入 `RENDERING` 超时后，失败检查点记录 `retryable=true` 并进入隔离目录，当前发布仍指向上一个完整版本；依赖恢复后同阶段重跑成功。
- [x] Web新增完整运行、八阶段选择和阶段重跑接口，展示当前发布短标识、复用/重建数量和发布摘要；路演 `DEMO_FALLBACKS` 新增 `artifact_stage_rerun=true`，同时核对七个复用阶段前后哈希相同。
- [x] 本机与DGX Python 3.12全量回归均为 `127 passed`；路演17项成果和新增阶段重跑断言全部通过，状态保持 `READY_WITH_HUMAN_GATES`。
- [x] 用禁用AppleDouble的15文件增量归档同步DGX，最终归档104,174字节、SHA-256为 `e3c2da665278f7fe04b48be29aa1683e50c3baf67edfbc14749b46b5bd26eeff`，本机与远端一致；首次归档产生的 `._*` 已在测试前清理，最终部署数量为0。
- [x] DGX实际完整运行重建8阶段，从 `RENDERING` 重跑复用7阶段、重建1阶段，`source_run_id`绑定正确且Gold严重问题仍为5→0；路演 `artifact_stage_rerun=true`。
- [x] DGX用户服务重启PID由467358变为474352，保持 `active/enabled` 并只监听 `127.0.0.1:7860`；Web完整运行重建8阶段，渲染重跑复用7阶段、重建1阶段，当前发布清单为 `published=true`。
- [x] DGX部署副本预检为预期 `DEVELOPMENT_CHECK`：127项测试、17项成果和公开边界通过，凭证/原始媒体均为false；部署目录 `.env`、原始输入和AppleDouble数量均为0。未处理Docker权限、系统防火墙或云安全组。

## 已完成：冲突裁决、人工安全门禁与最终采用结果

- [x] 新增 `CONFLICT_DECISION_SESSION` Schema和私有会话存储，绑定首轮冲突、Revision Audit、建议SOP和最终复检四项SHA-256；会话目录700、JSON权限600，不含原始媒体、绝对路径或凭证。
- [x] 当前Gold五个确定性冲突均生成自动采用理由和最终结果；四项有直接变更路径，一个依赖断裂由缺失步骤插入关联解决，最终状态为 `AUTO_FINALIZED / publishable=true / adopted=5`。
- [x] 路由不信任单一 `automatic` 字段：安全声明、缺失证据、无效证据和 `REVIEW` 动作固定进入人工确认，其余 `automatic=false` 或 `NEEDS_REVIEW` 同样转人工。
- [x] 构造 `automatic=true` 的“100%安全”无依据声明后，裁决仍为 `HUMAN / DEFER_TO_HUMAN / publishable=false`；人工拒绝后状态为 `NEEDS_REVIEW`，重新复核批准后才变为 `FINALIZED / publishable=true`。
- [x] 人工决定记录操作者、时间、说明和事件；会话来源变化时拒绝继续，自动路由冲突不能伪装成人工覆盖，说明中的Bearer凭证模式在落盘前脱敏。
- [x] Web新增“冲突裁决与最终采用”面板、会话创建、逐项批准/拒绝和汇总状态；Gold自动裁决接口返回5项全部采用，非法人工覆盖返回400。
- [x] 路演 `DEMO_FALLBACKS` 新增 `conflict_decision_auditable=true` 与 `safety_conflict_human_gate=true`；本机与DGX Python 3.12全量均为 `131 passed`，17项成果通过，状态保持 `READY_WITH_HUMAN_GATES`。
- [x] 用13文件、105,312字节的安全增量归档同步DGX，SHA-256为 `9b1a800150c1aca87edbfe00c1ef186ddc986a53a799eaba568d8cab4d1a0e94`，本机与远端一致；归档和部署目录均不含 `.env`、原始输入、私有派生目录或AppleDouble文件。
- [x] DGX用户服务重启PID由474352变为479614，保持 `active/enabled` 且只监听 `127.0.0.1:7860`；回环Web返回冲突裁决面板、Gold五项自动采用和400非法人工覆盖拒绝，两项新增路演断言均为true。
- [x] DGX部署副本预检按无Git边界返回预期 `DEVELOPMENT_CHECK`：131项测试、17项成果和公开边界通过，5项人工门禁保留；凭证、原始媒体、`.env`、原始输入和AppleDouble均为0。未处理Docker权限、系统防火墙或云安全组。

## 已完成：ASR人工快速修正与Evidence摘要重绑

- [x] 新增 `ASR_CORRECTION_SESSION` Schema和700/600权限私有原子存储；会话绑定专家转写、Gold SOP和原始ASR三项SHA-256，源变化后拒绝继续写入。
- [x] 12段口述逐项保留原始ASR、受控术语基线、当前修正值、时间点和E143–E154映射；每次修正记录前后文本、操作者、原因、时间和新旧Evidence绑定摘要。
- [x] Evidence摘要由Evidence ID、来源、声明、录音时间点和当前校正文本共同计算；修正、再次修正和恢复基线均形成连续摘要链，不直接覆盖Gold或原始转写。
- [x] 修正值、操作者和原因在落盘前执行凭证模式脱敏；必要术语不能被删除，空值、同值、超长值、未知问题和来源变化均被拒绝。
- [x] Web新增“专家口述 ASR 快速修正”面板和创建、读取、逐段修正接口；非Gold结果不能创建会话，页面明确私有存储和零外部模型调用边界。
- [x] 路演 `DEMO_FALLBACKS` 新增 `asr_correction_auditable=true`，实际创建会话、修正Q02并验证原始文本不变、Evidence摘要变化和私有数据策略。
- [x] 本机与DGX Python 3.12全量均为 `135 passed`，17项成果和三种演示兜底通过，状态保持 `READY_WITH_HUMAN_GATES`。
- [x] 用14文件、109,234字节的Git归档安全同步DGX，SHA-256为 `678ee655cfaff8b02ca52b06272c1d3f9145bb54b313ad5760e22cfc40326d3e`，本机与远端一致；不含 `.env`、原始输入、私有派生目录、会话数据或AppleDouble文件。
- [x] DGX用户服务重启后PID为486316，保持 `active/enabled` 且只监听一个 `127.0.0.1:7860`；回环Web创建12段会话、修正Q02、持久化读取、原始文本保留、摘要重绑和必要术语400拒绝均通过。
- [x] DGX路演 `asr_correction_auditable=true`，17项成果和5项人工门禁状态正确；实际会话目录权限700、JSON权限600。
- [x] DGX部署副本预检按无Git边界返回预期 `DEVELOPMENT_CHECK`：135项测试、17项成果和公开边界通过；凭证、原始媒体、`.env`、原始输入和AppleDouble均为0。未处理Docker权限、系统防火墙或云安全组。

## 已完成：案例交付配置与低码率视频预览（本机）

- [x] 新增严格 `CASE_OUTPUT_PROFILE` Schema和N31实例，固定新操作员、初学者、设备旁任务支持、`zh-CN`简体中文普通话、源视频180–480秒、培训视频60–90秒且目标80秒。
- [x] 六类必需输出固定为结构化SOP、手机检查清单、培训测验、A4海报、MP4培训视频和修订审计；缺类型、重复类型、错误格式或非必需状态均拒绝。
- [x] 新增低码率预览生成器、命令行入口和一键脚本；只接受 `LOCAL_QA_PASSED` 视频，禁止第三方来源和外部模型调用，输出采用原子发布。
- [x] 6段自摄安全视频生成总计420.430秒、24,620,361字节预览，均为852×480、15 fps、H.264/AAC；最大平均总码率471.701 kbps，源/预览时长最大偏差33毫秒，全部QA通过。
- [x] 每项清单绑定来源角色、源/预览SHA-256、时长、编码、分辨率、帧率、码率、faststart和源0点到预览0点的一一时间轴映射；结构化报告不含媒体路径、绝对路径、凭证或原始内容。
- [x] 实际MP4目录权限700、文件权限600且被Git忽略；Web逐文件核验大小和SHA-256后才提供Range播放，未携带私有预览时只展示结构化映射。
- [x] Web新增交付配置与6段预览面板；训练视频渲染入口直接校验case、语言、目标时长和MP4必需输出，防止外围配置与实际渲染脱节。
- [x] 路演新增 `output_profile_complete=true`、`low_bitrate_previews_mapped=true` 和 `video_preview_configured=true`；本机全量 `141 passed`，前端JavaScript语法通过，状态保持 `READY_WITH_HUMAN_GATES`。
- [x] 用21文件、130,761字节的安全增量归档同步DGX，SHA-256为 `98fc651c54fb67485b35983e44ad93908750944b254d0580f97cd76e68a29a27`，本机与远端一致；归档不含 `.env`、原始输入、私有预览或AppleDouble文件。
- [x] DGX从已有安全视频目录生成6段预览，总计420.420秒、24,637,616字节，最大平均总码率471.846 kbps；全部QA通过，目录700、文件600，外部模型调用0。
- [x] DGX Python 3.12全量 `141 passed`；路演三项新增断言均为true，状态保持 `READY_WITH_HUMAN_GATES`。用户服务重启PID由486316变为498533，保持 `active/enabled` 且只有一个 `127.0.0.1:7860` 监听。
- [x] DGX回环Web返回新操作员、`zh-CN`、目标80秒和6/6可用预览；主视频预览Range请求返回HTTP 206和32字节有效MP4头。
- [x] DGX部署副本预检按无Git边界返回预期 `DEVELOPMENT_CHECK`：141项测试、17项成果和公开边界通过；凭证、原始媒体、`.env`、原始输入和AppleDouble均为0。未处理Docker权限、系统防火墙或云安全组。

## 已完成：五类Agent与可审计工具执行追踪（本机）

- [x] 新增严格 `AGENT_TOOL_EXECUTION_TRACE` Schema和生成器，固定Perception、SOP、Creator、Verifier、Revision五类职责、禁止动作、授权工具及执行顺序。
- [x] 登记PDF页、关键帧、视频/录音区间、Evidence检索、SOP规划、保存、渲染、规则/语义质检、局部修订、选择性重建和人工确认13个真实实现入口。
- [x] 14次实际工具调用全部绑定输入/输出成果ID和执行依据；5次Agent交接明确包含 `Creator → Verifier → Revision → Verifier` 的发现、局部修订与复检环。
- [x] 19个真实成果逐项记录类型、分级、大小和SHA-256；生成和Web读取时都重新核验当前文件，任一成果漂移、未知引用或越权工具都会拒绝。
- [x] 公开追踪不含成果路径、绝对路径、凭证或原始媒体；生成追踪本身不调用外部模型，历史模型结果明确标为授权模型推断或文本TTS执行依据。
- [x] Web新增“Agent工具链”面板、独立 `/api/n31/agents` 接口和白名单下载；页面展示5 Agent、13工具、14调用、19哈希、5交接和1次人工确认。
- [x] 路演必需成果增至18项，新增 `five_agent_tool_trace=true` 和 `agent_tool_interface_auditable=true`；本机全量 `145 passed`、前端JavaScript语法和18项成果通过，状态保持 `READY_WITH_HUMAN_GATES`。
- [x] 用19文件、130,756字节的安全增量归档同步DGX，SHA-256为 `d7bb153689d179f112f947aee5ab4c9f046288f6b31ded8a7202ae82301a2fb4`，本机与远端一致；归档不含 `.env`、原始输入、媒体、私有输出或AppleDouble文件。
- [x] DGX重新生成Agent追踪并复核5 Agent、13工具、14调用、19成果、5交接、14次证据绑定和1次人工确认；`Revision → Verifier` 复检环为true，追踪生成外部模型调用0。
- [x] DGX Python 3.12全量 `145 passed`，18项成果、`five_agent_tool_trace=true` 和 `agent_tool_interface_auditable=true`；路演状态保持 `READY_WITH_HUMAN_GATES`。
- [x] DGX用户服务重启PID由498533变为505911，保持 `active/enabled` 且只有一个 `127.0.0.1:7860` 监听；Gold负载与独立Agent接口完全一致，白名单下载HTTP 200且内容一致。
- [x] DGX部署副本预检按无Git边界返回预期 `DEVELOPMENT_CHECK`：145项测试、18项成果和公开边界通过；凭证、原始媒体、`.env`、原始输入和AppleDouble均为0。未处理Docker权限、系统防火墙或云安全组。

## 已完成：八阶段资源统计（本机）

- [x] 为Gold阶段运行清单新增向后兼容的 `metrics` 契约；旧清单可继续读取，新建的完成、复用和失败阶段都必须由执行器写入指标。
- [x] 每阶段记录单调时钟耗时、CPU用户/系统时间增量、进程累计峰值RSS、阶段输出字节和外部模型调用数；macOS按字节、Linux按KiB正确换算 `ru_maxrss`。
- [x] `output_bytes` 必须等于该阶段全部输出记录之和，进程峰值必须大于0，外部模型调用固定为0；失败注入阶段同样保留资源数据。
- [x] Web“可恢复工作流”面板新增阶段总耗时和峰值RSS，说明CPU/耗时为阶段增量、RSS为进程累计峰值，不把它冒充单阶段独占内存。
- [x] 路演新增 `stage_resource_metrics_recorded=true`，在完整运行后从 `RENDERING` 重跑并核对8阶段全部指标、资源范围和输出字节。
- [x] 本机全量 `145 passed`、前端JavaScript语法、18项成果和三种演示兜底通过，状态保持 `READY_WITH_HUMAN_GATES`。
- [x] 以增量包 `skillforge-1a87b64-clean.tar.gz` 安全同步DGX；两端SHA-256均为 `3d4ce1a68cc11acde89e9bfffdff2f4c299f1bf8b4bdc9743b465bb1f8ee0687`，包内无 `.env`、输入、私有派生物、媒体或AppleDouble。
- [x] DGX完整运行重建8阶段：合计148.013毫秒、CPU用户/系统时间0.075667/0.002587秒、进程峰值RSS 86,425,600字节、输出708,851字节、外部模型调用0。
- [x] DGX从 `RENDERING` 重跑复用7阶段、只重建1阶段：合计41.221毫秒、CPU用户/系统时间0.027172/0.000042秒、进程峰值RSS 86,425,600字节、输出708,851字节；全部8阶段资源字段逐项通过。
- [x] DGX全量 `145 passed`、18项路演成果、`stage_resource_metrics_recorded=true`、Web资源字段与三项界面标签通过；部署预检为预期 `DEVELOPMENT_CHECK`。用户服务重启PID由505911变为512891，保持 `active/enabled` 且只有一个 `127.0.0.1:7860` 监听。

## 已完成：私有人工门禁确认器

- [x] 新增严格 `human_gate_confirmations` Schema和原子私有存储；目录权限700、JSON权限600，位置在Git忽略的 `outputs/submission/`。
- [x] 新增 `status / confirm / revoke / reset-stale` 命令。自动测试不能替代人工确认；确认必须显式提供确认人和本地证据文件或安全HTTPS网址。
- [x] 本地文件证据只记录定位、大小和SHA-256，不复制证据内容；安全状态输出和提交预检不包含确认人、说明或证据路径。HTTPS网址拒绝账号、密码、查询参数和片段，避免把签名参数写入私有记录。
- [x] 每项确认绑定完整路演运行单哈希和当前门禁文案；证据删除、大小或哈希变化、运行单变化、门禁文案变化、未知门禁、重复记录或权限放宽都会失效。
- [x] 提交预检新增 `HUMAN_GATE_CONFIRMATIONS` 检查；无私有记录时保持既有五项 `PENDING`，五项有效确认可将最终提交预检推进到 `READY_FOR_SUBMISSION`，但不能覆盖Git、测试或成果失败。公开路演验收文件仍保持不带私有状态的可复现结果。
- [x] 本机相关专项35项、全量 `159 passed`、Python编译、Shell语法和 `git diff --check` 通过；实际默认私有确认仍为0/5，没有替参赛者关闭任何门禁。
- [x] 以16文件、105,093字节的安全增量包 `skillforge-6c212ef-clean.tar.gz` 同步DGX；两端SHA-256均为 `4f81e5c480a09a36d3f62f3d5e620355e9f42ab922617977701ecde57dc3bcb8`，不含 `.env`、人工确认、证据、输入、私有派生物、媒体或AppleDouble。
- [x] DGX Python 3.12全量 `159 passed`；缺省确认记录不存在，`status` 返回0/5和5项 `PENDING`。部署预检为预期 `DEVELOPMENT_CHECK`，人工确认检查通过、18项成果通过、159项测试通过，退出码2。
- [x] DGX提交报告目录700、报告文件600；部署目录 `.env`、原始输入和AppleDouble均为0。用户服务重启PID由512891变为524247，保持 `active/enabled` 且只有一个 `127.0.0.1:7860` 监听；Gold仍为 `GOLD / FINAL / COMPLETED`，8阶段资源字段完整。

## 已完成：官方规则公开核验快照与提交预检门禁

- [x] 2026-07-18重新核对NVIDIA赛事公开说明和训练营交互页面；公开确认主题、多模态Agent方向、DGX Spark平台、2–5人组队、一人一队、提交/评审/决赛日程共8项。
- [x] 匿名页面可见“黑客松赛事规则说明”课程标题，但该行没有公开讲义下载；3个讲义控件均与后续技术课程对齐。未登录、未提交报名信息、未触发下载，也未发现单独公开规则正文或附件链接。
- [x] 将评分权重、提交字段、视频要求、外部API、开源和现场运行要求6项保持为 `OFFICIAL_DETAIL_REQUIRED`；这表示公开匿名访问未取得细则，不表示赛事不存在相应要求。
- [x] 新增 `official_rules_status` 严格Schema和 `config/official_rules_status.json` 受控快照；状态、公开项、未知项、来源和访问审计必须一致，不能把仍有未知项的快照标成已取得官方细则。
- [x] 提交预检新增 `OFFICIAL_RULES_STATUS` 自动检查。机器检查只验证核验快照诚实、完整且未被误关闭；不会移除或确认 `OFFICIAL_RULES_VERIFIED` 人工门禁。
- [x] 本机提交专项9项、全量 `163 passed`、Python编译、Shell语法、内嵌Web JavaScript语法和 `git diff --check` 通过；开发预检显示公开确认8项、待官方细则6项，五项人工门禁仍为0/5。
- [x] 功能提交 `11acace` 的本机干净预检为 `READY_WITH_HUMAN_GATES`：163项测试、271个跟踪文件、18项公开成果和密钥实际值扫描通过，`.env` 权限600且受Git忽略，五项人工门禁保持 `PENDING`。
- [x] 以10文件、77,736字节安全增量包同步DGX；两端SHA-256均为 `0b0adc277d6a595c3353961a31a6851c729a90cebf31955b60055ebe0bdedfc2`，包内没有 `.env`、输入、派生物、媒体、私有确认或AppleDouble。
- [x] DGX Python 3.12全量 `163 passed`；部署预检为预期 `DEVELOPMENT_CHECK`，规则快照8项已确认/6项待细则、人工确认0/5，数据策略无绝对路径、凭证或原始媒体。用户服务保持PID 524247、`active/enabled`，且只有一个 `127.0.0.1:7860` 监听。

## 已完成：最终有声录屏机器QA与人工门禁绑定

- [x] 新增最终录屏内部策略，明确175–180.5秒、1080p/30fps等范围仅为三分钟路演彩排目标，`official_video_requirements_verified=false`，不能冒充尚未取得的官方视频规则。
- [x] 新增严格策略和QA报告Schema；`READY_FOR_HUMAN_REVIEW` 强制16项机器检查全部通过，字幕、隐私、完整播放和音画同步固定为 `PENDING` 人工复核。
- [x] 新增原生Python + FFmpeg检查器和 `scripts/check_final_recording.sh`：验证私有权限、固定MP4文件名、时长、画面/音频编码、采样率、响度、真峰值、累计/最长静音和累计/最长黑屏。
- [x] 私有QA报告只保存安全逻辑文件名、媒体哈希、大小、技术参数和检查结果；不含媒体、绝对路径、凭证或外部模型调用，目录0700、文件0600。
- [x] `FINAL_RECORDING_REVIEW` 人工门禁只接受本地MP4，并绑定同一媒体SHA-256、大小、机器QA通过状态和当前策略SHA-256；媒体或策略变化、报告缺失或失效后确认失效。
- [x] 录屏尚未产生时，一键命令安全返回退出码1和“最终录屏不存在或为空”，不泄露绝对路径，也不创建虚假报告或人工确认。相关专项31项通过。
- [x] 本机全量 `172 passed`、Python编译、全部Shell脚本、内嵌Web JavaScript和 `git diff --check` 通过；开发预检保持人工门禁0/5、规则细则6项待确认，未因新增工具误关门禁。
- [x] 功能提交 `c739758` 的本机干净预检为 `READY_WITH_HUMAN_GATES`：172项测试、277个跟踪文件、18项公开成果、`.env`忽略/600权限和密钥实际值扫描通过，人工门禁保持0/5。
- [x] 以14文件、88,179字节安全增量包同步DGX；两端SHA-256均为 `8feb0adb5dfb226e9a1ffd71448ff8ad1921f17bd1e51e7fbd4201dfe565607c`，包内无 `.env`、输入、派生物、媒体、私有录屏、QA报告、人工确认或AppleDouble。
- [x] DGX Python 3.12全量 `172 passed`；缺录屏命令安全退出1，部署预检为预期 `DEVELOPMENT_CHECK`、人工确认0/5。服务保持PID 524247、`active/enabled`和单一 `127.0.0.1:7860` 监听。

## 已完成：18项成果发布冻结与公开链接核验边界

- [x] 新增六类发布职责和严格角色配置；18项路演成果逐项绑定负责人角色、不同的最终检查角色和发布渠道，真实姓名映射明确保持为私有待填写状态。
- [x] 新增严格角色/发布清单Schema、确定性生成器和一键脚本；清单记录18项成果相对路径、SHA-256、大小、责任角色、终检角色和渠道，技术冻结摘要在时间戳变化时保持稳定，成果内容漂移时安全失败。
- [x] 当前发布清单状态为 `TECHNICAL_ARTIFACTS_FROZEN_HUMAN_GATES_PENDING`：18项成果、6类角色、3个公开入口和5个人工门禁边界全部匹配；清单作为控制面文件，不加入自身哈希集合。
- [x] 提交预检新增 `RELEASE_FREEZE_MANIFEST` 检查；无法用缺失、重复或同一责任/终检角色的配置冒充已冻结，也不会因技术冻结自动关闭五个人工门禁。
- [x] 新增作品页、代码仓库和最终录屏三个公开入口的私有输入/QA Schema及匿名检查器。未填写链接时安全退出1；只允许无凭证、无片段的HTTPS公网地址和HTTPS重定向，最终连接IP也必须为公网，报告不保存真实网址、域名、IP、正文、Authorization或Cookie。
- [x] 私有链接输入和QA报告固定在Git忽略的 `outputs/submission/`，父目录0700、文件0600；真实上传未发生，因此没有创建实际输入或伪造通过报告。
- [x] 本机发布、链接和提交专项 `29 passed`，全量 `192 passed`；Python编译、两个新增Shell脚本语法、发布清单严格复核和 `git diff --check` 通过。
- [x] 功能提交 `60274f2` 的本机正式预检为 `READY_WITH_HUMAN_GATES`：192项测试、289个Git文件、18项冻结成果、`.env`忽略/600权限和实际密钥值扫描通过，人工门禁保持0/5。
- [x] 21文件、98,136字节安全增量包同步DGX，两端SHA-256均为 `1ef9ab7bbf4c507b472999f4a9392ca61ac9813f733060b9c096e937ac0644b2`；首轮冻结复核发现远端 `AGENT_TOOL_TRACE` 为同大小旧字节版本，单文件补齐后18项哈希全部与本机一致。
- [x] DGX Python 3.12全量 `192 passed`；发布清单摘要一致，缺少真实链接时命令安全退出1，部署预检为预期 `DEVELOPMENT_CHECK`、人工门禁0/5。用户服务保持 `active/enabled` 和单一 `127.0.0.1:7860` 监听。

## 已完成：私有团队名单与职责映射准备

- [x] 新增严格私有团队名单Schema：草稿不生成姓名，预列六类发布职责；就绪状态强制2–5位成员、姓名/单位、唯一主联系人、逐人报名确认和一人一队确认。
- [x] 新增团队名单安全QA Schema、原子0600存储和 `scripts/check_team_roster.sh`；初始化拒绝覆盖已有文件，父目录必须0700，输入和QA均位于Git忽略的 `outputs/submission/`。
- [x] 六类职责必须各出现一次并引用现有成员；人数、重复成员编号、0或多个主联系人、未确认报名/一人一队、缺失/重复角色、未知成员和权限放宽都会安全失败。
- [x] QA只保存输入SHA-256、人数、角色数和八项布尔检查；不保存姓名、单位或成员编号，固定为 `READY_FOR_HUMAN_CONFIRMATION / human_gate_status=PENDING`，不会替参赛者确认资格。
- [x] 提交预检新增 `TEAM_ROSTER_PRIVATE_STATE`：团队资格门禁未确认时允许名单保持 `ABSENT`；门禁确认后，缺失或无效名单会使预检进入 `NOT_READY`，有效名单也不能反向自动关闭门禁。
- [x] `TEAM_ELIGIBILITY_CONFIRMED` 人工确认强制绑定当前名单SHA-256和 `READY_FOR_HUMAN_CONFIRMATION` QA；名单缺失、变更或QA失效后旧确认自动过期，旧版无绑定确认也不能继续生效。
- [x] 团队名单、人工门禁和提交专项 `40 passed`，本机全量 `208 passed`；Python编译、新Shell脚本语法、缺名单安全退出和 `git diff --check` 通过。当前没有创建实际团队名单或QA报告，人工确认仍为0/5。
- [x] 功能提交 `7247a3b` 的本机正式预检为 `READY_WITH_HUMAN_GATES`：208项测试、294个Git文件、18项冻结成果、`.env`忽略/600权限和实际密钥值扫描通过，私有团队名单与人工确认均为 `ABSENT`。
- [x] 17文件、100,280字节安全增量包同步DGX，两端SHA-256均为 `bb6309f46ecac818696da965b276048b70d2937ab805afdd0a04d0ea58c1be19`；包内没有 `.env`、实际名单、QA、人工确认、报名证据、输入、派生物、媒体或AppleDouble。
- [x] DGX Python 3.12全量 `208 passed`；缺名单命令安全退出1，部署预检为预期 `DEVELOPMENT_CHECK`、名单 `ABSENT`、人工确认0/5。用户服务保持 `active/enabled` 和单一 `127.0.0.1:7860` 监听。

## 已完成：提交倒排任务看板与风险清单

- [x] 新增严格P0任务看板：10项任务全部绑定截止日、六类责任角色、下一动作和证据类型；五项人工门禁各出现一次，所有任务截止日不晚于2026-07-22。
- [x] 状态词仅允许 `COMPLETED / READY / AWAITING_HUMAN / AWAITING_EXTERNAL`，不使用含混的 `BLOCKED`。2026-07-18基线为1项完成、1项技术就绪、7项等待人、1项等待官方材料，明确 `implementation_goal_blocked=false`。
- [x] 建立7项风险：官方规则、最终录屏、团队资格、公开链接、敏感数据、DGX连接和Docker权限；Docker固定为 `ACCEPTED / NON_BLOCKING`，只有决定本地GPU模型或VSS时再处理。
- [x] 新增 `scripts/check_project_board.sh` 和安全状态Schema；当前为 `ON_TRACK`、下一截止日7月20日，过期任务进入 `ATTENTION_REQUIRED`，截止日后未完成进入 `DEADLINE_MISSED`，均列出不含个人信息的任务ID。
- [x] 提交预检新增 `PROJECT_BOARD_STATUS`，只在看板诚实且按期时通过；任务/风险/日检命令必须是冻结集合，拒绝同数量替换和绝对路径，不会因为看板通过而关闭任何人工门禁。相关专项 `48 passed`，本机全量 `216 passed`。
- [x] 功能提交 `ae46d8f` 的本机正式预检为 `READY_WITH_HUMAN_GATES`：216项测试、300个Git文件、看板 `ON_TRACK / 实现目标受阻=false`、18项冻结成果和密钥扫描通过，人工确认仍为0/5。
- [x] 15文件、96,063字节安全增量包同步DGX，两端SHA-256均为 `6f9b8d5ac96d9c353518a911902e3d2777cba59b8d23b788cf4bb9a933e7e6ce`；包内没有 `.env`、私有名单、链接、录屏、人工确认、输入、派生物、媒体或AppleDouble。
- [x] DGX Python 3.12全量 `216 passed`；看板同为 `ON_TRACK / implementation_goal_blocked=false`，部署预检为预期 `DEVELOPMENT_CHECK`、人工确认0/5。用户服务保持 `active/enabled` 和单一 `127.0.0.1:7860` 监听。

## 已完成：180秒最终真人彩排记录与门禁绑定

- [x] 新增内部彩排策略，固定7段运行单和175–180秒目标，并明确 `INTERNAL_STAGE_REHEARSAL_TARGET_NOT_OFFICIAL_RULE / official_video_requirements_verified=false`，不会冒充尚未取得的官方视频规则。
- [x] 新增 `scripts/check_final_rehearsal.sh`：初始化Git忽略的私有模板且拒绝覆盖；输入和QA必须位于0700目录、0600文件中。机器检查7段顺序、冻结计划时间、实际连续时间线、总时长，以及逐段讲解、操作、证明点、兜底和三项总体验收。
- [x] 安全QA只保存记录/运行单/策略SHA-256、字节数、分段数、时长和布尔结果，不复制私有备注或绝对路径；全部通过后也只进入 `READY_FOR_HUMAN_CONFIRMATION / PENDING`。
- [x] `FINAL_STAGE_REHEARSAL` 现在只接受本地计时记录，并强制绑定同目录QA、当前运行单和内部策略；网址、缺报告、权限放宽、记录/运行单/策略漂移均不能关闭人工门禁。
- [x] 提交预检新增 `FINAL_REHEARSAL_PRIVATE_STATE`：实际记录不存在时诚实显示 `ABSENT` 并保留门禁；一旦存在，草稿、缺QA或失效QA会令预检失败，不会用任意文本证据冒充180秒彩排。
- [x] 彩排、人工门禁、提交和看板专项 `48 passed`，本机全量 `231 passed`；Python编译、全部Shell语法、缺彩排记录安全退出和 `git diff --check` 通过。当前没有创建实际彩排记录、QA或人工确认，门禁仍为0/5。
- [x] 功能提交 `9792192` 的本机正式预检为 `READY_WITH_HUMAN_GATES`：231项测试、307个Git文件、18项冻结成果、看板 `ON_TRACK / 实现目标受阻=false`、`.env`忽略/600权限和实际密钥值扫描全部通过；彩排状态诚实显示 `ABSENT`。
- [x] 18文件、108,635字节安全增量包同步DGX，两端SHA-256均为 `ecad46a3e732cbf388f42d8d35b81ca3e86376ea323b91b3a16eb1d7c816966e`；包内没有 `.env`、私有彩排、QA、人工确认、输入、派生物、媒体或AppleDouble。
- [x] DGX Python 3.12全量 `231 passed`；缺记录命令安全退出1，部署预检为预期 `DEVELOPMENT_CHECK`、彩排 `ABSENT`、人工确认0/5。用户服务保持 `active/enabled` 和单一 `127.0.0.1:7860` 监听。

## 已完成：80秒培训成片完整观看记录与门禁绑定

- [x] 新增Git忽略的私有观看审核表和 `scripts/check_training_video_review.sh`；初始化预绑定当前80秒成片和生成清单SHA-256，采用0700目录、0600文件和原子替换，已有记录时拒绝覆盖。
- [x] 就绪审核必须由参赛者填写观看时间和播放方式，并逐项确认完整播放、旁白可听、节奏可接受、音画同步、13步可理解、无敏感内容、无播放损坏和接受最终剪辑；任一false都不能生成就绪QA。
- [x] 检查器重新验证生成清单严格Schema、`READY_FOR_HUMAN_REVIEW`边界、自动QA、AI辅助抽帧检查及当前MP4哈希/大小；QA只保存哈希、时长和布尔结果，不复制私有备注、路径或媒体。
- [x] `TRAINING_VIDEO_FULL_WATCH` 现在只接受固定名称的本地审核记录，并绑定同目录QA、当前成片和生成清单；网址、视频本身、缺报告、权限放宽或任一内容漂移均不能关闭人工门禁。
- [x] 提交预检新增 `TRAINING_VIDEO_REVIEW_PRIVATE_STATE`；实际记录不存在时显示 `ABSENT` 并保留门禁，存在草稿、缺QA或过期状态时安全失败。公开运行单同步写明观看/彩排/录屏三类私有证据路径，发布冻结摘要更新为 `d4020009e560e0ca1c7fee814b46675b60cc06a4a028778ccf92b66302f5071b`。
- [x] 观看审核、门禁、提交、彩排、看板、路演和发布专项 `87 passed`，本机全量 `246 passed`；Python编译、全部Shell语法、缺观看记录安全退出和 `git diff --check` 通过。当前没有创建实际观看记录、QA或人工确认，门禁仍为0/5。
- [x] 功能提交 `50c844b` 的本机正式预检为 `READY_WITH_HUMAN_GATES`：246项测试、312个Git文件、18项新冻结成果、看板 `ON_TRACK / 实现目标受阻=false`、`.env`忽略/600权限和实际密钥值扫描全部通过；观看和彩排状态均为 `ABSENT`。
- [x] 20文件、121,379字节安全增量包同步DGX，两端SHA-256均为 `6178975c1e009d47bab29e0084858ce67842a4e8578f0860b139de56bbd299b1`；包内没有 `.env`、私有观看/彩排、QA、人工确认、输入、派生物、媒体或AppleDouble。
- [x] DGX Python 3.12全量 `246 passed`；发布冻结严格复核通过，缺观看记录命令安全退出1，部署预检为预期 `DEVELOPMENT_CHECK`、观看 `ABSENT`、人工确认0/5。用户服务保持 `active/enabled` 和单一 `127.0.0.1:7860` 监听。

## 已完成：官方规则私有六项审核与门禁绑定

- [x] 新增 `scripts/check_official_rules_review.sh` 和两份严格Schema：初始化Git忽略的私有六项审核表且拒绝覆盖；审核表和QA必须位于0700目录、0600文件中。
- [x] 官方来源可复制为私有PDF、网页、文本或图片，也可绑定无账号、查询参数和片段的HTTPS网址；本地来源固定复制到私有目录并绑定SHA-256、大小和相对定位，网址只以哈希进入QA。
- [x] 就绪审核必须按公开快照中的固定顺序核对评分权重、提交字段、视频要求、外部API、开源和现场运行六项，逐项填写非空结论与原文定位、人工确认，并完成五个总检查。
- [x] 安全QA只保存审核表/公开快照/来源哈希、来源类型、六项ID和布尔结果，不复制规则正文、私有备注、来源地址或绝对路径；通过后仍为 `READY_FOR_HUMAN_CONFIRMATION / PENDING`。
- [x] `OFFICIAL_RULES_VERIFIED` 只接受固定名称的本地审核记录，并强制复核同目录QA、当前公开快照及来源绑定；任意讲义、任意网址、缺QA、权限放宽、审核表/来源/快照漂移都不能关闭门禁。
- [x] 提交预检新增 `OFFICIAL_RULES_REVIEW_PRIVATE_STATE`；实际记录不存在时诚实显示 `ABSENT` 并保留门禁，存在草稿、缺QA或过期状态时安全失败。公开运行单和任务看板均改为引用私有审核记录，发布冻结摘要更新为 `53d27d6b75b3825d0ccb5d60a1f2bbc45959d585466584c9c580b0dff29c4953`。
- [x] 首轮规则审核、人工门禁、提交、路演、发布和看板联合回归 `76 passed`。当前没有取得或创建实际官方规则审核记录、来源副本、QA或人工确认，官方规则门禁仍为 `ABSENT / PENDING`。
- [x] 本机全量 `263 passed`；Python编译、全部Shell语法、发布冻结严格复核、缺审核记录安全退出1、16项开发预检和 `git diff --check` 通过。开发预检为预期 `DEVELOPMENT_CHECK`，五项人工确认仍为0/5。
- [x] 功能提交 `9a0ae47` 的本机干净预检为 `READY_WITH_HUMAN_GATES`：263项测试、317个Git文件、18项冻结成果、看板 `ON_TRACK / 实现目标受阻=false`、`.env`忽略/600权限和实际密钥值扫描通过；官方规则审核与人工确认均为 `ABSENT`。
- [x] 从 `623b8f4` 到 `9a0ae47` 生成22文件、132,521字节安全增量包；本机与DGX SHA-256均为 `f4a5a3c63741d90b3ccb4cbf64a7444906d09bf4b78d5ac9e8a29a8d5b513f4f`。包内没有 `.env`、私有审核表、规则来源、QA、人工确认、输入、派生物、媒体或AppleDouble。
- [x] DGX Python 3.12全量 `263 passed`；Python编译、全部Shell语法、发布冻结严格复核、缺规则记录安全退出1和部署预检通过。部署预检为预期 `DEVELOPMENT_CHECK`、规则审核 `ABSENT`、人工确认0/5；用户服务业务验证通过，保持 `active/enabled` 和单一 `127.0.0.1:7860` 监听。

## 已完成：提交成功回执私有证据链准备

- [x] 新增 `scripts/check_submission_receipt.sh` 和两份严格Schema：私有审核表初始化拒绝覆盖，提交成功截图或PDF复制到Git忽略的专用目录并绑定SHA-256和大小，所有私有目录0700、文件0600。
- [x] 回执审核表要求带时区的提交/复核时间、私有提交编号或回执标识，以及成功状态、项目身份、编号记录、最终版本、作品页重开、公开链接复查和无凭证七项人工确认；程序不通过图像猜测这些事实。
- [x] 回执QA强制绑定固定 `submission_preflight_final.json`：必须为 `READY_FOR_SUBMISSION`、工作树干净、无待确认门禁，且精确16项检查按冻结顺序全部通过；同数量替换不能冒充最终预检。
- [x] QA同时重新验证当前18项发布冻结清单，并要求公开链接QA为三项全部通过、绑定当前私有网址输入且检查时间不早于提交时间；旧预检、失败/过期链接或变化后的截图均不能归档。
- [x] 安全QA只保存回执/审核表/预检/发布清单/链接输入与QA哈希、数量、源码提交和布尔结果；不复制截图、提交编号、网址、个人信息、备注、凭证或绝对路径，状态为 `READY_FOR_ARCHIVE`。
- [x] 回执、公开链接、提交预检和看板首轮联合回归 `43 passed`。当前尚未提交，因此实际回执审核、截图来源、最终预检副本和QA均保持不存在，不伪造 `READY_FOR_ARCHIVE`。
- [x] 本机全量 `276 passed`；Python编译、全部Shell语法、发布冻结严格复核、缺回执安全退出1、开发预检和 `git diff --check` 通过。当前五项人工门禁仍为0/5，正式状态不受测试样例影响。
- [x] 功能提交 `e58a4a2` 的本机干净正式预检为 `READY_WITH_HUMAN_GATES`：276项自动测试、322个Git文件、18项冻结成果、工作树干净、`.env`受Git忽略且权限600、实际密钥值泄漏0；私有回执、来源、最终预检和QA均为 `ABSENT`，人工确认0/5。
- [x] 从 `212273c` 到 `e58a4a2` 生成14文件、112,605字节安全增量包；本机与DGX SHA-256均为 `17a5f54f20c2a70a1a4ac59b93b53afa447572b89c3fd0a7763d5e7986b1b5a7`。包内没有 `.env`、私有回执、来源、最终预检、公开链接记录、原始输入、私有派生物、媒体或AppleDouble。
- [x] DGX Python 3.12全量 `276 passed`；Python编译、全部Shell语法和18项发布冻结严格复核通过。缺少实际回执时检查器安全退出1；部署预检为预期 `DEVELOPMENT_CHECK`、退出码2，276项测试通过、规则审核 `ABSENT`、人工确认0/5，数据策略无绝对路径、凭证或原始媒体。
- [x] DGX部署副本无 `.env`、私有回执审核/QA、回执来源、最终预检、公开链接记录或AppleDouble。用户服务保持PID 524247、`active/enabled`、`native-python / docker_required=false`，且只有一个 `127.0.0.1:7860` 监听。

## 已完成：18项成果确定性公开技术交付包

- [x] 新增 `scripts/build_public_release_bundle.sh`、两份严格Schema和独立构建/验真模块；默认输出到Git忽略的 `outputs/release/`，目录0700、ZIP与QA文件0600。
- [x] ZIP精确包含18项冻结成果、当前发布冻结清单和规范包内清单共20个成员，保留安全项目相对路径；固定1980时间戳、0644成员权限、空注释/扩展字段和无压缩存储，同一冻结版本重复构建字节完全一致。
- [x] 构建和反向验真均重新检查当前发布冻结清单、18项来源大小/SHA-256、包内成员大小/SHA-256、精确成员集合、符号链接、路径穿越、重复/多余成员、绝对路径和本机实际密钥值。
- [x] 包内数据策略明确不含凭证、个人信息、绝对路径、私有提交网址、原始媒体或私有提交状态；证据原文允许保留厂商手册公开参考地址，只包含已经冻结的安全派生成片，并固定 `requires_human_gates_before_submission=true / official_upload_format_claimed=false`。
- [x] 首个本机构建为37,185,174字节、20成员，SHA-256为 `a66fe96761a7b63708b8377b225bbb60f41fbfe94bccd56c37cde0bc1a70878d`；10项专项覆盖确定性、权限、篡改、重复/多余成员、路径穿越、非确定元数据、QA漂移、符号链接和缺包安全退出。
- [x] 交付包、发布冻结和提交专项26项通过，本机全量现为 `286 passed`；Python编译、全部Shell语法、`--verify-only`、20成员私有路径边界和 `git diff --check` 通过。
- [x] 功能提交 `c1cbf57` 的本机正式干净预检为 `READY_WITH_HUMAN_GATES`：16/16项自动检查、286项测试、327个Git文件、工作树和18项冻结成果通过，实际密钥值泄漏0，人工确认0/5。
- [x] 从 `133e4cb` 到 `c1cbf57` 生成13文件、112,542字节安全增量包，本机与DGX SHA-256均为 `360295df0ca764e0f5e55de810aea4ba79162f393e1e198d5bfcb66d01e1a111`；包内没有 `.env`、运行时ZIP/QA、私有提交状态、原始输入、私有派生物、媒体或AppleDouble。
- [x] DGX首次全量发现新增测试错误依赖 `.git` 元数据，结果为285项通过、1项失败；功能构建、反向验真及本机/DGX归档哈希均已通过。修复提交 `d40f22a` 改为无Git部署时核对 `.gitignore` 规则，1文件、2,884字节修复包两端SHA-256均为 `eed328df1294bae072829ec769d0b0492594f9a66c55dbf14fb57b647171811d`。
- [x] 修复后DGX Python 3.12全量 `286 passed`；Python编译、全部Shell语法、18项发布冻结和技术包 `--verify-only` 通过。DGX本地重建ZIP同为37,185,174字节、20成员和SHA-256 `a66fe96761a7b63708b8377b225bbb60f41fbfe94bccd56c37cde0bc1a70878d`，权限为700/600。
- [x] DGX部署预检为预期 `DEVELOPMENT_CHECK`、退出码2，286项测试、18项冻结成果通过、人工确认0/5；部署副本无 `.env`、私有提交材料或AppleDouble，服务保持 `active/enabled` 和单一 `127.0.0.1:7860` 监听。

## 已完成：10阶段统一提交收尾协调器（本机与DGX）

- [x] 新增严格状态Schema、只读协调模块和 `scripts/check_submission_closeout.sh`，按技术交付包、五项人工/外部门禁、最终干净预检、上传、公开链接QA和提交回执固定为10阶段依赖链。
- [x] 状态词使用 `COMPLETED / READY / READY_FOR_CONFIRMATION / AWAITING_HUMAN / AWAITING_EXTERNAL / WAITING_ON_DEPENDENCIES / NEEDS_REVIEW`，不使用 `BLOCKED`；报告固定 `implementation_goal_blocked=false`，只把正式提交等待与实现目标状态分开表达。
- [x] 协调器不联网、不上传、不初始化私有模板、不自动确认门禁；只复核已有安全QA、哈希、权限、当前Git提交和固定依赖顺序。报告目录0700、文件0600且由Git忽略，不含姓名、单位、网址、证据路径、私有备注、凭证或原始媒体。
- [x] 最终预检必须绑定当前干净Git提交、无人工作业待办，并按冻结顺序精确包含16项全部通过的检查；同数量替代检查、提前填写链接、缺失门禁或非最终预检不能越过依赖链。
- [x] 当前实际状态为 `TECHNICAL_READY_HUMAN_GATES_PENDING`：1/10阶段完成、人工门禁0/5、下一阶段为完整观看80秒成片；命令和 `--verify-only` 均安全退出2，自动人工确认0、网络请求0。
- [x] 新增11项协调器专项和1项中国赛程时区回归；本机最终全量 `298 passed`。Python编译、全部Shell语法、18项发布冻结、20成员技术包反向验真和 `git diff --check` 通过。
- [x] 功能提交 `5d4f447` 的本机正式干净预检为 `READY_WITH_HUMAN_GATES`：16/16项自动检查、297项测试、331个Git文件、18项成果、`.env`忽略/600权限和实际密钥值泄漏0，人工确认仍为0/5。
- [x] 从 `ad530cb` 到 `5d4f447` 的16文件安全增量包为124,440字节，SHA-256 `c37c27c46f0c632bcd3d0d105cf3358684fd017bd423cbd300104b78f8cf393b`；不含 `.env`、outputs、原始输入、私有派生物或AppleDouble，两端哈希一致。
- [x] DGX首轮全量为296项通过、1项失败；唯一失败是新增测试错误依赖 `.git` 元数据，功能状态生成未失败。修复提交 `19946b6` 改为无Git部署时检查 `.gitignore`，1文件、3,723字节修复包两端SHA-256均为 `2f537f38625fa76eae0c0725916b839516b9f819d7d5cde38476d01adcc29718`。
- [x] 修复后DGX 297项通过，并发现任务看板默认日期随机器时区变化：本机为7月19日、UTC远端为7月18日。提交 `389430a` 将默认日期固定为 `Asia/Shanghai` 并加入跨时区回归；2文件、4,089字节修复包两端SHA-256均为 `6e3939af04fa8c63bbfbb226b66854dbeb9c459116a8df762a0e97e727d2460e`。
- [x] 最终DGX全量 `298 passed`；看板与本机一致为 `2026-07-19 / ON_TRACK`，收尾命令和 `--verify-only` 均返回2、1/10阶段完成、0/5门禁确认、网络请求0、自动确认0，私有目录/报告权限700/600。
- [x] DGX部署预检为预期 `DEVELOPMENT_CHECK`、退出码2，298项测试和18项成果通过；部署副本无 `.env`、私有门禁/链接/回执/最终预检文件或AppleDouble。用户服务保持PID 524247、`active/enabled`、单一 `127.0.0.1:7860` 和HTTP 200。

## 已完成：三种演示模式语义一致性门禁（本机与DGX）

- [x] 新增严格报告Schema、独立一致性模块和 `scripts/check_demo_mode_parity.sh`；现场模式每次从Gold重新计算，预处理模式核验当前产物或显式重新摄取，离线模式只读取仓库内无素材冻结包。
- [x] 三种模式都必须具备summary、最终SOP、修订前后冲突、修订审计和工作流6类结构化产物，并严格通过冲突、审计和工作流Schema；缺文件、篡改步骤或核心语义漂移均安全失败。
- [x] 当前实际刷新结果为 `PASSED`：3种模式均为13步、严重错误5→0、必要步骤覆盖率0.9→1.0、4项局部修订和 `COMPLETED / GOLD / FINAL`；比较忽略运行时间和来源措辞，只对确定性业务语义生成指纹。
- [x] `--refresh-preprocessed` 强制 `SKILLFORGE_OFFLINE_OCR=1`；OCR缓存缺失或固定SHA-256不匹配时拒绝下载并退出，保证演示准备过程网络请求0、外部模型调用0、自动人工确认0。
- [x] 私有报告目录/文件权限为0700/0600且受Git忽略，不保存原始素材、来源原文、绝对路径、凭证或人工门禁状态。新增7项专项，和任务看板联合专项共16项通过。
- [x] 功能提交 `4e3f3ae` 的本机正式干净预检为 `READY_WITH_HUMAN_GATES`：16/16项自动检查、305项测试、335个Git文件、工作树和密钥边界通过，人工确认仍为0/5。
- [x] 17文件、120,486字节安全增量包的本机/DGX SHA-256均为 `7f7e0b29b45d2fe726f12e3fe0ffd3d95575177a1384c207fe70c5562faccaa9`；不含 `.env`、outputs、原始输入、私有派生物或AppleDouble。
- [x] DGX使用现有安全预处理结果完成默认检查和 `--verify-only`：均为 `PASSED`、3种模式、5→0、4项修订、网络请求0、外部模型调用0和自动确认0，报告权限700/600；部署副本不含原始输入，因此未在远端执行刷新。
- [x] DGX Python 3.12全量 `305 passed`；编译、Shell/JSON、18项冻结成果、20成员技术包和看板通过。部署预检为预期 `DEVELOPMENT_CHECK`、退出码2；服务保持PID 524247、`active/enabled`、单一 `127.0.0.1:7860` 和HTTP 200，`.env`、私有门禁文件与AppleDouble均为0。

## 尚存风险

1. 自摄安全派生视频不再需要单独授权，但第三方教程、厂商手册原件和真实面单原件仍不能作为自有素材公开或发送外部模型。
2. 严格视觉复核确认当前稀疏关键帧不足以单独证明任何步骤的完整动作加完成状态；S04开机在选定窗口中不可见。Gold v1 继续由手册、连续视频引用和操作者口述联合支撑，视觉结果只作模型推断。
3. 当前 Gold v1 只有一名实际操作者审核；第二名熟悉标签打印机的复核者属于增强项。
4. 三来源候选分离、粗细粒度合并和依赖排序已实现，但当前真实案例使用审核后的来源投影和候选语义规范；开放式无语义规范动作发现未实现，也不在当前P0能力声明内。
5. 视觉模型标记11步可能含编码或二维码形状，显著比本地机器检查和人工抽检更保守；这些标记必须保留给最终录屏前复核，不能当作已证实泄漏。
6. PDF结构解析、中文OCR、检索和视频低码率预览已在本机与DGX完成；预览是私有派生物，提交包和无素材离线演示不会携带实际MP4。
7. 本机与DGX全量现均为305项；三种演示模式语义一致性门禁已在本机完成真实离线刷新，并在DGX使用安全预处理结果完成反向验真。10阶段提交收尾协调器、18项成果确定性技术交付包、私有提交回执审核、来源绑定、安全QA、缺回执边界和提交预检已完成DGX复验。官方规则私有六项审核、来源绑定、安全QA和提交门禁已完成DGX复验。80秒成片观看和180秒私有彩排检查器、任务看板、风险清单、私有团队名单校验器、发布冻结、公开链接核验器、最终录屏机器QA、官方规则公开核验快照、私有人工门禁确认器、八阶段资源统计、Linux峰值RSS换算、真实完整运行和渲染局部重跑已在DGX实测。五类Agent工具追踪、Gold闭环、真实产物阶段重跑与原子发布、可恢复工作流、ASR人工修正与Evidence摘要重绑、冲突裁决和安全人工门禁、四场景无来源内容门禁、高推理语义报告、选择性重建边界、离线包跨文件门禁、原生GPU视觉计算、连续动作候选窗口、PDF结构报告、三来源候选合成、三种SOP视图、13张安全预览、6段低码率素材预览、交互检查清单、五类证据测验、运行时基准、离线包、18项路演成果、用户服务和SSH隧道均已验证；整机重启未执行，但服务已启用且用户Linger已开启。
8. 当前 API 密钥曾通过聊天明文传递，项目公开或扩大协作前必须轮换。
9. 80秒培训视频已通过自动和AI辅助抽帧检查，私有完整观看审核器也已就绪，但尚未由参赛者从头到尾观看；当前不得用于“最终批准”表述。
10. 公开组队规则为2–5人，一人仅限一队；私有名单和职责校验器已就绪，但尚未创建实际名单，参赛资格仍待用户对照报名资料确认。
11. 官方评分权重、提交字段、外部API、开源、视频和现场运行要求仍未取得可核对的原文；私有六项审核、来源绑定和安全QA工具已就绪，但不能替代参赛者取得并阅读官方材料。
12. DGX目前承担真实本地GPU候选筛选和完整离线闭环，但Step 3.7语义规划/视觉复核使用外部Step Plan；若官方评分显著偏重本地模型，这是竞争力风险。
13. 人工门禁确认记录是Git忽略的本机私有状态，不会随代码同步；文件丢失、证据移动或运行单更新后必须由参赛者重新确认。DGX部署副本不能代替本地正式提交确认。
14. 最终有声字幕录屏尚未由参赛者实际录制；机器QA工具已经就绪，但不能替代真实录制、完整观看或人工隐私复核。
15. 三个公开入口的匿名核验器已经就绪，但当前没有作品页、代码仓库或最终录屏真实网址；未上传前必须保持待检查，不能把模板或网址哈希当作已公开。
16. 最终180秒真人彩排尚未由参赛者实际执行；结构化计时模板和机器QA已经就绪，但不能替代真人连续操作、故障恢复判断或人工确认。
17. 提交成功回执审核器已经就绪，但当前没有实际提交、成功截图、最终预检副本或提交后公开链接QA；工具通过不能在真实材料缺失时模拟。

非阻塞项：`Developer` 无 Docker Socket 权限。只有决定部署本地 GPU 模型或 NVIDIA VSS 时才处理。

## 下一批执行任务

1. 每日先运行 `bash scripts/check_project_board.sh`、`bash scripts/check_demo_mode_parity.sh --verify-only` 和 `bash scripts/check_submission_closeout.sh`；由参赛者依次关闭五项门禁：完整观看、180秒彩排、最终录屏、团队资格和官方规则。完整观看后运行 `bash scripts/check_training_video_review.sh --init`；彩排运行 `bash scripts/check_final_rehearsal.sh --init`；团队资格运行 `bash scripts/check_team_roster.sh --init`；取得规则原文后运行 `bash scripts/check_official_rules_review.sh --init` 并绑定来源、填写六项结论；最终录屏运行 `bash scripts/check_final_recording.sh`。每项实际通过后再使用 `bash scripts/manage_human_gates.sh confirm` 绑定证据，不手改运行单。
2. 成果冻结后运行 `bash scripts/build_public_release_bundle.sh` 及 `--verify-only` 保存技术交付兜底；录制前运行 `bash scripts/check_submission.sh`。五项门禁关闭后以 `--output outputs/submission/submission_preflight_final.json` 冻结最终预检；确认官方附件格式后再决定是否上传技术ZIP。上传后使用 `bash scripts/check_publication_links.sh --init` 运行匿名检查，人工重开作品页并保存成功截图，最后运行 `bash scripts/check_submission_receipt.sh --init`、绑定截图并完成私有回执QA。
3. 路演前运行 `bash scripts/dgx_demo_tunnel.sh --smoke`；公网入口不再作为P0依赖，不为此修改防火墙、云安全组或Docker权限。
4. 可选补拍或人工审阅S04连续原片以提升视觉可见性，并邀请第二名熟悉物流标签打印机的人员复核Gold v1；两项均不阻塞当前路演，不能仅凭候选时间点把S04升级为视觉支持。
