# SkillForge 最终录屏候选制作

## 1. 当前定位

该流程从公开安全的 Web 演示画面、已发布的80秒培训视频和冻结讲解词，自动生成一条接近三分钟的有声字幕候选成片。它用于减少手工录屏和剪辑风险，但只完成机器可验证部分；完整播放、字幕隐私、音画同步、节奏和官方视频规格仍须参赛者人工确认。

成片画面使用对外展示身份“星星之火 · SKILLFORGE”，不烧录内部的“候选成片”或“机器QA”状态；是否可以正式发布仍由私有构建报告、媒体QA和最终人工观看门禁共同决定。

故事板、构建报告和数据边界分别由以下严格契约约束：

- `config/final_recording_storyboard.json`
- `schemas/final_recording_storyboard.schema.json`
- `schemas/final_recording_build.schema.json`
- `schemas/final_recording_qa.schema.json`

## 2. 九个冻结场景

| 场景 | 时长 | 证明内容 | 画面来源 |
|---|---:|---|---|
| R01 | 15秒 | 项目定位与培训闭环 | Web总览安全截图 |
| R02 | 20秒 | 五类Agent与工具追踪 | Agent安全截图 |
| R03 | 20秒 | N31现场重算Gold闭环 | 完整运行后的Web截图 |
| R04 | 22秒 | 受控错误与Evidence边界 | Grounding安全截图 |
| R05 | 20秒 | 页码、视频和录音证据定位 | Evidence安全裁剪截图 |
| R06 | 20秒 | 局部修订前后对比 | Revision安全裁剪截图 |
| R07 | 20秒 | DGX Spark本地视觉计算 | DGX安全截图 |
| R08 | 26秒 | 清单、测验、海报与培训视频 | 已发布80秒培训视频片段 |
| R09 | 15秒 | 三种演示兜底与能力边界 | Web成果安全截图 |

总目标为178秒、1920×1080、30fps。该范围是内部路演目标，不冒充尚未取得的官方视频规格。

## 3. 安全截图规范

截图只从本地Web演示的公开展示区域采集，固定为八个PNG文件：

```text
01_overview.png
02_agents.png
03_dgx.png
04_grounding.png
05_revision.png
06_outputs.png
07_evidence.png
08_live_run.png
```

文件保存在Git忽略的 `outputs/submission/final_recording_assets/`，目录权限0700、文件权限0600。不得截入上传表单、原始素材入口、私有成员信息、绝对路径、凭证或Authorization。R05和R06在故事板中有固定裁剪区，用于只展示Evidence和修订对比的安全区域。

## 4. 自动构建

```bash
bash scripts/build_final_recording_candidate.sh
```

构建器逐场调用 `stepaudio-2.5-tts` 生成中文旁白，仅发送冻结旁白文本，不发送原始视频、PDF、录音或截图。相同模型、音色、指令和文本会命中本地私有缓存。随后使用原生Python、Pillow和FFmpeg完成字幕烧录、响度规范、九场景拼接及H.264/AAC编码，不依赖Docker。

候选成片和报告位于Git忽略的私有目录：

```text
outputs/submission/skillforge_final_recording.mp4
outputs/submission/final_recording_build.json
outputs/submission/final_recording_qa.json
```

构建报告只保存相对安全元数据和SHA-256。除既有媒体QA外，构建器会分别抽取九个场景中点，与最终成片对应时间点做差异哈希匹配，防止漏场景或顺序错乱。

## 5. 机器QA与人工门禁

可独立复验媒体机器指标：

```bash
bash scripts/check_final_recording.sh
```

机器检查覆盖：

- 175–180.5秒内部时长范围；
- 1920×1080、约30fps、H.264；
- AAC、48kHz、双声道；
- 响度、真峰值、静音和黑屏阈值；
- 构建报告中的九场景顺序中点匹配；
- 私有目录与文件权限。

机器通过状态只能是 `READY_FOR_HUMAN_REVIEW`。当前本机已初始化Git忽略的 `outputs/submission/final_recording_review.json`，不要重复执行 `--init`。参赛者完整观看时填写带时区的开始/完成时间、播放方式，把11项检查全部按实际结果设置为true，并将状态改为 `READY_FOR_CHECK`：

```bash
bash scripts/check_final_recording_review.sh
```

审核器要求记录的观看时间不少于成片时长减2秒，并同时绑定当前MP4、媒体机器QA、构建报告、故事板和内部策略。通过后生成 `final_recording_review_qa.json`，只保存哈希、观看耗时和布尔结果，不复制备注、实际时间点、媒体或路径，状态仍为 `READY_FOR_HUMAN_CONFIRMATION`。参赛者确认无隐私泄漏、字幕清晰、声音与画面同步、九场景可理解、事实边界准确、文件可从头播放到结尾后，才可绑定 `FINAL_RECORDING_REVIEW`；官方视频细则继续由独立规则门禁确认，程序不得推测或代替人工确认。
