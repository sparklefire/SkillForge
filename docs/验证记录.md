# 验证记录

## 2026-07-14：接入、DGX 与 P0 模拟闭环

### Step Plan

命令：

```bash
python3 scripts/verify_step_plan.py
```

结果：请求成功，模型为 `step-3.7-flash`，回复为 `STEP_PLAN_OK`。记录不包含密钥或 Authorization Header。

随后通过新客户端调用 `step-3.5-flash-2603`，让模型返回一个不含真实素材的模拟 Evidence 对象；结果一次通过 `evidence.schema.json` 校验，输出标记为 `STRUCTURED_STEP_PLAN_OK`。

### DGX Spark

命令：

```bash
bash scripts/check_dgx.sh
```

关键结果：

- `spark-1130` / `Developer` / `aarch64`。
- NVIDIA GB10，驱动 `580.126.09`。
- Docker `28.5.1`，服务为 `active`。
- Docker Socket 为 `root:docker`、`660`，当前账号不可访问。
- NVIDIA Container Toolkit 存在，container CLI 可识别 GPU。
- 用户级 FFmpeg 为 `7.0.2-static`。
- `~/skillforge` 及数据、输出和缓存目录存在。
- `nvcc` 不存在；当前预构建容器路线不要求安装。

### 自动测试

命令：

```bash
.venv/bin/python -m pytest
```

结果：`8 passed in 0.21s`。

覆盖范围：Schema、非法 JSON 修复、凭证脱敏、模型输出重试、状态机、受控错误检测和局部修订。

### P0 模拟闭环

命令：

```bash
.venv/bin/python -m skillforge.demo --output outputs/demo_run
```

结果：

| 指标 | 修订前 | 修订后 |
|---|---:|---:|
| 必要步骤覆盖率 | 88.89% | 100% |
| 必要步骤证据覆盖率 | 88.89% | 100% |
| 严重错误 | 5 | 0 |
| 冲突总数 | 5 | 0 |

工作流最终状态为 `COMPLETED`，自动变更 4 项。该结果只用于验证工程闭环，不能当作真实设备案例指标。

## 2026-07-14：原生 Python + FFmpeg 路线

### 本机原生摄取

生成了无版权的8秒测试视频、2页测试PDF和5秒测试录音，并执行：

```bash
.venv/bin/python scripts/generate_synthetic_assets.py
.venv/bin/python -m skillforge.ingest \
  --video outputs/synthetic_assets/synthetic_operation.mp4 \
  --pdf outputs/synthetic_assets/synthetic_manual.pdf \
  --audio outputs/synthetic_assets/synthetic_expert.wav \
  --output outputs/synthetic_ingest \
  --frame-interval 2 \
  --synthetic
```

结果：H.264/AAC 标准化成功，抽取4个关键帧、2页PDF文本与页面预览、规范化音轨，并生成6条通过 Evidence Schema 的候选证据。

### StepAudio ASR

使用本机系统语音合成一条无版权中文测试句，经 FFmpeg 转为16 kHz单声道WAV后调用 `stepaudio-2.5-asr`。ASR 返回22个SSE事件和21个带时间戳的增量片段，最终文本完整匹配测试句。

### N31 真实专家口述与 Gold v1

实际操作者录音文件时长约6分29秒，AAC、48 kHz、双声道；原始文件权限为600。FFmpeg检查得到平均音量约-25 dB、最大音量约-4 dB，规范化为16 kHz单声道后调用 `stepaudio-2.5-asr`。

结果：

- ASR返回1301个SSE事件、1300个逐字时间戳片段和1399字最终文本。
- 最终文本与逐字时间戳流相似度超过0.98；尺寸写法和第10–12题编号存在格式差异，以逐字时间戳流定位、两种原始结果均保留。
- 受控术语表校正N31、缝标、导纸夹、胶辊和72×130毫米等识别项；12个问题的必要术语检查全部通过。
- 12个问题分别生成 `E143`–`E154` 已验证专家口述证据，完整 Evidence Catalog 为154条。
- 13步Gold SOP全部为 `VERIFIED`：10步必需，S05、S06、S11条件执行。
- 删除无明确来源的“短按0.5秒”精确参数，补齐72×130毫米参数的口述证据。
- Gold评测初始发现5个高严重度冲突，4项局部修订后降为0；必要步骤和证据覆盖均由90%恢复到100%。
- 本机全量自动测试为 `32 passed`。
- 本机真实Web进程的 `/health`、`/api/n31` 和首页均返回HTTP 200；N31接口返回 `GOLD / FINAL`。

### Web Demo

原生 Uvicorn 服务在本机启动后：

- `GET /health` 返回200，`runtime=native-python`、`docker_required=false`。
- `POST /api/demo/run` 返回200，闭环状态为 `COMPLETED`。
- 首页返回200并展示问题、证据、修订前后和修订审计。
- Web 上传接口使用三类合成素材完成原生摄取并返回证据候选。
- ASR默认关闭，未确认外部处理授权时接口拒绝发送音频。

### Step 3.7 感知与 SOP 规划

使用合成视频关键帧执行真实 Step 3.7 多模态调用。首轮测试因输出预算不足未产生JSON，客户端按设计拒绝；把视觉输出预算提高并补全必填字段约束后，返回结果通过 Evidence Schema。程序随后覆盖并锁定证据ID、源文件、时间点和关键帧路径，确保模型不能修改引用位置。

使用模拟案例的13条规范证据执行 SOP 规划，实际返回10个必要步骤、13个证据引用；全部引用属于输入 Evidence Catalog，完整结果通过 SOP Schema。

### DGX 原生验证

项目运行副本同步到 `~/skillforge/app`，不包含 `.env`、Git历史或原始用户素材。通过本地准备的 `aarch64` wheelhouse 建立 Python 3.12 虚拟环境。

结果：

- Gold流水线加入前28项自动测试通过；同步本批Git跟踪代码和Gold JSON后，32项自动测试通过。
- DGX 无 `ffprobe` 时成功使用 FFmpeg 回退探测。
- 原生摄取输出4帧、2页和6条证据候选。
- 实际操作者审核的Gold闭环在DGX完成：严重问题5→0、必要步骤覆盖90%→100%、证据覆盖90%→100%，状态为 `GOLD / FINAL / COMPLETED`。
- Web健康检查、N31接口和首页均返回200；`n31_rehearsal_available=true`，N31接口返回4项局部修订。
- 同步范围仅为Git跟踪文件，没有上传原始录音、视频、PDF、私有面单或本地 `.env`，明确不需要 Docker。

## 2026-07-17：N31 视觉序列、多源对比与演示兜底

### Step 3.7 真实关键帧序列复核

使用 `scripts/run_n31_visual.sh`，为13个Gold步骤分别组合步骤直接引用关键帧及同源相邻帧，每步最多6帧。只发送已通过本地机器检查和人工抽检的安全派生JPG；不发送原始视频、手册、录音或真实面单。

结果：

- 13个步骤均返回通过 Visual Assessment Schema 的结果；模型为 `step-3.7-flash`。
- 12步为 `PARTIAL`，S04为 `NOT_VISIBLE`，0步为 `SUPPORTED`，0步为 `CONTRADICTED`。
- 整体至少部分可观察率为12/13；10个必要步骤中9个至少部分可观察。
- 严格标准要求关键动作和完成状态同时可见，因此不把5秒间隔稀疏帧误报为完整支持。
- 视觉模型标记11步可能出现编码或二维码形状；本地安全派生QA已通过，这些模型标记只进入保守复核队列。
- 首轮中途出现截断或非法JSON时客户端拒绝发布；增加4096 token预算、严格输出示例和逐步检查点后完整成功。

### 多源消融

使用同一个 `OPERATOR_REVIEWED_GOLD` 对手册、视频、专家口述和组合来源进行必要步骤覆盖对比：

| 来源条件 | 覆盖 |
|---|---:|
| 手册单源 | 8/10 |
| 专家口述单源 | 9/10 |
| 视频引用存在 | 10/10 |
| 视频严格视觉支持 | 0/10 |
| 视频至少部分可观察 | 9/10 |
| 至少两种来源 | 10/10 |

这组结果区分了“存在视频引用”和“当前关键帧足以严格证明动作”。Gold规则闭环仍稳定检测5个高严重度问题并通过4项局部修订降为0。

### Web 与三种演示模式

新增现场重算、完整预处理和离线无素材三种模式。离线演示包只含结构化Gold步骤、冲突、修订审计、指标、检查清单和测验。

本机验证：

- 全量自动测试：`35 passed`。
- `offline` 模式的 `/health` 和 `/api/n31` 返回HTTP 200，接口包含视觉和多源评测。
- `POST /api/n31/run` 返回HTTP 200，现场重算结果为 `GOLD / FINAL`、严重错误5→0、4项局部修订。
- 现场重算后再次读取 `/api/n31` 仍包含视觉和多源评测。

DGX Python 3.12 复验：

- 只同步本批Git跟踪文件和无素材离线包，未同步 `.env`、视频、关键帧、录音、PDF或真实面单。
- 全量自动测试：`35 passed`。
- 远端回环地址的 `/health`、`/api/n31` 和 `POST /api/n31/run` 均返回HTTP 200。
- 离线接口返回 `GOLD / FINAL`、视觉矛盾0步、至少两种来源覆盖100%；现场重算返回严重错误5→0和4项局部修订。
- 现场重算后视觉与多源结构化结果仍存在；烟测结束后远端验证服务已停止。

### Web 培训成果

本机离线模式进一步验证：

- `/api/n31` 返回13项检查清单、5题培训测验和 `COMPLETED` 工作流。
- 最终SOP、检查清单、测验和修订审计四个白名单下载均返回HTTP 200及附件文件名。
- 未列入白名单的 `private-video` 请求返回HTTP 404，下载接口不接受任意路径。
- Web专项与全量测试继续通过，共 `35 passed`。

同一批代码同步到DGX后再次通过35项测试；远端回环Web返回13项清单、5题测验和 `COMPLETED` 工作流，四项白名单下载均为200，`private-video` 为404。验证命令通过退出清理钩子停止Web进程。

### N31 A4培训海报

从 `cases/n31/gold/gold_sop.json` 确定性生成 `output/pdf/n31_a4_training_poster.pdf`：

- 单页标准A4，595.276 × 841.89 pt，130442字节，无加密和JavaScript。
- 包含设备、72mm × 130mm介质、缝标定位、13步操作、3个条件步骤、异常处理、完成标准和隐私提示。
- 相同输入重复生成的SHA-256一致，当前为 `d87dd874dc332d8057f0a1106b5b42085f6127f87ddeaa8f4b8a79eee08a8fba`。
- 使用 Poppler 以150 dpi渲染为PNG并人工检查，未发现裁切、重叠、黑块、乱码或不可读文字。
- PyMuPDF检查40个文字块均在页面边界内；文本抽取包含N31、S01和S13。
- Web海报下载返回HTTP 200、`application/pdf`，下载字节与生成文件完全一致。
- 全量自动测试新增海报确定性、页数、尺寸和文本检查后为 `36 passed`。

DGX验证时，旧项目虚拟环境因未安装ReportLab而在测试收集阶段停止。检查确认不是代码或PDF错误后，将 `reportlab 4.4.9`、`Pillow 12.2.0` 和 `charset-normalizer 3.4.9` 的Python 3.12 aarch64 wheel加入既有远端wheelhouse并离线安装。原锁定值 `Pillow 12.3.0` 没有对应aarch64 wheel，因此调整为可跨架构复现的12.2.0。

修复后：

- DGX全量测试为 `36 passed`。
- 远端重新生成海报为单页595.28 × 841.89 pt，可抽取1067字符，包含S01和S13。
- 远端Web海报接口返回HTTP 200、`application/pdf`和有效 `%PDF-` 文件头。
- 未使用Docker，未修改系统Python，未同步原始N31素材。

## 2026-07-17：N31 80秒培训视频

新增 `skillforge.training_video`，以可审计分镜重剪6段 `LOCAL_QA_PASSED` 自摄安全派生视频。每个步骤镜头同时绑定Gold步骤、Evidence ID、来源ID和源视频时间窗口；片头片尾不得伪装成Gold步骤，条件步骤必须与Gold保持一致，未知Evidence或不重叠的视频引用会直接拒绝生成。

### StepAudio旁白

- 模型：`stepaudio-2.5-tts`；音色：`zhixingjiejie`。
- 输入：371字合并旁白；未发送视频、手册、录音、面单或Evidence正文。
- TTS原始音频：85560毫秒、48kHz；为适配80秒成片执行1.0695倍语速调整。
- TTS缓存和临时Header均为`600`；结构化日志只记录模型、音色、字符数、字节数和哈希。

### 成片与证据

- 输出：`output/video/n31_training_video_v1.mp4`。
- 参数：80000毫秒、1920×1080、30fps、H.264、AAC、48kHz。
- CRF 22归档版本：35194451字节；SHA-256 `61ec74826befc89889a6025a9acdb217adbd32af589a9c1f4bb4be1958ff695f`。
- 覆盖：15镜头、13/13 Gold步骤、10/10必要步骤、30次Evidence引用。
- 独立证据包：15个镜头、25条去重Evidence，`contains_raw_media=false`、`contains_credentials=false`，并与成片SHA-256双向绑定。

### 质量与Web

- 自动QA：时长、尺寸、帧率、音频、步骤覆盖、Evidence边界和来源隐私门禁全部通过。
- 音频：综合响度-16.18 LUFS，真峰值-1.43 dBTP。
- 视觉：检查16帧联系表和5张1080p重点帧；字幕安全区、S11裁切、S12-S13遮挡通过。S04没有完整接电画面，因此字幕明确写“接电见手册P2”，不伪造视觉支持。
- Web：`/api/n31`返回生成清单；内嵌媒体端点支持Range读取；视频、生成清单和证据包使用固定白名单下载，展示前核对视频和证据包SHA-256。
- 当前状态：`READY_FOR_HUMAN_REVIEW`。AI辅助抽帧检查不替代参赛者最终完整观看。
- 本机视频与Web专项测试11项通过；全量测试待本批最终回归记录。

本机培训视频首轮最终回归为 `44 passed`。随后只将本批代码、Schema、文档、分镜和 `output/video/` 三个公开安全产物同步到DGX `~/skillforge/app`；未同步 `.env`、原始视频、手册、录音、真实面单、私有照片、关键帧或TTS缓存。

首次macOS归档携带19个 `._*` AppleDouble辅助文件，远端 `schemas/._*.schema.json` 被Schema目录扫描器当作JSON，导致22项测试失败。确认失败输入均以 `Mac OS X` AppleDouble头开头后，只删除本次同步产生的19个辅助文件；未修改任何业务数据。清理后：

- DGX Python 3.12首轮清理后全量测试：`44 passed`。
- 远端成片：35194451字节，SHA-256与本机及证据包绑定值一致。
- `/health`：`training_video_available=true`、`training_video_status=READY_FOR_HUMAN_REVIEW`。
- `/api/n31`：13/13 Gold步骤覆盖；视频清单校验通过。
- 视频Range请求：返回32字节并包含MP4 `ftyp`头。
- 视频证据包下载：25条去重Evidence，`contains_raw_media=false`。
- 烟测结束后远端Uvicorn进程自动停止。

后续从macOS制作同步归档时必须禁用扩展属性，或在测试前拒绝任何 `._*` 文件，避免AppleDouble辅助文件进入Schema目录。

同时在Schema注册器中增加 `._*` 显式忽略规则和专项测试。再次使用不生成AppleDouble文件的归档同步后，远端项目内 `._*` 数量为0，本机与DGX最终全量测试均为 `45 passed`；成片哈希和审核状态保持不变。

## 2026-07-17：DGX用户服务与安全隧道

- 用户级 `skillforge-demo.service` 通过 `systemd-analyze --user verify`，状态为 `enabled` 和 `active/running`，用户 `Linger=yes`。
- 服务固定使用仓库内离线Gold包并只监听 `127.0.0.1:7860`；环境明确设置跳过 `.env`，日志未发现密钥、Authorization Header或私有路径。
- 两次对主进程执行SIGKILL后，systemd均以新PID恢复；每次恢复后 `/health`、`/api/n31` 和Gold现场重算再次通过，结果为 `GOLD / FINAL / COMPLETED`、严重问题5→0和4项局部修订。
- 本机SSH隧道烟测将 `127.0.0.1:17860` 映射到DGX回环服务，退出后本机端口自动关闭；没有修改公网安全组、系统防火墙或Docker权限。
- 本机与DGX全量测试均为 `51 passed`。

## 2026-07-17：可复现运行时基准

新增运行时Schema、Python运行器、一键脚本、命令行入口和路演验收断言。基准固定执行2次预热、20次正式测量；临时输出留在项目 `outputs/` 下并自动删除，不读取凭证、不处理原始媒体、不调用外部模型。

本机 Darwin arm64 / Python 3.14：

- 直接Python Gold闭环：中位38.755毫秒，P95 39.317毫秒，最大39.707毫秒。
- Web现场重算：中位44.523毫秒，P95 49.086毫秒，最大50.279毫秒。
- 基准进程高水位RSS：100,057,088字节。

DGX Linux aarch64 / Python 3.12.3 / NVIDIA GB10：

- 直接Python Gold闭环：中位37.626毫秒，P95 37.758毫秒，最大37.796毫秒。
- Web现场重算：中位44.800毫秒，P95 56.040毫秒，最大59.932毫秒。
- 基准进程高水位RSS：86,491,136字节。

两类基准的20个样本均返回 `GOLD / FINAL / COMPLETED`、严重问题5→0、4项局部修订、输出234,237字节和外部模型调用0次。路演验收增加第10项必需成果并通过运行环境、样本数、业务断言、资源与数据边界检查；本机与DGX最终全量测试均为 `55 passed`。

以上性能数据只覆盖已审核结构化Gold的确定性质检、局部修订和成果输出；Web数字使用进程内HTTP，不包含网络传输。它们不代表原始视频、PDF、录音的完整预处理速度，也不代表GPU语义推理速度。

## 2026-07-17：提交预检与参赛材料

新增 `skillforge.submission`、提交预检Schema和 `scripts/check_submission.sh`。检查器复用路演验收，并增加项目身份、9份说明文档、Git工作树、跟踪敏感路径、`.env`忽略与600权限、实际本地密钥值泄漏、10项公开成果绝对路径和全量测试检查。输出只记录相对路径、计数和状态，不记录实际密钥值或原始媒体。

开发工作树验证：

- 10项路演成果、9份文档、项目身份和公开成果边界通过。
- `.env`存在、权限600、被Git忽略；检查1个本地密钥值，Git跟踪文件泄漏数为0。
- 全量测试为 `60 passed`。
- 因实现尚未提交，Git工作树检查按显式 `--allow-dirty` 标为 `SKIPPED`，总体为 `DEVELOPMENT_CHECK`，没有误报为正式就绪。
- `DEVELOPMENT_CHECK` 返回退出码2；只有五项门禁全部关闭后的 `READY_FOR_SUBMISSION` 才返回0。
- 五项门禁保持 `PENDING`：成片完整观看、180秒彩排、最终有声字幕录屏、团队资格和官方规则讲义。

DGX Python 3.12复验：

- 公开安全文件同步后项目内AppleDouble文件为0；未同步 `.env`、原始视频、手册、录音、真实面单或私有照片。
- 全量测试为 `60 passed`。
- 路演验收为 `READY_WITH_HUMAN_GATES`，10项成果和全部自动断言通过，五项门禁与本机一致。
- DGX部署目录没有Git元数据；使用 `--allow-missing-git --allow-dirty` 时测试通过，Git检查明确为 `SKIPPED`，总体为 `DEVELOPMENT_CHECK`。该结果只验证部署副本，不替代本地正式提交预检。

同时新增 `docs/架构与数据边界.md` 和 `docs/参赛提交材料.md`。后者包含20秒、约100字和一页版介绍、可复现指标、演示顺序、已知限制及待参赛者填写字段，没有编造团队、公开链接或官方规则。

## 2026-07-17：Gold对齐连续动作候选窗口

新增 `temporal_action_windows` Schema、确定性生成器、一键脚本、Web面板、白名单下载和路演自动断言。输入固定绑定Gold SOP、Step 3.7严格视觉复核和DGX原生CUDA候选报告的SHA-256；生成阶段不读取原始媒体、凭证或绝对路径，外部模型调用为0。

本机验证：

- 13个Gold步骤的51次已审核帧引用按同一来源时间线合并为19个开始/结束时间窗口，覆盖6段视频。
- 12个窗口命中附近的DGX场景候选时间点，来源内去重后共41个；范围固定为 `GOLD_ALIGNED_CANDIDATE_WINDOW_ONLY`。
- S04窗口为60,000至75,000毫秒并绑定62,000、63,000、67,000毫秒三个候选点，但视觉结论仍为 `NOT_VISIBLE`，没有把邻近时间点冒充动作支持。
- 路演验收更新为11项必需成果和8组核心主张，全部自动断言通过，状态为 `READY_WITH_HUMAN_GATES`。
- 暂存后开发态提交预检通过9份文档、11项成果、`.env`边界、1个本地密钥值和179个Git文件检查；全量测试为 `66 passed`，因工作树尚未提交而按预期返回 `DEVELOPMENT_CHECK` 和退出码2。

DGX Python 3.12复验：

- 使用不携带 `.env`、原始媒体或AppleDouble元数据的安全归档同步公开文件；项目内 `._*` 数量为0。
- 全量测试为 `66 passed`，路演验收11项成果和8组核心主张全部通过，状态为 `READY_WITH_HUMAN_GATES`。
- 部署副本提交预检按 `--allow-missing-git --allow-dirty` 运行，Git相关检查明确跳过，总体为 `DEVELOPMENT_CHECK`；该结论不替代本地干净工作树正式预检。

## 2026-07-17：PDF结构化解析、中文OCR与页码检索

新增PDF布局分块、纯文本回退、页级OCR质量门禁、私有检索索引、公开结构报告Schema、一键脚本、Web面板、路演断言和专项测试。OCR支持 `disabled / auto / required` 三种模式；中文与英文Tesseract数据固定到提交 `87416418657359cb625c412a48b6e1d6d41c29bd`，下载后分别核对SHA-256并以600权限保存到Git忽略缓存。

本机真实手册验证：

- 两份N31手册共58页、28,172字符、607个结构块：49个标题、87个列表、448个段落、5个表格和18个警告。
- 精细质量门禁识别9个文本层不足页面并执行 `chi_sim+eng` OCR，最终待OCR页为0；用户手册第16页渲染复核确认只有页眉、页脚和小型Logo，图像覆盖率门禁不会把装饰图误判为待OCR正文。
- 私有索引保持来源、PDF页码、块ID和结构类型；“进纸导轨”首条精确命中用户手册第14页，“缝标模式标签学习”和“按住走纸键”首条精确命中第20页。
- 完整执行 `run_n31_local.sh` 后仍为8个来源、142条Evidence、13步候选SOP、Gold严重问题5→0和4项局部修订；两份PDF资产分别记录9页与0页OCR，原有页码选择器和Gold闭环未漂移。
- 公开 `pdf_structure_v1.json` 通过Schema，包含两份输入哈希、统计、OCR数据哈希和检索页码，不含手册正文、页面图、私有索引、绝对路径或凭证。
- 本机全量测试为 `72 passed`；路演验收12项成果和9组核心主张全部通过，状态为 `READY_WITH_HUMAN_GATES`。
- 暂存后开发态提交预检检查186个Git文件，12项成果、9份文档、`.env` 600权限、1个本地密钥值和敏感路径边界全部通过；因尚未提交按预期返回 `DEVELOPMENT_CHECK` 与退出码2。

DGX Python 3.12安全副本复验：

- 同步包不含原始手册、OCR页面、含正文索引、`.env`或AppleDouble元数据；项目内 `._*` 数量为0。
- 全量测试为 `72 passed`；公开PDF报告58页、607块、9页OCR、0页待处理和三项检索断言通过。
- `/api/n31`与PDF结构报告白名单下载均返回200；路演验收12项成果通过，部署副本提交预检按预期为 `DEVELOPMENT_CHECK`。

## 2026-07-17：三来源候选步骤抽取、合并与排序

新增来源候选计划与合成报告Schema、`source_candidates.py`、一键脚本、N31真实输入、Web面板、路演断言和专项测试。该阶段读取完整Evidence Catalog但不读取Gold步骤文本，不调用外部模型；当前运行模式明确标为 `REVIEWED_SOURCE_PROJECTION`，不能冒充开放式无提示动作发现。

本机真实N31验证：

- 分别形成视频18条、PDF手册7条、专家口述8条候选，共33条；每条Evidence ID均验证存在且来源类型、来源引用完全一致。
- 8条 `TOO_COARSE` 候选拆成18个语义片段，8条 `TOO_FINE` 候选参与合并；最终得到13个规范步骤和12个实际合并组。
- 阶段分布为准备3、执行6、验证4、复位0；复位明确为成功路径不适用，异常路径由12个步骤级恢复策略覆盖。
- 依赖图无环；S07不再强制依赖条件步骤S06，而是依赖S04。S11和S12因消耗标签被标记为不可逆耗材动作。
- 12个步骤至少含两类来源，10个步骤含三类来源；S04只由快速指南PDF支持，`NOT_VISIBLE`视频证据E014未进入合并结果。
- `run_n31_local.sh` 完整通过：8个输入来源、142条初始Evidence、33条来源候选、13步合并结果、Gold严重问题5→0、4项局部修订，外部模型调用0次。
- 新增候选报告Web接口和下载均通过专项测试；路演验收新增 `source_candidates_grounded` 断言并把报告列为必需成果之一。
- 报告不含原始媒体、凭证、绝对路径或外部模型调用；公开文本与项目敏感模式扫描无命中。

## 2026-07-17：可解释置信度与审核路由

在三来源候选合成中加入证据权威性、审核状态、支持状态、多源佐证和负面观察五部分可解释评分。评分只用于候选路由，不覆盖Gold结论，也不会把来源数量直接等同于事实正确。

本机验证：

- 证据分类权重固定为 `SOURCE_FACT=1.0`、`EXPERT_ADVICE=0.9`、`MODEL_INFERENCE=0.65`；每个来源保留候选ID、分类、原始置信度、支持系数、审核系数和调整后分数。
- 两类来源佐证增加0.04，三类来源增加0.08；`NOT_VISIBLE / CONTRADICTED / MISSING_SOURCE / SOURCE_CONFLICT` 分别有独立惩罚上限，报告记录负面观察ID。
- 13步最终分布为 `HIGH=6`、`MEDIUM=6`、`LOW=1`；路由为 `AUTO_VERIFY=6`、`VERIFIER_QUEUE=6`、`HUMAN_REVIEW_REQUIRED=1`。
- S04仅有快速指南PDF候选，基础调整分为0.799；视频 `NO001 / NOT_VISIBLE` 置信度0.9产生0.108惩罚，最终0.691并进入人工复核。
- 专项测试同时覆盖候选跨来源借证据、负面观察跨来源借证据、依赖环、向后恢复目标和精确重复去重；Web与路演断言固定检查S04保守路由。

## 2026-07-17：三种SOP视图与交互检查清单

新增 `sop_views`、`mobile_checklist` 和 `checklist_session` 三份Schema。Gold、候选和模拟闭环统一从修订后SOP生成简洁版、详细版、带证据版和一步一屏检查清单；离线Gold包新增 `sop_views.json`，Web新增白名单下载、逐步关键帧、风险与证据展开、完成勾选和问题反馈。

- 三种SOP视图均为13步，每步保留动作、原因、完成标志、风险和来源；带证据版逐条展开证据分类、审核状态和PDF页码或音视频时间点。
- 手机检查清单为 `ONE_STEP_PER_SCREEN`，13步均带完成标志、风险、Evidence和可用关键帧；S04使用60.0至65.0秒定位帧但严格显示 `NOT_VISIBLE`，页面明确提示该画面不能证明连接电源和开机动作。
- 完成与重开操作写入 `completion_log`，四类问题反馈写入 `feedback_log`；会话使用不可预测ID，目录权限700、JSON文件权限600，位于Git忽略的 `outputs/` 下。
- Web现场验证：首页、健康检查、N31数据、三视图下载和E001本地关键帧均返回200；会话创建、完成S01、提交反馈和重新读取记录全部成功。
- 路演成果增至14项，新增 `training_package_traceable` 自动断言；专项测试13项、全量测试82项和JavaScript语法检查均通过。
- 从已审核80秒成片的每个步骤镜头中点提取13张640像素宽JPEG，合计约432KiB；联系表人工检查未见真实面单信息，S12/S13仍使用成片内的空白脱敏标签画面。
- 缩略图清单绑定成片SHA-256，并逐张记录步骤、镜头、时间点、大小和SHA-256；Web默认读取公开预览接口，路演新增 `checklist_previews_public` 哈希断言。加入该回归后本机全量测试为 `83 passed`。

DGX Python 3.12安全同步复验：

- 使用仅含Git跟踪文件的归档同步到 `~/skillforge/app`，同步前后SHA-256一致；归档不含 `.env`、原始媒体或私有派生目录。
- 全量测试为 `82 passed`，路演验收14项成果全部通过，状态为 `READY_WITH_HUMAN_GATES`。
- 重启已有用户级SkillForge服务后，N31接口返回简洁、详细和带证据三种视图；S04仍为 `NOT_VISIBLE`。
- 创建检查会话并完成S01后，进度为1/13；远端会话JSON权限为600。整个过程未处理Docker权限或修改系统防火墙、云安全组。
- 同步13张成片安全预览后，DGX全量测试更新为 `83 passed`；用户服务重启成功，S01预览接口返回HTTP 200和有效JPEG，清单13步均绑定公开预览路径，S04仍保持 `NOT_VISIBLE`。

## 2026-07-17：五类证据培训测验

新增 `training_quiz.schema.json` 和确定性测验生成/完整性校验。题目固定覆盖 `ORDERING / TOOL_SELECTION / RISK_RESPONSE / STATUS_RECOGNITION / ERROR_JUDGMENT`，类型分别为排序、工具多选、风险单选、状态单选和错误判断。

- N31 Gold测验生成5题：排序题覆盖S01至S04依赖链；工具题答案为S04已绑定电源；风险题使用S12隐私提示；状态题使用S13整体验收标志；错误判断拒绝忽略S13实体隐私遮挡要求。
- 正确答案和解析均保存非空Evidence集合；每个干扰项也绑定其原步骤Evidence，题目保存完整Evidence对象及PDF页码或音视频时间点。
- 完整性校验覆盖未知Evidence、答案/解析来源越界、选项来源越界、重复选项、答案不在选项、工具/风险/状态与SOP不一致、排序违反依赖和错误判断未拒绝风险。
- 新版测验通过Gold、候选和模拟回归；Web和白名单下载返回五类题及来源详情，JavaScript语法检查通过。
- 专项测试26项、全量测试90项通过；路演 `training_quiz_grounded` 断言通过，状态保持 `READY_WITH_HUMAN_GATES`。
- DGX Python 3.12同步后全量90项测试通过；重启用户服务后 `/api/n31` 与测验白名单下载均返回五类题，五题答案/解析Evidence非空，路演状态同为 `READY_WITH_HUMAN_GATES`。

## 2026-07-17：确定性无来源内容拒绝门禁

新增 `grounding_gate_report.schema.json` 和四个互相隔离的结构化篡改场景，覆盖跨步骤工具、参数数值篡改、额外安全提示和绝对安全承诺。门禁只读取Gold SOP与约束，不访问原始媒体、凭证或外部模型。

本机验证：

- `bash scripts/build_n31_grounding_gate.sh` 返回 `PASSED`；4个场景全部检出、全部恢复，复检残留冲突0，外部模型调用0。
- 跨步骤工具场景证明“出现在全局允许列表”不能替代当前步骤来源；冲突和修订审计均绑定S01的E001、E096和E144边界。
- 参数场景把S01标签宽度从72毫米改为999毫米，Verifier提出 `REPLACE`，Revision只恢复该参数为72毫米并引用E144。
- 无来源警告执行 `REMOVE`；完成标志中的“保证100%安全”执行 `REPLACE`，两者复检均无残留。
- Web `/api/n31` 返回完整门禁报告，白名单下载返回 `DETERMINISTIC_GROUNDING_GATE_V1`；首页包含四场景面板和修订前后展开。
- 无素材离线包增至11个JSON文件并包含 `grounding_gate.json`；包仍声明不含原始媒体和凭证。
- 全量测试为 `96 passed`；Web JavaScript通过 `node --check`，路演15项成果和 `grounding_gate_closed` 断言通过，状态为 `READY_WITH_HUMAN_GATES`。
- 开发模式提交预检确认9份文档、15项成果、公开成果边界、`.env`忽略与600权限、实际密钥值扫描和96项测试全部通过；因工作区尚未提交，整体状态按设计为 `DEVELOPMENT_CHECK`。

DGX Python 3.12安全同步复验：

- 首次两次传输完整仓库归档时，远端文件大小分别只到约27 MB和24 MB，均与本机约39 MB及SHA-256不符；部署命令在解压前失败，远端旧应用和用户服务未被覆盖。
- 当前提交没有删除文件，随后改用只含27个变更文件的327,680字节增量归档；本机和远端SHA-256均为 `d036a4d16bc084d1a662b39f038849a16684dedd881b120341b0551cc7b43827`，远端无AppleDouble文件。
- DGX全量测试为 `96 passed`；重建门禁报告返回 `PASSED / 4场景 / 4检出 / 4恢复 / 0残留 / 0模型调用`，报告SHA-256与本机一致。
- 路演验收返回 `READY_WITH_HUMAN_GATES`，15项成果全部通过，`grounding_gate_closed=true`，五项人工/外部门禁保持不变。
- 重启用户服务后健康检查返回 `native-python / docker_required=false / GOLD / FINAL / COMPLETED / 5→0 / 4项修订`；服务仍为 `active/enabled` 且只监听回环地址。
- `/api/n31` 和 `/api/n31/artifacts/grounding-gate` 均返回四场景门禁，五类测验仍为5题；下载结果为 `PASSED` 且残留冲突0。
- DGX提交预检按部署副本边界返回 `DEVELOPMENT_CHECK`；15项成果和96项测试通过，Git、跟踪文件和`.env`三项检查按设计为 `SKIPPED`。全程未处理Docker权限、云安全组或系统防火墙。

## 2026-07-17：高推理语义复核与选择性重建边界

新增 `semantic_review_response`、`semantic_review_report` 和 `selective_rebuild_report` 三份Schema，接入Web、无素材离线包、路演断言和白名单下载。语义复核与确定性重建边界分开：前者只提出模型推断，后者只按Revision Audit计算实际失效范围。

本机验证：

- `step-3.7-flash / high` 只接收13步结构化Gold、36条被引用Evidence陈述和约束；请求明确排除原始媒体、完整转写、手册页面、本地路径和凭证。
- 第一次执行的3次传输均因响应不满足严格Schema而被拒绝；加强输出形状后，下一次先出现长度截断，再在第二次传输通过。最终报告如实记录累计 `model_calls=5`。
- 最终13个assessment逐步覆盖且Evidence不越界，13步均为 `SUPPORTED`；来源曲解、来源冲突、顺序风险和异常遗漏发现项均为0，自动Gold修改为0。
- Revision Audit从Before SOP精确重放出After SOP；受影响步骤为S07–S13，其中3步内容变化、7步位置变化。
- 确定性逐题比较只失效Q02，Q01及Q03–Q05逐对象相同；视频只选择V07–V13，V00–V06和V14共8个镜头保持不变。
- A4因固定单页布局和步骤插入/顺序变化整页重建；SOP、三视图和手机检查清单按步骤级范围重建。选择性阶段外部模型调用0。
- 无素材离线包包含13个JSON，不含原始媒体或凭证；Web数据和两个新增白名单下载通过专项测试。
- 语义、选择性重建、Web和路演专项测试28项通过；加入离线包发布前Schema门禁回归后，全量测试为 `106 passed`；Web JavaScript通过 `node --check`。
- 路演验收17项必需成果全部通过，`semantic_review_grounded=true`、`selective_rebuild_bounded=true`，状态为 `READY_WITH_HUMAN_GATES`；五项人工/外部门禁保持不变。

DGX Python 3.12安全同步复验：

- 从提交 `05775c7` 相对 `2cec64d` 的34个文件生成112,129字节增量归档；本机与远端SHA-256均为 `ff9b8769738059a0214f53a23d21f30a67de68c69b1a208e28b6efca4a7ba721`。解压前确认不含 `.env`、原始输入、私有派生目录或AppleDouble文件。
- 解压后首次全量测试为 `105 passed`，证明代码与冻结公开包可用。随后调用离线包构建器时，远端被Git忽略的 `gold_rehearsal_v1` 缓存仍为旧检查清单格式；路演Schema校验立即失败，未将该状态用于服务验收。
- 使用当前结构化Gold、约束和受控错误在DGX本地重新生成Gold运行缓存，全程不访问原始媒体、不调用外部模型；随后重建选择性报告和13个JSON的离线包。
- 新增离线包构建前Schema门禁和回归测试：人为删除检查清单 `artifact_type` 时，构建器在发布前明确拒绝；三视图、测验、无来源门禁、语义复核与选择性重建也统一验证Schema和通过状态。
- 离线包门禁通过38,072字节第二增量归档同步；本机与远端SHA-256均为 `e37d652d27e263b5b92c5b6ee9dd4fad31eff8c2e8e01438a71fab9f47c88b05`，安全路径检查通过。
- 最终全量测试为 `106 passed`；17项必需成果、`semantic_review_grounded`、`selective_rebuild_bounded`、离线包发布前Schema门禁和三种演示兜底均通过，状态为 `READY_WITH_HUMAN_GATES`。
- 重启用户服务后健康检查返回 `native-python / docker_required=false`，N31接口为 `GOLD / FINAL / 5→0 / 4项修订`；语义复核和选择性重建白名单下载均为200，伪造私有下载为404。
- 用户服务保持 `active/enabled` 并只监听 `127.0.0.1:7860`。部署副本预检为预期的 `DEVELOPMENT_CHECK`：17项成果、公开边界和106项测试通过，Git与`.env`检查明确跳过；未处理Docker权限、系统防火墙或云安全组。

## 2026-07-17：操作者审核台与Evidence安全定位

新增 `sop_review_session`、`step_rebuild_response` 和 `evidence_locator_response` 三份Schema，以及本地审核会话、单步重建、Evidence安全定位和Web交互。该能力只操作审核会话，不改写冻结Gold。

本机验证：

- 新建审核会话包含13步并绑定当前SOP SHA-256；会话目录权限700、JSON权限600，数据策略为无原始媒体、无凭证、无绝对路径和外部模型调用0。
- 未锁定步骤直接确认被拒绝；S01锁定后可人工确认，确认事件均记录 `actor=OPERATOR / automatic=false`；必须先撤回确认才能解锁。全部13步确认后会话状态才变为 `COMPLETED`。
- S11从第11位移动到第12位通过，形成S12→S11→S13；把S09移动到S08之前因违反前置依赖被拒绝；任何导致已锁定步骤换位的重排也被拒绝。
- S12单步重建只返回3种SOP视图、1个检查卡和Q03，明确记录其余12步不变、外部模型调用0；锁定后的S12不能重建。
- E001视频Evidence返回安全步骤预览且移除私有keyframe路径；E144录音定位为40.7–68.7秒；PDF Evidence返回原页码。三类响应的原始来源URL均为空。
- Web接口完成Evidence查询、会话创建、S12单步重建、S11/S12安全重排、S09非法重排拒绝、S01锁定确认和会话回读；未知Evidence返回404。
- 路演 `DEMO_FALLBACKS` 新增 `evidence_locator_safe=true` 和 `operator_review_controls=true`；专项25项、全量 `112 passed`、JavaScript语法和17项成果验收通过，状态为 `READY_WITH_HUMAN_GATES`。

DGX Python 3.12安全同步复验：

- 从提交 `2794711` 相对 `ea05bd3` 的19个文件生成100,455字节纯增量归档；本机与远端SHA-256均为 `fd6d90c91329c43f9a7e59d504d9bbd2b492dbab0def9926b7bc3a8000ca2934`。首次归档包含macOS扩展属性生成的AppleDouble条目，安全门禁在解压前拒绝；重新禁用扩展属性后条目数为19，不含 `.env`、原始输入、私有派生目录或AppleDouble文件。
- DGX全量测试为 `112 passed`；路演17项成果全部通过，`evidence_locator_safe=true`、`operator_review_controls=true`，状态为 `READY_WITH_HUMAN_GATES`。
- 重启用户服务后健康检查为 `native-python / docker_required=false`；服务保持 `active/enabled` 并只监听 `127.0.0.1:7860`。
- 实际HTTP接口返回：E144为 `AUDIO_TIME / 40.7–68.7秒 / raw_source_url=null`；审核会话13步；S12单步重建保持其余12步不变且模型调用0；S11移动到S12后通过，S09移动到S08前返回400；S01组合操作的审计顺序为 `LOCKED, CONFIRMED`。
- 部署副本预检按 `--allow-missing-git --allow-dirty` 返回预期 `DEVELOPMENT_CHECK`：17项成果、公开边界和112项测试通过，Git、跟踪敏感路径和`.env`三项明确 `SKIPPED`，数据策略为无凭证、无原始媒体。未处理Docker权限、系统防火墙或云安全组。

## 2026-07-17：传输容错与可恢复工作流检查点

本机验证：

- Step Plan模拟429后第二次返回合法Evidence：调用2次、消息完全相同、退避0.5秒；模拟400只调用1次；连续超时严格停止在3次并记录0.5/1.0秒退避。
- 传输日志包含3条错误和2条重试事件，但不包含测试异常中的Authorization值；模拟429响应正文中的私有详情不进入异常。
- 完整工作流结束后从 `EXTRACTING` 重跑，尝试次数由1增至2，并明确失效 `PLANNING / CREATING / VERIFYING / REVISING / RENDERING`；运行中重跑和从未执行阶段重跑均被拒绝。
- 可重试 `EXTRACTING` 超时检查点保存为700目录/600 JSON，重新加载后从原阶段恢复；不可重试输入错误拒绝自动恢复，操作者显式修复后才能重跑。
- 真实Gold重新运行后工作流为 `COMPLETED`、9个事件、`VERIFYING=2`、`last_failure=null`；新格式同时进入无素材离线包并通过Schema。
- 重新生成的Revision Audit把C004精确记录为 `/steps/S10/parameters/0` 单项删除，并为C005保留当前步骤Evidence边界；选择性重建报告重新绑定新审计且所有验证为真。
- 离线包发布门禁新增Before/After/Revision Audit三项摘要核验；Revision Audit绑定排除每次重跑都会变化的起止时间，但保留动作、路径、前后值、原因、Evidence与SOP摘要。人为修改修订原因后，即使单文件Schema仍合法，也会因 `revision_audit_sha256` 不一致而拒绝发布。
- Web显示可恢复工作流状态、尝试次数、失败和重跑数；路演 `workflow_checkpoint_safe=true`。关键专项、全量 `122 passed`、JavaScript语法、17项成果和三种演示兜底均通过，状态为 `READY_WITH_HUMAN_GATES`。
- 当前验证只证明状态检查点、恢复规则和下游失效审计；生产产物级阶段选择性执行尚未实现，任务保持未完成。

DGX Python 3.12安全同步复验：

- 26文件主增量归档为113,145字节，SHA-256为 `e681d207deeb05597f1c49157221d0aaa1bc80f6828ac0a29e78d7eebdd19904`；12文件修复增量为62,783字节，SHA-256为 `defbd061efee0163efa18deead25283c0eaf8624fd711628f351c724b3c5cf07`。两次传输前后哈希一致，归档不含 `.env`、原始输入、私有派生目录或AppleDouble文件。
- 首次全量回归只有1项失败：测试复制了远端被Git忽略的旧Gold运行缓存，因此先被新工作流Schema拒绝，产品代码和已跟踪离线包没有失败。将测试夹具改为Git跟踪的门禁包后，DGX全量 `122 passed`。
- 在DGX重新运行真实Gold后，检查点为 `COMPLETED / 9事件 / VERIFYING=2`；新运行时间戳下的Revision Audit语义摘要仍与选择性重建报告相同，证明绑定对内容敏感、对确定性重跑时间稳定。
- 路演17项成果、`workflow_checkpoint_safe=true` 和三种演示兜底通过，状态为 `READY_WITH_HUMAN_GATES`。
- 重启用户服务后新PID为467358，首页工作流面板返回200；重启前和现场重跑后的工作流均为 `COMPLETED`。现场重跑仍返回严重错误5→0和4项局部修订，运行审计与选择性报告摘要一致。
- 服务保持 `active/enabled` 并只监听 `127.0.0.1:7860`。部署副本预检按显式无Git边界返回 `DEVELOPMENT_CHECK`：17项成果、122项测试和公开边界通过，数据策略为无凭证、无原始媒体；未处理Docker权限、系统防火墙或云安全组。

## 2026-07-17：真实产物阶段重跑与原子发布

新增 `gold_stage_run` Schema、八阶段执行器、命令行入口、Web控制和路演门禁。阶段执行器只读取结构化Gold、约束、受控错误和已审核视觉报告，不读取原始视频、PDF、录音或凭证，外部模型调用为0。

本机验证：

- 完整运行实际重建八阶段，结果为 `GOLD / FINAL / COMPLETED`、严重问题5→0、4项局部修订。
- 从 `RENDERING` 重跑，前七阶段全部标记 `REUSED` 且每个产物SHA-256与来源运行一致，只有渲染阶段标记 `REBUILT`；从 `VERIFYING_INITIAL` 重跑时前四阶段复用，后五阶段实际重建。
- 当前发布指针仅包含 `runs/<uuid>` 相对路径；加载时核对Schema、阶段连续性、产物集合、文件大小、SHA-256、工作流检查点和发布摘要。运行目录为700，JSON与日志为600。
- 故障注入让渲染阶段抛出超时：失败运行进入 `failed/<uuid>`，检查点为 `FAILED / retryable=true`，`current.json`仍指向故障前版本；随后从渲染阶段重跑成功。
- Web完整运行和 `/api/n31/stages/RENDERING/rerun` 均返回200；活动结果包含阶段运行清单。路演新增 `artifact_stage_rerun=true`，验证复用七阶段、重建一阶段及上游哈希不变。
- 全量回归 `127 passed`；17项成果、三种演示模式和全部路演自动断言通过，状态为 `READY_WITH_HUMAN_GATES`。DGX复验在下一步执行。

DGX Python 3.12.3安全同步复验：

- 最终15文件增量归档为104,174字节，SHA-256为 `e3c2da665278f7fe04b48be29aa1683e50c3baf67edfbc14749b46b5bd26eeff`，传输前后一致；归档不含 `.env`、原始输入、私有派生目录或敏感素材。首次macOS归档附带的AppleDouble文件在测试前清理，最终部署内 `._*` 为0。
- DGX全量 `127 passed`。命令行完整运行重建8阶段；随后渲染重跑复用7阶段、重建1阶段，来源运行UUID绑定正确，Gold仍为5→0，路演为 `READY_WITH_HUMAN_GATES / artifact_stage_rerun=true`。
- 用户服务重启PID由467358变为474352，状态为 `active/enabled`，仅有一个 `127.0.0.1:7860` 监听。回环Web完整运行重建8阶段，渲染重跑复用7阶段、重建1阶段，当前清单为 `RENDERING / COMPLETED / published=true`。
- 部署副本预检按无Git边界返回预期 `DEVELOPMENT_CHECK`：17项成果、127项测试和公开边界通过，`contains_credentials=false / contains_raw_media=false`；部署内 `.env`、原始N31输入和AppleDouble均为0。未处理Docker权限、系统防火墙或云安全组。

## 2026-07-17：冲突裁决与安全人工门禁

新增冲突裁决Schema、700/600权限私有会话、来源摘要绑定、自动/人工路由、最终发布判定、Web面板和路演断言。裁决只处理结构化冲突、Revision Audit、建议SOP和复检报告，不读取原始媒体，不调用外部模型。

本机验证：

- 当前Gold五个冲突自动裁决为 `AUTO_FINALIZED`，四个绑定直接Revision Audit路径，一个标记为关联变更解决；最终 `publishable=true / adopted=5 / pending=0 / rejected=0`。
- 向Gold S01标题注入“保证100%安全”，Verifier原始结果仍为 `automatic=true`；裁决器强制覆盖为 `HUMAN / DEFER_TO_HUMAN`，未人工处理时不可发布。
- 人工拒绝安全修订后为 `NEEDS_REVIEW / KEPT_ORIGINAL / publishable=false`；同一冲突重新打开并批准后为 `FINALIZED / ADOPTED / publishable=true`，事件链保留两次决定。
- 修改已绑定冲突消息后继续裁决会被拒绝；自动路由冲突不能人工伪装覆盖。操作者说明中的Bearer模式落盘前被脱敏。
- Web Gold页面、会话创建、读取、自动裁决汇总和非法覆盖拒绝通过；路演新增 `conflict_decision_auditable=true / safety_conflict_human_gate=true`。
- 全量 `131 passed`，Web JavaScript语法、17项成果和三种演示兜底通过，状态为 `READY_WITH_HUMAN_GATES`。

DGX Python 3.12.3安全同步复验：

- 仅同步13个Git跟踪文件，增量归档105,312字节、SHA-256为 `9b1a800150c1aca87edbfe00c1ef186ddc986a53a799eaba568d8cab4d1a0e94`，传输前后一致；归档不含 `.env`、原始输入、私有输出、AppleDouble或资源分叉文件。
- 全量 `131 passed`；用户服务重启PID由474352变为479614，保持 `active/enabled` 且只有一个 `127.0.0.1:7860` 监听。
- 回环Web首页包含冲突裁决面板；Gold会话返回 `AUTO_FINALIZED / publishable=true / adopted=5`，对自动路由冲突提交人工覆盖返回HTTP 400。
- 路演状态为 `READY_WITH_HUMAN_GATES`，`conflict_decision_auditable=true / safety_conflict_human_gate=true`。
- 部署副本预检为预期 `DEVELOPMENT_CHECK`：131项测试、17项成果和公开边界通过；Git、跟踪敏感路径和 `.env` 扫描因部署副本无Git元数据明确跳过。数据策略为无绝对路径、无凭证、无原始媒体，部署目录 `.env`、原始输入和AppleDouble均为0。

## 2026-07-17：ASR人工快速修正与Evidence摘要重绑

新增严格Schema、700/600权限私有原子会话、三项来源摘要绑定、逐问答修正历史、Evidence摘要链、Web面板/接口和路演断言。会话保留每段原始ASR片段，因此只允许本地私有存储，不进入Git、不加入公开成果；不包含原始音频，不调用外部模型。

本机验证：

- 当前专家转写创建12段会话，E143–E154一一映射，状态为 `OPEN`；原始ASR、基线校正文本、时间点和Gold Evidence声明均完成摘要绑定。
- 修正Q02后状态为 `CORRECTED`，当前修正段1、历史事件1；原始ASR不变，前后文本、实际操作者、原因、时间和新旧Evidence摘要全部可审计。
- 对Q05执行修正再恢复基线后，当前修正段恢复为0但历史事件保留2次，Evidence当前摘要恢复为基线摘要且摘要链连续。
- 删除已审核必要术语、使用变化后的转写源、提交同值/空值或错误字段均被拒绝；操作者输入中的Bearer凭证模式在落盘前脱敏。
- Web Gold页面、创建/读取/逐段修正、非Gold拒绝和必要术语拒绝通过；路演新增 `asr_correction_auditable=true`。
- 全量 `135 passed`，Web JavaScript语法、17项成果和三种演示兜底通过，状态为 `READY_WITH_HUMAN_GATES`。

DGX Python 3.12.3安全同步复验：

- 仅同步当前提交的14个跟踪文件，Git归档109,234字节、SHA-256为 `678ee655cfaff8b02ca52b06272c1d3f9145bb54b313ad5760e22cfc40326d3e`，传输前后一致；不含 `.env`、原始输入、私有输出、会话数据或AppleDouble文件。
- 全量 `135 passed`；用户服务重启后PID为486316，保持 `active/enabled` 且只有一个 `127.0.0.1:7860` 监听。
- 回环Web首页包含ASR快速修正面板；创建会话返回12段，Q02修正后为 `CORRECTED / corrected_answers=1 / correction_events=1`，原始ASR不变且Evidence当前摘要不同于基线摘要；重新读取结果一致，删除必要术语返回HTTP 400。
- 路演状态为 `READY_WITH_HUMAN_GATES`，17项成果通过且 `asr_correction_auditable=true`；现存ASR会话目录均为700、JSON均为600。
- 部署副本预检为预期 `DEVELOPMENT_CHECK`：135项测试、17项成果和公开边界通过；Git、跟踪敏感路径和 `.env` 扫描因部署副本无Git元数据明确跳过。数据策略为无绝对路径、无凭证、无原始媒体，部署目录 `.env`、原始输入和AppleDouble均为0。

## 2026-07-18：N31交付配置与低码率视频预览

新增 `CASE_OUTPUT_PROFILE` 和 `VIDEO_PREVIEW_MANIFEST` 两份严格Schema、N31交付配置、原生Python/FFmpeg生成器、一键脚本、训练视频渲染配置门禁、Web配置/预览面板、哈希校验Range播放、路演断言和专项测试。所有预览均为本机私有派生物，不调用外部模型，不处理第三方教程。

本机验证：

- 交付配置固定新操作员/初学者/设备旁任务支持、`zh-CN`简体中文普通话、源视频180–480秒、培训视频60–90秒且目标80秒，并要求SOP、清单、测验、海报、培训视频和修订审计六类输出。
- 6段 `LOCAL_QA_PASSED` 视频全部转为852×480、15 fps、H.264/AAC、faststart预览；总时长420.430秒、总大小24,620,361字节、最大平均总码率471.701 kbps，源/预览时长最大偏差33毫秒，逐项QA全部通过。
- 每项清单绑定来源角色、源/预览SHA-256、时长、技术参数和同一时间轴；报告无媒体路径、绝对路径、凭证或原始内容，实际MP4目录700、文件600且被Git忽略。
- Web只对大小和SHA-256同时匹配的预览提供Range播放；临时夹具实测206响应，哈希篡改后拒绝。无私有文件的部署仍可显示结构化配置和映射。
- 训练视频入口会直接拒绝case、语言、目标时长或MP4必需状态不匹配的配置；不是只由路演外围断言检查。
- 路演状态为 `READY_WITH_HUMAN_GATES`，`output_profile_complete=true / low_bitrate_previews_mapped=true / video_preview_configured=true`；5项人工门禁保持不变。
- 相关专项31项、全量 `141 passed`；Python编译、Web JavaScript语法和 `git diff --check` 通过。DGX复验在本批安全提交后执行。

DGX Python 3.12.3安全同步复验：

- 从提交 `997755d` 相对 `344bb75` 的21个文件生成130,761字节纯增量归档，SHA-256为 `98fc651c54fb67485b35983e44ad93908750944b254d0580f97cd76e68a29a27`，传输前后一致；归档不含 `.env`、原始输入、私有预览或AppleDouble文件。
- 远端从 `~/skillforge/data/n31_safe/video` 的6段安全视频本地生成预览，总时长420.420秒、总大小24,637,616字节、最大平均总码率471.846 kbps；全部QA通过，目录700、文件600，外部模型调用0。
- 全量 `141 passed`；路演状态为 `READY_WITH_HUMAN_GATES`，`output_profile_complete / low_bitrate_previews_mapped / video_preview_configured` 均为true，5项人工门禁保持不变。
- 用户服务重启PID由486316变为498533，保持 `active/enabled` 且只有一个 `127.0.0.1:7860` 监听。回环N31接口返回新操作员、`zh-CN`、80秒和6/6可用预览；主视频预览Range请求返回HTTP 206、32字节且包含MP4 `ftyp`。
- 部署副本预检为预期 `DEVELOPMENT_CHECK`：141项测试、17项成果和公开边界通过；Git、跟踪敏感路径和 `.env` 检查因无Git部署边界明确跳过，数据策略无绝对路径、凭证或原始媒体。部署目录 `.env`、原始输入和AppleDouble均为0。

## 2026-07-18：五类Agent与可审计工具执行追踪

新增 `AGENT_TOOL_EXECUTION_TRACE` 严格Schema、生成器、命令行和一键脚本。追踪不重新执行模型，而是从当前真实成果重建Agent职责、工具权限、调用、交接和产物摘要，使“Agent闭环”成为可校验成果而非仅有架构图。

本机验证：

- Agent顺序固定为Perception、SOP、Creator、Verifier、Revision；每类记录职责、禁止动作、授权工具和实际调用，Agent数量严格限制为5。
- 13个工具覆盖PDF页、关键帧、视频/录音区间、Evidence检索、SOP规划、保存、渲染、规则/语义质检、局部修订、选择性重建和人工确认；14次调用全部有输入输出成果ID和执行依据。
- 5次交接包含 `Creator → Verifier → Revision → Verifier`，明确展示发现问题、传递冲突、局部修订和复检，不允许缺少回路仍标记完成。
- 19个真实成果逐项绑定类型、分级、大小和SHA-256；生成器和Web读取均重新核验当前文件。测试修改摘要时被拒绝，Perception越权调用Revision工具时也被拒绝。
- 公开报告无成果路径、绝对路径、凭证或原始媒体；追踪生成外部模型调用0。Web `/api/n31/agents`、Gold负载、面板和白名单下载使用同一份校验结果。
- 路演必需成果从17增至18，`five_agent_tool_trace=true / agent_tool_interface_auditable=true`；相关专项25项、全量 `145 passed`、Python编译、Web JavaScript和 `git diff --check` 通过，状态为 `READY_WITH_HUMAN_GATES`。DGX复验在本批安全提交后执行。

DGX Python 3.12.3安全同步复验：

- 从提交 `45f4535` 相对 `074e455` 的19个文件生成130,756字节纯增量归档，SHA-256为 `d7bb153689d179f112f947aee5ab4c9f046288f6b31ded8a7202ae82301a2fb4`，传输前后一致；归档不含 `.env`、原始输入、媒体、私有输出或AppleDouble文件。
- 在DGX重新生成追踪，结果为5 Agent、13工具、14次调用、19个哈希成果、5次交接、14次证据绑定和1次人工确认；Agent顺序正确，`Revision → Verifier` 复检环存在，生成外部模型调用0。
- 全量 `145 passed`；18项成果、`five_agent_tool_trace=true / agent_tool_interface_auditable=true`，状态为 `READY_WITH_HUMAN_GATES`，5项人工门禁保持不变。
- 用户服务重启PID由498533变为505911，保持 `active/enabled` 且只有一个 `127.0.0.1:7860` 监听。Gold负载、独立 `/api/n31/agents` 和白名单下载三份JSON内容一致，下载HTTP 200。
- 部署副本预检为预期 `DEVELOPMENT_CHECK`：145项测试、18项成果和公开边界通过；Git、跟踪敏感路径和 `.env` 检查因无Git部署边界明确跳过，数据策略无绝对路径、凭证或原始媒体。部署目录 `.env`、原始输入和AppleDouble均为0。

## 2026-07-18：八阶段耗时与资源统计

为真实Gold阶段运行清单增加向后兼容的资源指标。旧清单没有 `metrics` 时仍可读取；所有新运行、上游复用、下游重建和失败注入阶段都由执行器直接写入指标，不能由调用方伪造汇总。

本机验证：

- 每阶段记录单调时钟 `elapsed_ms`、CPU用户/系统时间增量、进程累计峰值RSS、输出字节和外部模型调用数；资源范围固定为 `PROCESS_CUMULATIVE_PEAK_AND_STAGE_DELTAS`。
- macOS `ru_maxrss`按字节、Linux按KiB换算；峰值RSS必须大于0。`output_bytes`逐阶段等于输出记录大小之和，外部模型调用固定为0。
- 完整运行后从 `RENDERING` 重跑的当前清单包含8阶段：前7阶段复用、渲染阶段重建；合计31.159毫秒，进程峰值288,194,560字节，输出708,851字节。该数值是本机路演验收进程样本，不替代DGX基准。
- 渲染超时故障注入的失败阶段也保留非负耗时、正峰值内存和0外部模型调用；稳定发布仍不被替换。
- Web显示阶段总耗时和峰值RSS；路演新增 `stage_resource_metrics_recorded=true` 并验证全部8阶段。相关专项27项、全量 `145 passed`、Python编译、前端JavaScript和 `git diff --check` 通过。DGX复验在本批安全提交后执行。

DGX Python 3.12.3安全同步复验：

- 功能提交为 `1a87b64 feat: add stage resource metrics`。增量包 `skillforge-1a87b64-clean.tar.gz` 两端SHA-256均为 `3d4ce1a68cc11acde89e9bfffdff2f4c299f1bf8b4bdc9743b465bb1f8ee0687`；包内没有 `.env`、输入、私有派生物、媒体或AppleDouble。部署目录没有 `.env`、原始输入或AppleDouble，仍按既定边界保留6段受控低码率预览供回环Web演示。
- DGX完整运行UUID `83cbbf78-4e8b-4561-ae0e-08b65a261ff0` 从 `INGESTING` 开始，8阶段全部重建：合计148.013毫秒，CPU用户/系统时间0.075667/0.002587秒，进程峰值RSS 86,425,600字节，输出708,851字节，外部模型调用0。
- 渲染重跑UUID `3811a5e0-75c8-4440-84aa-d8dc3a3e62d7` 正确绑定前述来源运行，复用7阶段、仅重建 `RENDERING`：合计41.221毫秒，CPU用户/系统时间0.027172/0.000042秒，进程峰值RSS 86,425,600字节，输出708,851字节；8阶段逐项指标均通过。
- 全量 `145 passed`；18项成果和全部路演检查通过，`stage_resource_metrics_recorded=true`，状态为 `READY_WITH_HUMAN_GATES`。部署副本预检为预期 `DEVELOPMENT_CHECK`，自动测试仍为145项。
- 回环Web负载包含8阶段资源统计并显示“阶段耗时”“峰值RSS”“逐阶段记录耗时”三处说明。用户服务重启PID由505911变为512891，保持 `active/enabled`，且只有一个 `127.0.0.1:7860` 监听。

## 2026-07-18：私有人工门禁确认与提交接入

新增人工门禁确认器，但不替参赛者关闭门禁。默认运行 `status` 返回5项 `PENDING`、有效确认0/5；没有创建实际确认记录。

本机验证：

- 严格Schema限制5项确认、门禁ID、确认人、时间、说明、文件/HTTPS证据和审计历史；目录权限700、记录权限600并采用原子替换。
- 本地证据文件绑定大小与SHA-256。测试覆盖文件内容变化、删除、运行单哈希变化、门禁文案变化、未知门禁、重复确认、权限放宽、撤销和显式过期重置；任一异常都不能计入有效门禁。
- HTTPS证据只允许无账号、密码、查询参数和片段的地址，避免把访问凭证或签名参数写入记录。确认状态和提交报告不输出确认人、说明或本地证据定位。
- 提交预检新增私有确认检查。无记录时既有状态保持 `READY_WITH_HUMAN_GATES`；测试中为5项门禁提供显式、有效、证据绑定的临时确认后，路演可进入 `READY_FOR_SUBMISSION`，同时开发模式仍因跳过全量测试或Git干净检查保持 `DEVELOPMENT_CHECK`，证明人工确认不能覆盖自动失败。
- 相关专项35项、全量 `159 passed`、Python编译、Shell语法和 `git diff --check` 通过。提交报告目录权限已收紧为700，报告文件权限600。

DGX Python 3.12.3安全同步复验：

- 功能提交为 `6c212ef feat: add evidence-bound human gate confirmations`。16文件、105,093字节增量包两端SHA-256均为 `4f81e5c480a09a36d3f62f3d5e620355e9f42ab922617977701ecde57dc3bcb8`；包内没有 `.env`、人工确认、证据、输入、私有派生物、媒体或AppleDouble。
- 全量 `159 passed`。远端不存在人工确认记录，`manage_human_gates.sh status` 返回 `ABSENT / valid=true / 0 passed / 5 pending`，证明安全同步没有替参赛者关闭门禁。
- 部署预检为预期 `DEVELOPMENT_CHECK`，退出码2；`HUMAN_GATE_CONFIRMATIONS`、18项成果和159项测试均通过，五项人工门禁保持 `PENDING`。报告目录权限700、文件权限600，报告未输出确认人、说明或证据定位。
- 部署目录 `.env`、原始输入和AppleDouble均为0。用户服务重启PID由512891变为524247，保持 `active/enabled` 且只有一个 `127.0.0.1:7860` 监听；回环Web保持 `GOLD / FINAL / COMPLETED`，当前8阶段均含资源统计。

## 2026-07-18：官方规则公开核验快照

以匿名、只读方式复核NVIDIA赛事公开说明和训练营交互页。训练营页列出“黑客松赛事规则说明”，但该课程行没有公开讲义控件；页面的3个讲义下载控件逐项对应后续3门技术课程。没有登录、报名、下载或提交用户数据，也没有观察到单独公开规则正文或附件链接。

本机实现与验证：

- 新增严格 `official_rules_status` Schema和受控JSON快照：公开确认8项；评分权重、提交字段、视频要求、外部API、开源和现场运行要求6项继续标记为 `OFFICIAL_DETAIL_REQUIRED`。
- Schema拒绝“仍有未知项却标记已取得官方细则”的假关闭；提交预检进一步固定当前公开结论、两个来源ID和访问审计结果，漂移时安全失败并要求重新核验。
- `OFFICIAL_RULES_STATUS` 自动检查通过时只说明快照诚实完整，报告明确写出“规则人工门禁保持待确认”；实际人工确认仍为0/5。
- 提交专项9项、全量 `163 passed`；Python编译、Shell语法、内嵌Web JavaScript语法和 `git diff --check` 通过。开发模式预检为预期 `DEVELOPMENT_CHECK`，`OFFICIAL_RULES_STATUS` 显示公开确认8项、待官方细则6项，人工确认仍为0/5。

本机干净提交与DGX复验：

- 功能提交为 `11acace feat: audit official rules status`。本机正式预检为 `READY_WITH_HUMAN_GATES`；163项自动测试、271个Git文件、18项公开成果、`.env`忽略/600权限和本地密钥实际值泄漏扫描全部通过，五项人工门禁保持 `PENDING`。
- 从 `505bcad` 到 `11acace` 生成10文件、77,736字节安全增量包；本机与DGX SHA-256均为 `0b0adc277d6a595c3353961a31a6851c729a90cebf31955b60055ebe0bdedfc2`。包内无 `.env`、输入、派生物、媒体、人工确认或AppleDouble；DGX部署目录 `.env` 和AppleDouble均为0。
- DGX Python 3.12全量 `163 passed`，Python编译和Shell语法通过。部署预检为预期 `DEVELOPMENT_CHECK`、退出码2：规则状态8项公开确认/6项待细则、人工确认0/5，报告数据策略不含绝对路径、凭证或原始媒体。
- DGX用户服务无需重启，继续保持PID 524247、`active/enabled`，且只有一个 `127.0.0.1:7860` 监听；本批没有修改Web运行代码、Docker、系统防火墙或云安全组。
