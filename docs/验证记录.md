# 验证记录

## 2026-07-14：接入、DGX 与 P0 模拟闭环

### Step Plan

命令：

```bash
python3 scripts/verify_step_plan.py
```

结果：请求成功，模型为 `step-3.7-flash`，回复为 `STEP_PLAN_OK`。记录不包含密钥或 Authorization Header。

随后通过新客户端调用 `step-3.5-flash-2603`，让模型返回一个不含真实素材的模拟 Evidence 对象；结果一次通过 `evidence.schema.json` 校验，输出标记为 `STRUCTURED_STEP_PLAN_OK`。

### DGX Spark

命令：

```bash
bash scripts/check_dgx.sh
```

关键结果：

- `spark-1130` / `Developer` / `aarch64`。
- NVIDIA GB10，驱动 `580.126.09`。
- Docker `28.5.1`，服务为 `active`。
- Docker Socket 为 `root:docker`、`660`，当前账号不可访问。
- NVIDIA Container Toolkit 存在，container CLI 可识别 GPU。
- 用户级 FFmpeg 为 `7.0.2-static`。
- `~/skillforge` 及数据、输出和缓存目录存在。
- `nvcc` 不存在；当前预构建容器路线不要求安装。

### 自动测试

命令：

```bash
.venv/bin/python -m pytest
```

结果：`8 passed in 0.21s`。

覆盖范围：Schema、非法 JSON 修复、凭证脱敏、模型输出重试、状态机、受控错误检测和局部修订。

### P0 模拟闭环

命令：

```bash
.venv/bin/python -m skillforge.demo --output outputs/demo_run
```

结果：

| 指标 | 修订前 | 修订后 |
|---|---:|---:|
| 必要步骤覆盖率 | 88.89% | 100% |
| 必要步骤证据覆盖率 | 88.89% | 100% |
| 严重错误 | 5 | 0 |
| 冲突总数 | 5 | 0 |

工作流最终状态为 `COMPLETED`，自动变更 4 项。该结果只用于验证工程闭环，不能当作真实设备案例指标。

## 2026-07-14：原生 Python + FFmpeg 路线

### 本机原生摄取

生成了无版权的8秒测试视频、2页测试PDF和5秒测试录音，并执行：

```bash
.venv/bin/python scripts/generate_synthetic_assets.py
.venv/bin/python -m skillforge.ingest \
  --video outputs/synthetic_assets/synthetic_operation.mp4 \
  --pdf outputs/synthetic_assets/synthetic_manual.pdf \
  --audio outputs/synthetic_assets/synthetic_expert.wav \
  --output outputs/synthetic_ingest \
  --frame-interval 2 \
  --synthetic
```

结果：H.264/AAC 标准化成功，抽取4个关键帧、2页PDF文本与页面预览、规范化音轨，并生成6条通过 Evidence Schema 的候选证据。

### StepAudio ASR

使用本机系统语音合成一条无版权中文测试句，经 FFmpeg 转为16 kHz单声道WAV后调用 `stepaudio-2.5-asr`。ASR 返回22个SSE事件和21个带时间戳的增量片段，最终文本完整匹配测试句。

### N31 真实专家口述与 Gold v1

实际操作者录音文件时长约6分29秒，AAC、48 kHz、双声道；原始文件权限为600。FFmpeg检查得到平均音量约-25 dB、最大音量约-4 dB，规范化为16 kHz单声道后调用 `stepaudio-2.5-asr`。

结果：

- ASR返回1301个SSE事件、1300个逐字时间戳片段和1399字最终文本。
- 最终文本与逐字时间戳流相似度超过0.98；尺寸写法和第10–12题编号存在格式差异，以逐字时间戳流定位、两种原始结果均保留。
- 受控术语表校正N31、缝标、导纸夹、胶辊和72×130毫米等识别项；12个问题的必要术语检查全部通过。
- 12个问题分别生成 `E143`–`E154` 已验证专家口述证据，完整 Evidence Catalog 为154条。
- 13步Gold SOP全部为 `VERIFIED`：10步必需，S05、S06、S11条件执行。
- 删除无明确来源的“短按0.5秒”精确参数，补齐72×130毫米参数的口述证据。
- Gold评测初始发现5个高严重度冲突，4项局部修订后降为0；必要步骤和证据覆盖均由90%恢复到100%。
- 本机全量自动测试为 `32 passed`。
- 本机真实Web进程的 `/health`、`/api/n31` 和首页均返回HTTP 200；N31接口返回 `GOLD / FINAL`。

### Web Demo

原生 Uvicorn 服务在本机启动后：

- `GET /health` 返回200，`runtime=native-python`、`docker_required=false`。
- `POST /api/demo/run` 返回200，闭环状态为 `COMPLETED`。
- 首页返回200并展示问题、证据、修订前后和修订审计。
- Web 上传接口使用三类合成素材完成原生摄取并返回证据候选。
- ASR默认关闭，未确认外部处理授权时接口拒绝发送音频。

### Step 3.7 感知与 SOP 规划

使用合成视频关键帧执行真实 Step 3.7 多模态调用。首轮测试因输出预算不足未产生JSON，客户端按设计拒绝；把视觉输出预算提高并补全必填字段约束后，返回结果通过 Evidence Schema。程序随后覆盖并锁定证据ID、源文件、时间点和关键帧路径，确保模型不能修改引用位置。

使用模拟案例的13条规范证据执行 SOP 规划，实际返回10个必要步骤、13个证据引用；全部引用属于输入 Evidence Catalog，完整结果通过 SOP Schema。

### DGX 原生验证

项目运行副本同步到 `~/skillforge/app`，不包含 `.env`、Git历史或原始用户素材。通过本地准备的 `aarch64` wheelhouse 建立 Python 3.12 虚拟环境。

结果：

- Gold流水线加入前28项自动测试通过；同步本批Git跟踪代码和Gold JSON后，32项自动测试通过。
- DGX 无 `ffprobe` 时成功使用 FFmpeg 回退探测。
- 原生摄取输出4帧、2页和6条证据候选。
- 实际操作者审核的Gold闭环在DGX完成：严重问题5→0、必要步骤覆盖90%→100%、证据覆盖90%→100%，状态为 `GOLD / FINAL / COMPLETED`。
- Web健康检查、N31接口和首页均返回200；`n31_rehearsal_available=true`，N31接口返回4项局部修订。
- 同步范围仅为Git跟踪文件，没有上传原始录音、视频、PDF、私有面单或本地 `.env`，明确不需要 Docker。

## 2026-07-17：N31 视觉序列、多源对比与演示兜底

### Step 3.7 真实关键帧序列复核

使用 `scripts/run_n31_visual.sh`，为13个Gold步骤分别组合步骤直接引用关键帧及同源相邻帧，每步最多6帧。只发送已通过本地机器检查和人工抽检的安全派生JPG；不发送原始视频、手册、录音或真实面单。

结果：

- 13个步骤均返回通过 Visual Assessment Schema 的结果；模型为 `step-3.7-flash`。
- 12步为 `PARTIAL`，S04为 `NOT_VISIBLE`，0步为 `SUPPORTED`，0步为 `CONTRADICTED`。
- 整体至少部分可观察率为12/13；10个必要步骤中9个至少部分可观察。
- 严格标准要求关键动作和完成状态同时可见，因此不把5秒间隔稀疏帧误报为完整支持。
- 视觉模型标记11步可能出现编码或二维码形状；本地安全派生QA已通过，这些模型标记只进入保守复核队列。
- 首轮中途出现截断或非法JSON时客户端拒绝发布；增加4096 token预算、严格输出示例和逐步检查点后完整成功。

### 多源消融

使用同一个 `OPERATOR_REVIEWED_GOLD` 对手册、视频、专家口述和组合来源进行必要步骤覆盖对比：

| 来源条件 | 覆盖 |
|---|---:|
| 手册单源 | 8/10 |
| 专家口述单源 | 9/10 |
| 视频引用存在 | 10/10 |
| 视频严格视觉支持 | 0/10 |
| 视频至少部分可观察 | 9/10 |
| 至少两种来源 | 10/10 |

这组结果区分了“存在视频引用”和“当前关键帧足以严格证明动作”。Gold规则闭环仍稳定检测5个高严重度问题并通过4项局部修订降为0。

### Web 与三种演示模式

新增现场重算、完整预处理和离线无素材三种模式。离线演示包只含结构化Gold步骤、冲突、修订审计、指标、检查清单和测验。

本机验证：

- 全量自动测试：`35 passed`。
- `offline` 模式的 `/health` 和 `/api/n31` 返回HTTP 200，接口包含视觉和多源评测。
- `POST /api/n31/run` 返回HTTP 200，现场重算结果为 `GOLD / FINAL`、严重错误5→0、4项局部修订。
- 现场重算后再次读取 `/api/n31` 仍包含视觉和多源评测。

DGX Python 3.12 复验：

- 只同步本批Git跟踪文件和无素材离线包，未同步 `.env`、视频、关键帧、录音、PDF或真实面单。
- 全量自动测试：`35 passed`。
- 远端回环地址的 `/health`、`/api/n31` 和 `POST /api/n31/run` 均返回HTTP 200。
- 离线接口返回 `GOLD / FINAL`、视觉矛盾0步、至少两种来源覆盖100%；现场重算返回严重错误5→0和4项局部修订。
- 现场重算后视觉与多源结构化结果仍存在；烟测结束后远端验证服务已停止。

### Web 培训成果

本机离线模式进一步验证：

- `/api/n31` 返回13项检查清单、5题培训测验和 `COMPLETED` 工作流。
- 最终SOP、检查清单、测验和修订审计四个白名单下载均返回HTTP 200及附件文件名。
- 未列入白名单的 `private-video` 请求返回HTTP 404，下载接口不接受任意路径。
- Web专项与全量测试继续通过，共 `35 passed`。

同一批代码同步到DGX后再次通过35项测试；远端回环Web返回13项清单、5题测验和 `COMPLETED` 工作流，四项白名单下载均为200，`private-video` 为404。验证命令通过退出清理钩子停止Web进程。

### N31 A4培训海报

从 `cases/n31/gold/gold_sop.json` 确定性生成 `output/pdf/n31_a4_training_poster.pdf`：

- 单页标准A4，595.276 × 841.89 pt，130442字节，无加密和JavaScript。
- 包含设备、72mm × 130mm介质、缝标定位、13步操作、3个条件步骤、异常处理、完成标准和隐私提示。
- 相同输入重复生成的SHA-256一致，当前为 `d87dd874dc332d8057f0a1106b5b42085f6127f87ddeaa8f4b8a79eee08a8fba`。
- 使用 Poppler 以150 dpi渲染为PNG并人工检查，未发现裁切、重叠、黑块、乱码或不可读文字。
- PyMuPDF检查40个文字块均在页面边界内；文本抽取包含N31、S01和S13。
- Web海报下载返回HTTP 200、`application/pdf`，下载字节与生成文件完全一致。
- 全量自动测试新增海报确定性、页数、尺寸和文本检查后为 `36 passed`。

DGX验证时，旧项目虚拟环境因未安装ReportLab而在测试收集阶段停止。检查确认不是代码或PDF错误后，将 `reportlab 4.4.9`、`Pillow 12.2.0` 和 `charset-normalizer 3.4.9` 的Python 3.12 aarch64 wheel加入既有远端wheelhouse并离线安装。原锁定值 `Pillow 12.3.0` 没有对应aarch64 wheel，因此调整为可跨架构复现的12.2.0。

修复后：

- DGX全量测试为 `36 passed`。
- 远端重新生成海报为单页595.28 × 841.89 pt，可抽取1067字符，包含S01和S13。
- 远端Web海报接口返回HTTP 200、`application/pdf`和有效 `%PDF-` 文件头。
- 未使用Docker，未修改系统Python，未同步原始N31素材。

## 2026-07-17：N31 80秒培训视频

新增 `skillforge.training_video`，以可审计分镜重剪6段 `LOCAL_QA_PASSED` 自摄安全派生视频。每个步骤镜头同时绑定Gold步骤、Evidence ID、来源ID和源视频时间窗口；片头片尾不得伪装成Gold步骤，条件步骤必须与Gold保持一致，未知Evidence或不重叠的视频引用会直接拒绝生成。

### StepAudio旁白

- 模型：`stepaudio-2.5-tts`；音色：`zhixingjiejie`。
- 输入：371字合并旁白；未发送视频、手册、录音、面单或Evidence正文。
- TTS原始音频：85560毫秒、48kHz；为适配80秒成片执行1.0695倍语速调整。
- TTS缓存和临时Header均为`600`；结构化日志只记录模型、音色、字符数、字节数和哈希。

### 成片与证据

- 输出：`output/video/n31_training_video_v1.mp4`。
- 参数：80000毫秒、1920×1080、30fps、H.264、AAC、48kHz。
- CRF 22归档版本：35194451字节；SHA-256 `61ec74826befc89889a6025a9acdb217adbd32af589a9c1f4bb4be1958ff695f`。
- 覆盖：15镜头、13/13 Gold步骤、10/10必要步骤、30次Evidence引用。
- 独立证据包：15个镜头、25条去重Evidence，`contains_raw_media=false`、`contains_credentials=false`，并与成片SHA-256双向绑定。

### 质量与Web

- 自动QA：时长、尺寸、帧率、音频、步骤覆盖、Evidence边界和来源隐私门禁全部通过。
- 音频：综合响度-16.18 LUFS，真峰值-1.43 dBTP。
- 视觉：检查16帧联系表和5张1080p重点帧；字幕安全区、S11裁切、S12-S13遮挡通过。S04没有完整接电画面，因此字幕明确写“接电见手册P2”，不伪造视觉支持。
- Web：`/api/n31`返回生成清单；内嵌媒体端点支持Range读取；视频、生成清单和证据包使用固定白名单下载，展示前核对视频和证据包SHA-256。
- 当前状态：`READY_FOR_HUMAN_REVIEW`。AI辅助抽帧检查不替代参赛者最终完整观看。
- 本机视频与Web专项测试11项通过；全量测试待本批最终回归记录。

本机培训视频首轮最终回归为 `44 passed`。随后只将本批代码、Schema、文档、分镜和 `output/video/` 三个公开安全产物同步到DGX `~/skillforge/app`；未同步 `.env`、原始视频、手册、录音、真实面单、私有照片、关键帧或TTS缓存。

首次macOS归档携带19个 `._*` AppleDouble辅助文件，远端 `schemas/._*.schema.json` 被Schema目录扫描器当作JSON，导致22项测试失败。确认失败输入均以 `Mac OS X` AppleDouble头开头后，只删除本次同步产生的19个辅助文件；未修改任何业务数据。清理后：

- DGX Python 3.12首轮清理后全量测试：`44 passed`。
- 远端成片：35194451字节，SHA-256与本机及证据包绑定值一致。
- `/health`：`training_video_available=true`、`training_video_status=READY_FOR_HUMAN_REVIEW`。
- `/api/n31`：13/13 Gold步骤覆盖；视频清单校验通过。
- 视频Range请求：返回32字节并包含MP4 `ftyp`头。
- 视频证据包下载：25条去重Evidence，`contains_raw_media=false`。
- 烟测结束后远端Uvicorn进程自动停止。

后续从macOS制作同步归档时必须禁用扩展属性，或在测试前拒绝任何 `._*` 文件，避免AppleDouble辅助文件进入Schema目录。

同时在Schema注册器中增加 `._*` 显式忽略规则和专项测试。再次使用不生成AppleDouble文件的归档同步后，远端项目内 `._*` 数量为0，本机与DGX最终全量测试均为 `45 passed`；成片哈希和审核状态保持不变。

## 2026-07-17：DGX用户服务与安全隧道

- 用户级 `skillforge-demo.service` 通过 `systemd-analyze --user verify`，状态为 `enabled` 和 `active/running`，用户 `Linger=yes`。
- 服务固定使用仓库内离线Gold包并只监听 `127.0.0.1:7860`；环境明确设置跳过 `.env`，日志未发现密钥、Authorization Header或私有路径。
- 两次对主进程执行SIGKILL后，systemd均以新PID恢复；每次恢复后 `/health`、`/api/n31` 和Gold现场重算再次通过，结果为 `GOLD / FINAL / COMPLETED`、严重问题5→0和4项局部修订。
- 本机SSH隧道烟测将 `127.0.0.1:17860` 映射到DGX回环服务，退出后本机端口自动关闭；没有修改公网安全组、系统防火墙或Docker权限。
- 本机与DGX全量测试均为 `51 passed`。

## 2026-07-17：可复现运行时基准

新增运行时Schema、Python运行器、一键脚本、命令行入口和路演验收断言。基准固定执行2次预热、20次正式测量；临时输出留在项目 `outputs/` 下并自动删除，不读取凭证、不处理原始媒体、不调用外部模型。

本机 Darwin arm64 / Python 3.14：

- 直接Python Gold闭环：中位38.755毫秒，P95 39.317毫秒，最大39.707毫秒。
- Web现场重算：中位44.523毫秒，P95 49.086毫秒，最大50.279毫秒。
- 基准进程高水位RSS：100,057,088字节。

DGX Linux aarch64 / Python 3.12.3 / NVIDIA GB10：

- 直接Python Gold闭环：中位37.626毫秒，P95 37.758毫秒，最大37.796毫秒。
- Web现场重算：中位44.800毫秒，P95 56.040毫秒，最大59.932毫秒。
- 基准进程高水位RSS：86,491,136字节。

两类基准的20个样本均返回 `GOLD / FINAL / COMPLETED`、严重问题5→0、4项局部修订、输出234,237字节和外部模型调用0次。路演验收增加第10项必需成果并通过运行环境、样本数、业务断言、资源与数据边界检查；本机与DGX最终全量测试均为 `55 passed`。

以上性能数据只覆盖已审核结构化Gold的确定性质检、局部修订和成果输出；Web数字使用进程内HTTP，不包含网络传输。它们不代表原始视频、PDF、录音的完整预处理速度，也不代表GPU语义推理速度。

## 2026-07-17：提交预检与参赛材料

新增 `skillforge.submission`、提交预检Schema和 `scripts/check_submission.sh`。检查器复用路演验收，并增加项目身份、9份说明文档、Git工作树、跟踪敏感路径、`.env`忽略与600权限、实际本地密钥值泄漏、10项公开成果绝对路径和全量测试检查。输出只记录相对路径、计数和状态，不记录实际密钥值或原始媒体。

开发工作树验证：

- 10项路演成果、9份文档、项目身份和公开成果边界通过。
- `.env`存在、权限600、被Git忽略；检查1个本地密钥值，Git跟踪文件泄漏数为0。
- 全量测试为 `60 passed`。
- 因实现尚未提交，Git工作树检查按显式 `--allow-dirty` 标为 `SKIPPED`，总体为 `DEVELOPMENT_CHECK`，没有误报为正式就绪。
- `DEVELOPMENT_CHECK` 返回退出码2；只有五项门禁全部关闭后的 `READY_FOR_SUBMISSION` 才返回0。
- 五项门禁保持 `PENDING`：成片完整观看、180秒彩排、最终有声字幕录屏、团队资格和官方规则讲义。

DGX Python 3.12复验：

- 公开安全文件同步后项目内AppleDouble文件为0；未同步 `.env`、原始视频、手册、录音、真实面单或私有照片。
- 全量测试为 `60 passed`。
- 路演验收为 `READY_WITH_HUMAN_GATES`，10项成果和全部自动断言通过，五项门禁与本机一致。
- DGX部署目录没有Git元数据；使用 `--allow-missing-git --allow-dirty` 时测试通过，Git检查明确为 `SKIPPED`，总体为 `DEVELOPMENT_CHECK`。该结果只验证部署副本，不替代本地正式提交预检。

同时新增 `docs/架构与数据边界.md` 和 `docs/参赛提交材料.md`。后者包含20秒、约100字和一页版介绍、可复现指标、演示顺序、已知限制及待参赛者填写字段，没有编造团队、公开链接或官方规则。

## 2026-07-17：Gold对齐连续动作候选窗口

新增 `temporal_action_windows` Schema、确定性生成器、一键脚本、Web面板、白名单下载和路演自动断言。输入固定绑定Gold SOP、Step 3.7严格视觉复核和DGX原生CUDA候选报告的SHA-256；生成阶段不读取原始媒体、凭证或绝对路径，外部模型调用为0。

本机验证：

- 13个Gold步骤的51次已审核帧引用按同一来源时间线合并为19个开始/结束时间窗口，覆盖6段视频。
- 12个窗口命中附近的DGX场景候选时间点，来源内去重后共41个；范围固定为 `GOLD_ALIGNED_CANDIDATE_WINDOW_ONLY`。
- S04窗口为60,000至75,000毫秒并绑定62,000、63,000、67,000毫秒三个候选点，但视觉结论仍为 `NOT_VISIBLE`，没有把邻近时间点冒充动作支持。
- 路演验收更新为11项必需成果和8组核心主张，全部自动断言通过，状态为 `READY_WITH_HUMAN_GATES`。
- 暂存后开发态提交预检通过9份文档、11项成果、`.env`边界、1个本地密钥值和179个Git文件检查；全量测试为 `66 passed`，因工作树尚未提交而按预期返回 `DEVELOPMENT_CHECK` 和退出码2。

DGX Python 3.12复验：

- 使用不携带 `.env`、原始媒体或AppleDouble元数据的安全归档同步公开文件；项目内 `._*` 数量为0。
- 全量测试为 `66 passed`，路演验收11项成果和8组核心主张全部通过，状态为 `READY_WITH_HUMAN_GATES`。
- 部署副本提交预检按 `--allow-missing-git --allow-dirty` 运行，Git相关检查明确跳过，总体为 `DEVELOPMENT_CHECK`；该结论不替代本地干净工作树正式预检。

## 2026-07-17：PDF结构化解析、中文OCR与页码检索

新增PDF布局分块、纯文本回退、页级OCR质量门禁、私有检索索引、公开结构报告Schema、一键脚本、Web面板、路演断言和专项测试。OCR支持 `disabled / auto / required` 三种模式；中文与英文Tesseract数据固定到提交 `87416418657359cb625c412a48b6e1d6d41c29bd`，下载后分别核对SHA-256并以600权限保存到Git忽略缓存。

本机真实手册验证：

- 两份N31手册共58页、28,172字符、607个结构块：49个标题、87个列表、448个段落、5个表格和18个警告。
- 精细质量门禁识别9个文本层不足页面并执行 `chi_sim+eng` OCR，最终待OCR页为0；用户手册第16页渲染复核确认只有页眉、页脚和小型Logo，图像覆盖率门禁不会把装饰图误判为待OCR正文。
- 私有索引保持来源、PDF页码、块ID和结构类型；“进纸导轨”首条精确命中用户手册第14页，“缝标模式标签学习”和“按住走纸键”首条精确命中第20页。
- 完整执行 `run_n31_local.sh` 后仍为8个来源、142条Evidence、13步候选SOP、Gold严重问题5→0和4项局部修订；两份PDF资产分别记录9页与0页OCR，原有页码选择器和Gold闭环未漂移。
- 公开 `pdf_structure_v1.json` 通过Schema，包含两份输入哈希、统计、OCR数据哈希和检索页码，不含手册正文、页面图、私有索引、绝对路径或凭证。
- 本机全量测试为 `72 passed`；路演验收12项成果和9组核心主张全部通过，状态为 `READY_WITH_HUMAN_GATES`。
- 暂存后开发态提交预检检查186个Git文件，12项成果、9份文档、`.env` 600权限、1个本地密钥值和敏感路径边界全部通过；因尚未提交按预期返回 `DEVELOPMENT_CHECK` 与退出码2。

DGX Python 3.12安全副本复验：

- 同步包不含原始手册、OCR页面、含正文索引、`.env`或AppleDouble元数据；项目内 `._*` 数量为0。
- 全量测试为 `72 passed`；公开PDF报告58页、607块、9页OCR、0页待处理和三项检索断言通过。
- `/api/n31`与PDF结构报告白名单下载均返回200；路演验收12项成果通过，部署副本提交预检按预期为 `DEVELOPMENT_CHECK`。

## 2026-07-17：三来源候选步骤抽取、合并与排序

新增来源候选计划与合成报告Schema、`source_candidates.py`、一键脚本、N31真实输入、Web面板、路演断言和专项测试。该阶段读取完整Evidence Catalog但不读取Gold步骤文本，不调用外部模型；当前运行模式明确标为 `REVIEWED_SOURCE_PROJECTION`，不能冒充开放式无提示动作发现。

本机真实N31验证：

- 分别形成视频18条、PDF手册7条、专家口述8条候选，共33条；每条Evidence ID均验证存在且来源类型、来源引用完全一致。
- 8条 `TOO_COARSE` 候选拆成18个语义片段，8条 `TOO_FINE` 候选参与合并；最终得到13个规范步骤和12个实际合并组。
- 阶段分布为准备3、执行6、验证4、复位0；复位明确为成功路径不适用，异常路径由12个步骤级恢复策略覆盖。
- 依赖图无环；S07不再强制依赖条件步骤S06，而是依赖S04。S11和S12因消耗标签被标记为不可逆耗材动作。
- 12个步骤至少含两类来源，10个步骤含三类来源；S04只由快速指南PDF支持，`NOT_VISIBLE`视频证据E014未进入合并结果。
- `run_n31_local.sh` 完整通过：8个输入来源、142条初始Evidence、33条来源候选、13步合并结果、Gold严重问题5→0、4项局部修订，外部模型调用0次。
- 新增候选报告Web接口和下载均通过专项测试；路演验收新增 `source_candidates_grounded` 断言并把报告列为必需成果之一。
- 报告不含原始媒体、凭证、绝对路径或外部模型调用；公开文本与项目敏感模式扫描无命中。

## 2026-07-17：可解释置信度与审核路由

在三来源候选合成中加入证据权威性、审核状态、支持状态、多源佐证和负面观察五部分可解释评分。评分只用于候选路由，不覆盖Gold结论，也不会把来源数量直接等同于事实正确。

本机验证：

- 证据分类权重固定为 `SOURCE_FACT=1.0`、`EXPERT_ADVICE=0.9`、`MODEL_INFERENCE=0.65`；每个来源保留候选ID、分类、原始置信度、支持系数、审核系数和调整后分数。
- 两类来源佐证增加0.04，三类来源增加0.08；`NOT_VISIBLE / CONTRADICTED / MISSING_SOURCE / SOURCE_CONFLICT` 分别有独立惩罚上限，报告记录负面观察ID。
- 13步最终分布为 `HIGH=6`、`MEDIUM=6`、`LOW=1`；路由为 `AUTO_VERIFY=6`、`VERIFIER_QUEUE=6`、`HUMAN_REVIEW_REQUIRED=1`。
- S04仅有快速指南PDF候选，基础调整分为0.799；视频 `NO001 / NOT_VISIBLE` 置信度0.9产生0.108惩罚，最终0.691并进入人工复核。
- 专项测试同时覆盖候选跨来源借证据、负面观察跨来源借证据、依赖环、向后恢复目标和精确重复去重；Web与路演断言固定检查S04保守路由。

## 2026-07-17：三种SOP视图与交互检查清单

新增 `sop_views`、`mobile_checklist` 和 `checklist_session` 三份Schema。Gold、候选和模拟闭环统一从修订后SOP生成简洁版、详细版、带证据版和一步一屏检查清单；离线Gold包新增 `sop_views.json`，Web新增白名单下载、逐步关键帧、风险与证据展开、完成勾选和问题反馈。

- 三种SOP视图均为13步，每步保留动作、原因、完成标志、风险和来源；带证据版逐条展开证据分类、审核状态和PDF页码或音视频时间点。
- 手机检查清单为 `ONE_STEP_PER_SCREEN`，13步均带完成标志、风险、Evidence和可用关键帧；S04使用60.0至65.0秒定位帧但严格显示 `NOT_VISIBLE`，页面明确提示该画面不能证明连接电源和开机动作。
- 完成与重开操作写入 `completion_log`，四类问题反馈写入 `feedback_log`；会话使用不可预测ID，目录权限700、JSON文件权限600，位于Git忽略的 `outputs/` 下。
- Web现场验证：首页、健康检查、N31数据、三视图下载和E001本地关键帧均返回200；会话创建、完成S01、提交反馈和重新读取记录全部成功。
- 路演成果增至14项，新增 `training_package_traceable` 自动断言；专项测试13项、全量测试82项和JavaScript语法检查均通过。
- 从已审核80秒成片的每个步骤镜头中点提取13张640像素宽JPEG，合计约432KiB；联系表人工检查未见真实面单信息，S12/S13仍使用成片内的空白脱敏标签画面。
- 缩略图清单绑定成片SHA-256，并逐张记录步骤、镜头、时间点、大小和SHA-256；Web默认读取公开预览接口，路演新增 `checklist_previews_public` 哈希断言。加入该回归后本机全量测试为 `83 passed`。

DGX Python 3.12安全同步复验：

- 使用仅含Git跟踪文件的归档同步到 `~/skillforge/app`，同步前后SHA-256一致；归档不含 `.env`、原始媒体或私有派生目录。
- 全量测试为 `82 passed`，路演验收14项成果全部通过，状态为 `READY_WITH_HUMAN_GATES`。
- 重启已有用户级SkillForge服务后，N31接口返回简洁、详细和带证据三种视图；S04仍为 `NOT_VISIBLE`。
- 创建检查会话并完成S01后，进度为1/13；远端会话JSON权限为600。整个过程未处理Docker权限或修改系统防火墙、云安全组。
- 同步13张成片安全预览后，DGX全量测试更新为 `83 passed`；用户服务重启成功，S01预览接口返回HTTP 200和有效JPEG，清单13步均绑定公开预览路径，S04仍保持 `NOT_VISIBLE`。

## 2026-07-17：五类证据培训测验

新增 `training_quiz.schema.json` 和确定性测验生成/完整性校验。题目固定覆盖 `ORDERING / TOOL_SELECTION / RISK_RESPONSE / STATUS_RECOGNITION / ERROR_JUDGMENT`，类型分别为排序、工具多选、风险单选、状态单选和错误判断。

- N31 Gold测验生成5题：排序题覆盖S01至S04依赖链；工具题答案为S04已绑定电源；风险题使用S12隐私提示；状态题使用S13整体验收标志；错误判断拒绝忽略S13实体隐私遮挡要求。
- 正确答案和解析均保存非空Evidence集合；每个干扰项也绑定其原步骤Evidence，题目保存完整Evidence对象及PDF页码或音视频时间点。
- 完整性校验覆盖未知Evidence、答案/解析来源越界、选项来源越界、重复选项、答案不在选项、工具/风险/状态与SOP不一致、排序违反依赖和错误判断未拒绝风险。
- 新版测验通过Gold、候选和模拟回归；Web和白名单下载返回五类题及来源详情，JavaScript语法检查通过。
- 专项测试26项、全量测试90项通过；路演 `training_quiz_grounded` 断言通过，状态保持 `READY_WITH_HUMAN_GATES`。
