# SkillForge 任务拆解

目标提交日：2026-07-22

优先级：P0为必须完成；P1提高获奖表现；P2有余力再做。

## 0. 项目与赛事管理

### SF-000 核实提交规则｜P0

- [x] 建立公开赛事要求对齐矩阵，核对主题、多模态能力、DGX、日程和2–5人组队规则；未发现方向性偏离。
- [x] 复核赛事页和训练营页的匿名可访问内容，将公开确认8项、待官方细则6项固化为严格Schema和提交预检检查；规则课程没有公开讲义入口，未知项不得自动关闭。
- [x] 接收参赛者转交的新增完赛要求：跑通何老师参考代码为基础完赛标准；已建立 Git 忽略的 `external/teacher_he_reference/drop/` 独立投递区。
- [x] 接收并静态审计何老师参考Notebook和官方开幕PPT；保存文件哈希、结构、上游历史成功证据、危险单元和9类缺失bundle项，不把上游输出冒充本项目实跑。
- [x] 完成何老师参考材料静态分析并明确边界；按参赛者2026-07-19决定暂不复现Workshop，以SkillForge真实N31 P0闭环为主，不把参考Notebook上游输出声明为本项目实跑。
- [x] 从官方开幕PPT第9页取得六项评分权重，并映射到现有成果和风险。
- [x] 确认开源项目URL、600字以上说明、部署说明、技术栈、作品视频和团队合影要求；视频时长/格式和现场路演方式仍待补充。
- [ ] 确认是否必须实机运行、是否允许外部API、是否要求开源。
- [ ] 确认已报名队伍满足2–5人且一人仅限一队。
- [x] 为18项冻结成果逐项指定负责人角色和独立最终检查角色；真实姓名映射待最终团队名单确认，并只保存在私有提交状态。
- [x] 建立2–5人私有团队名单和六类职责校验器；姓名、单位和成员编号均不进入Git或安全QA报告，机器检查不能替代资格人工确认。

交付物：赛事要求清单。验收：没有未知的必交材料。

### SF-001 建立开发管理｜P0

- [x] 建立本地项目目录。
- [x] 建立环境变量和忽略规则。
- [x] 建立机器可验P0任务看板、每日状态检查命令和风险清单；等待人、等待外部、技术就绪和已完成分开表达，不把提交门禁写成整个实现目标受阻。
- [x] 确定代码分支、Review和冻结规则：`main`、P0成果必须独立终检、干净工作树生成冻结清单、成果变化后重新冻结；参赛者授权后已配置公开GitHub远端并完成推送。

交付物：任务看板、风险表。验收：每个P0任务有负责人和截止时间。

## 1. 场景与产品定义

### SF-100 锁定唯一主案例｜P0

- [x] 确定设备与操作任务：汉印 N31 更换标签纸、介质学习与试印验收。
- [x] 确定目标用户和真实痛点：电商仓库、门店和小型物流团队的新操作员；换纸后的导轨、定位模式、学习和验收容易遗漏。
- [x] 确认公开展示边界：参赛者本人自摄视频、照片、口述及其脱敏衍生成果可用于黑客松提交/展示；第三方教程、厂商手册原件和真实面单继续排除在公开成果之外。
- [x] 确认领域审核者：参赛者本人是N31设备实际操作者，已通过专家口述和逐步审核生成 `OPERATOR_REVIEWED_GOLD / Gold v1`；第二名熟悉标签打印机的复核者仅为增强项。
- [x] 写出20秒、约100字和一页版项目介绍，统一保存在 `docs/参赛提交材料.md`。

推荐条件：3–8分钟可完成、8–15个步骤、动作视觉清晰、有权威手册、有成功标准。

验收：团队任何成员能在20秒说明用户、输入、输出和价值。

### SF-101 固化演示故事｜P0

- [x] 设计上传视频/PDF/录音的起点。
- [x] 设计一次可控的遗漏步骤或顺序冲突。
- [x] 设计证据定位和自动修订过程。
- [x] 设计最终培训包展示。
- [x] 将全流程压缩到三分钟内；运行单精确覆盖180秒并通过连续性校验。

验收：演示中明确出现“发现问题→引用证据→局部修订”。

### SF-102 冻结范围｜P0

P0仅包含SOP、证据、质检、修订、检查清单、测验和可演示UI。

以下默认为P2或砍掉：数字人、多语言、全生成视频、多行业、真实机器人控制、复杂权限系统、大规模微调。

## 2. 素材与Gold数据

### SF-200 拍摄操作视频｜P0

- [x] 准备 N31 分镜、设备初始状态、隐私要求和拍摄验收单。
- [x] 完成目标5分钟的一镜到底逐步脚本，包含操作、动作、台词、时长和完成标志。
- [x] 从私有历史面单提取75.85 × 129.82 mm页面几何；随后以实物确认的折叠纸 72.0 × 130.0 mm 为准，生成无真实客户数据的测试面单。
- [x] 私有归档一次真实试印照片，确认 N31、折叠供纸和成功出纸；照片含真实物流内容，不提交 Git、不进入演示。
- [x] 按用户决定将历史真实面单作为本机试印文件，虚构测试面单降为未采用的可选兜底；拍摄采用全景和实体遮挡，禁止正文进入外部处理或公开演示。
- [x] 私有保存标签背面交界处照片；确认无周期性黑标，将本批介质锁定为缝标并选用缝标学习。
- [x] 接收并检查一条20.72秒竖屏剪辑教程；用户确认由京东购机订单客服私发，仅作本地辅助参考，公开展示和外部 AI 处理未获授权。
- [x] 接收并检查首版5分56秒横屏1080p连续主视频；主要动作可见，但真实面单正文可辨识、音量偏小，标记为私有复盘并要求重拍。
- [x] 接收七条短视频并按介质、导纸、学习、进纸、结果和补充镜头完成语义化重命名。
- [x] 检查横竖屏、时长、编码、音轨、旋转、关键动作和隐私暴露，保存视频验收记录。
- [x] 接收四条第二版重拍素材；主视频时长和流程通过，导纸夹特写通过，介质类型与结果特写的横屏和时长通过。
- [x] 对第二版主视频尾段、介质类型和结果特写中残余的唯一标识、条码和二维码做本地不透明遮挡或安全片段拼接，生成正式文件并完成机器检查与高频抽检；同时规范化并限幅音频。
- [x] 保存原始文件；用户确认本人是参赛选手和自摄素材提供者，自摄素材用于项目处理及赛事提交/展示无需另签授权书。第三方教程、手册和真实面单仍保持受限。

验收：不看手册也能由视频复述完整操作。

### SF-201 准备设备手册｜P0

- [x] 收集 N31 官方用户手册 Rev.1.0 和快速安装指南 Rev.1.5，原文件仅存私有输入目录。
- [x] 存档 N31 官方产品页长截图，记录访问时间、SHA-256和可核对的产品参数。
- [x] 标记介质规格、装纸、卡纸处理、走纸、自检和标签学习的相关页。
- [x] 对文本层不足页面执行本地 `chi_sim+eng` OCR并保留质量门禁；N31两份手册9页执行OCR、0页待处理，关键术语检索命中页码与Gold定位一致。
- [x] 保留 PDF 页码与手册印刷页码映射。

验收：任一关键步骤可定位到正确页码。

### SF-202 录制专家口述｜P0

- [x] 准备 N31 新手易错点、异常信号、学习模式、完成标准和手册遗漏经验的口述提纲。
- [x] 将空白提纲改为可直接照读的预填答案版，明确区分已有证据结论与必须由实际操作者确认的经验。
- [x] 录制6分29秒清晰单人语音；虽然超过原建议时长，但内容完整且音质通过，不要求重录。
- [x] 使用 StepAudio ASR 获取逐字时间戳，并用受控术语表校正N31、缝标、导纸夹、胶辊和72×130毫米等识别项；原始转写保留供审计。
- [x] 提取操作者使用经历、学习触发条件、导纸夹松紧、最低验证、自检条件、排障顺序、风险动作和成功标准等手册外经验。

### SF-203 制作Gold SOP｜P0

- [x] 将 N31 候选清单扩充为13步证据绑定 JSON，并生成逐步人工审核队列和可填写审核表；当前明确标记为非 Gold。
- [x] 预填13步必需/可选/改写建议、不可交换顺序、风险和成功标准，并由实际操作者口述确认。
- [x] 标注动作、对象、前置条件、工具、风险和成功标准。
- [x] 标注PDF页码、视频时间点和12段专家录音时间点。
- [x] 标注10个必需步骤、3个条件步骤和不可交换顺序。
- [x] 由设备实际操作者完成审核，生成 `OPERATOR_REVIEWED_GOLD / Gold v1`。

验收：每个必需步骤至少有一个可靠证据。

### SF-204 构造错误集｜P0

- [x] 删除一个必要步骤（已在明确标注的模拟案例中实现）。
- [x] 调换两个不可交换步骤（已在明确标注的模拟案例中实现）。
- [x] 添加一个无来源工具、参数或动作（已注入工具和参数）。
- [x] 为每种错误记录正确答案和证据（模拟基线）。
- [x] 在 N31 候选基准上完成一次受控彩排：缺步骤、错误顺序、无依据工具和无依据参数均被检测并局部修订；结果标为 `PROVISIONAL_ONLY`，待 Gold 后重跑。
- [x] 使用实际操作者审核的 Gold v1 重跑受控错误集：5个高严重度冲突经4项局部修订后降为0，必要步骤和证据覆盖均恢复到100%，结果标为 `GOLD / FINAL`。

验收：质检链路能稳定发现三类错误。

## 3. 数据结构与证据模型

### SF-300 SOP Schema｜P0

字段：`step_id`、`title`、`action`、`object`、`prerequisites`、`tools`、`warnings`、`success_check`、`evidence`、`confidence`、`required`、`status`。

- [x] 编写JSON Schema。
- [x] 准备合法和非法样例。
- [x] 在所有模型输出后执行Schema验证。

### SF-301 Evidence Schema｜P0

- [x] 支持PDF页码和段落。
- [x] 支持视频起止时间和关键帧。
- [x] 支持专家录音时间点。
- [x] 支持来源事实、专家建议和模型推断的区分。
- [x] 支持相关性、置信度和复核状态。

验收：最终输出的每个关键事实都可以点击回源。

### SF-302 冲突模型｜P0

- [x] 表示冲突来源、冲突内容和严重等级。
- [x] 新增来源哈希绑定的冲突裁决会话，逐项记录自动决策、路由原因、人工确认、Revision Audit变更路径和最终采用结果；拒绝或待确认时最终SOP不可发布。
- [x] 安全声明、缺失证据、无效证据和 `REVIEW` 动作强制人工确认；故障注入证明即使 `UNSUPPORTED_SAFETY_CLAIM` 被标为 `automatic=true` 也会被人工门禁覆盖。
- [x] 本机与DGX Python 3.12均完成131项回归；DGX回环Web实测Gold五项自动采用、非法人工覆盖拒绝、两项路演断言和部署公开边界。

## 4. 多模态素材解析

### SF-400 视频预处理｜P0

- [x] 检查编码、帧率、分辨率和音轨。
- [x] 生成标准格式、低码率预览和时间戳映射；6段 `LOCAL_QA_PASSED` 视频均生成854×480以内、15 fps、H.264/AAC私有预览，逐项绑定源/预览SHA-256、时长和同一时间轴，外部模型调用为0；本机与DGX均完成生成、141项回归、Web Range播放和路演断言复验。
- [x] 按固定间隔和场景变化抽取关键帧（固定间隔已完成；DGX原生CUDA对420帧完成场景变化排序并选出50个候选时间点）。
- [x] 保存镜头区间和帧路径。

### SF-401 视频/录音ASR｜P0

- [x] 调用 `stepaudio-2.5-asr`。
- [x] 保存逐句时间戳。
- [x] 使用受控术语表校正N31、缝标、导纸夹、胶辊和72×130毫米等设备名、部件名、数字和单位。
- [x] 建立人工快速修正入口：私有会话保留原始ASR、基线、修正值、操作者、原因、时间和完整历史，并按当前文本重绑Evidence摘要；本机与DGX Python 3.12均完成135项回归，DGX回环Web和路演断言实测通过。

### SF-402 视频视觉理解｜P0

- [x] 描述关键帧中的工具、部件、动作和设备状态。
- [x] 在同一视频时间线上，将Gold步骤已审核关键帧区间合并为候选操作窗口；当前13步形成19个窗口。
- [x] 为每个候选窗口保存来源、开始/结束时间、时长、Evidence ID和附近DGX候选时间点。
- [x] 标注视觉不确定性和遮挡（通过置信度与待复核状态；遮挡专用字段待补）。
- [x] 按Gold步骤组合直接关键帧和同源相邻帧为最多6帧的严格视觉复核窗口，保留每帧来源与起止时间。
- [x] 13步真实视觉复核完成：12步部分可见、1步不可见、0步视觉矛盾；不足画面进入待确认，不伪造支持。
- [x] 新增DGX本地视觉特征阶段：亮度、对比度、边缘能量和相邻帧变化只用于候选筛选，`semantic_claim_scope`强制为 `CANDIDATE_SELECTION_ONLY`。
- [x] 候选窗口范围强制为 `GOLD_ALIGNED_CANDIDATE_WINDOW_ONLY`；不把窗口合并声明为无Gold引导的通用动作识别，S04仍保持 `NOT_VISIBLE`。

### SF-403 PDF解析｜P0

- [x] 提取文字并保持页码。
- [x] 按标题、段落、列表、表格、警告和图注切分；N31两份手册58页形成607个结构块。
- [x] 建立页码保真本地检索索引，块ID固定绑定来源、PDF页码和结构类型；含正文索引不进入Git。
- [x] 建立OCR质量门禁、中文/英文语言数据哈希校验和页级复查；“进纸导轨”命中第14页，“缝标模式标签学习”和“按住走纸键”命中第20页。

### SF-404 多源对齐｜P0

- [x] 建立案例清单驱动的多视频、多PDF本地摄取，保留每个视频的独立时间轴和每份PDF的原页码。
- [x] 完成 N31 首次本地 Evidence Catalog：6段视频、两份手册、142条候选证据，模型调用为0。
- [x] 将专家口述1300个逐字时间戳片段合并为12条已验证口述证据，绑定到相关SOP步骤，Evidence Catalog扩充到154条。
- [x] 按13个Gold步骤对齐视频关键帧、手册段落和专家经验，并保留来源类型差异。
- [x] 对齐失败或视觉不足内容进入 `needs_review_step_ids`；当前S04在选定窗口中不可见。
- [x] 输出13步 Gold 步骤及证据集合；专家经验、步骤级视觉复核和Gold对齐连续候选窗口均已完成。另已实现不接收Gold步骤文本或semantic key的结构化Evidence候选发现实验；原始媒体到通用动作语义发现仍作为P2后续增强。

验收：至少80%的必需步骤有两种来源支持，或有单一权威手册依据。

## 5. SOP规划Agent

### SF-500 候选步骤抽取｜P0

- [x] 分别从视频、手册和口述抽取候选步骤；N31 当前为视频18条、手册7条、口述8条，全部保留来源和 Evidence ID。
- [x] 对模型输出做Schema验证和失败重试。

### SF-501 步骤合并与排序｜P0

- [x] 合并同义动作并去重；精确重复保留被去重候选ID，语义合并保留完整成员和理由。
- [x] 拆分过粗步骤、合并过细步骤；N31 当前识别8条过粗候选、18个拆分片段和8条过细候选。
- [x] 区分准备、执行、验证和复位；N31 为3个准备、6个执行、4个验证步骤，成功路径无独立复位并显式标记 `NOT_APPLICABLE`。
- [x] 建立依赖关系、不可逆步骤和回退步骤；输出13步无环图、2个耗材不可逆步骤和12个异常恢复策略。

### SF-502 证据绑定｜P0

- [x] 必需步骤、参数和安全警告必须绑定证据（规划层执行引用边界检查）。
- [x] 专家经验明确保留 `audio / EXPERT_ADVICE` 来源，不与手册 `SOURCE_FACT` 混写为官方要求。
- [x] 无证据必要步骤和未知证据引用不得进入规划结果。

### SF-503 置信度｜P1

- [x] 根据来源数量、证据分类权威性、审核状态、多源佐证和负面观察惩罚计算可解释置信度。
- [x] 按 `HIGH / MEDIUM / LOW` 路由到自动验证、Verifier队列或人工确认；N31 的S04因视频 `NOT_VISIBLE` 自动进入人工确认。

### SF-504 无Gold候选发现｜P2

- [x] 新增独立候选发现响应、公开报告、评测规范和评测报告四类严格Schema；模型输入不包含Gold步骤文本、semantic key、来源路径或原始媒体。
- [x] 模型只可提议8–15步候选、动作字段和Evidence ID；来源类型、输入摘要、调用次数、审核原因和最终状态由本地代码生成，所有候选强制进入 `HUMAN_REVIEW_REQUIRED`。
- [x] 对未知/驳回Evidence、未说明的Evidence、嵌套引用越界、非连续步骤ID、前置步骤前向引用、阶段回退、绝对路径和凭证样式内容安全失败；非法JSON与越权字段最多三次Schema重试，本地Evidence门禁最多两轮。
- [x] 建立九条无版权虚构Evidence、离线响应夹具和一键脚本；确定性评测为Evidence召回100%、相邻顺序冲突0、三来源类型覆盖100%、人工复核收容率100%、自动Gold修改0。
- [x] 使用同一安全Evidence完成一次真实 `step-3.7-flash / medium` 验证：最终版本单次调用生成10个候选、9/9 Evidence有去向、依赖图无环、阶段单向推进；开发中前两次旧提示输出因Schema不合格被拒绝且未发布。
- [ ] 在取得新的、可外发的真实案例结构化Evidence后测量人工接受率、步骤合并/拆分修改数和语义正确率；当前不把虚构Evidence评测冒充真实领域结果。

## 6. 内容创作Agent

### SF-600 生成SOP｜P0

- [x] 输出简洁版、详细版和带证据版；三种视图均由同一份已验证SOP确定性生成并通过Schema。
- [x] 每步包含动作、原因、完成标志、风险和来源；带证据版进一步展开证据分类、审核状态和PDF页码或音视频时间点。

### SF-601 生成检查清单｜P0

- [x] 一步一屏、完成勾选、关键帧、风险提示和证据展开；从已审核80秒成片生成13张可安全同步的训练预览，S04关键帧明确保留 `NOT_VISIBLE`，不冒充动作支持。
- [x] 支持问题反馈和完成记录；记录仅写入Git忽略的本机目录，目录权限700、会话文件权限600。

### SF-602 生成培训测验｜P0

- [x] 生成排序、工具多选、风险单选、状态识别和错误判断五类题；干扰项取自其他有证据步骤，不凭空编造。
- [x] 每题答案和解析必须有来源；题目同时保存答案Evidence、解析Evidence、选项Evidence及页码或时间点详情。

### SF-603 生成A4海报｜P1

- [x] 从Gold SOP生成设备型号、介质参数、13个关键步骤、条件步骤、异常处理、风险提醒和完成标准。
- [x] 做A4打印可读性检查：单页标准A4，150 dpi渲染无裁切、重叠、乱码或越界文字。

### SF-604 生成培训视频｜P1

- [x] 新增案例交付配置Schema并绑定新操作员、简体中文普通话、60–90秒/目标80秒和六类必需输出；训练视频渲染入口直接校验case、语言、时长和MP4必需状态。
- [x] 生成15镜头分镜、371字旁白、逐镜字幕、条件步骤和风险提示，并将镜头绑定Gold步骤与Evidence。
- [x] 只重剪6段 `LOCAL_QA_PASSED` 自摄安全视频，不生成关键操作事实，不使用第三方教程。
- [x] 使用 `stepaudio-2.5-tts` 的 `zhixingjiejie` 音色生成旁白；仅发送文本，未发送原始素材。
- [x] 生成80秒、1920×1080、30fps、H.264/AAC横屏成片；自动QA和AI辅助联系表检查通过，最终参赛者完整观看仍待完成。

## 7. 质检与自动修订

### SF-700 确定性规则质检｜P0

- [x] 检查缺步骤、编号、依赖、参数来源、成功标准和引用有效性。
- [x] 拒绝无来源工具、参数和安全承诺；工具必须属于当前步骤，参数名称、数值、单位和Evidence必须精确匹配来源，额外安全提示和绝对安全承诺自动删除或恢复为有证据版本。
- [x] 以四个隔离篡改场景生成公开可复现门禁报告，逐项保存拒绝原因、Evidence边界、修订前后值、局部动作和复检残留数。

### SF-701 语义质检｜P0

- [x] 检查曲解来源、来源冲突、顺序错误和异常处理遗漏；13步Gold当前均为 `SUPPORTED`，四类发现项为0。
- [x] 使用 `step-3.7-flash` 高推理档进行复杂复核；只发送结构化步骤、36条步骤内Evidence陈述和约束，不发送原始媒体、完整转写、手册页面、本地路径或凭证。
- [x] Schema和本地完整性门禁要求逐步覆盖、引用不越界、来源冲突至少双来源、顺序风险至少跨两步；模型结果固定为 `MODEL_INFERENCE`，自动Gold修改0。
- [x] 使用 `step-3.7-flash` 对13步安全关键帧序列执行严格动作与完成状态复核，模型结果不得覆盖Gold事实。

### SF-702 视觉质检｜P1

- [x] 检查成片动作与文案、设备名称、字幕安全区、隐私遮挡和条件步骤标识；当前成片不使用可能误导的箭头。
- [x] 已检查Gold动作、完成状态与关键帧窗口的一致性，并复核16帧成片联系表；S04不伪装为完整视觉支持，明确提示接电依据见手册P2。

### SF-703 局部修订｜P0

- [x] 缺步骤时插入并更新依赖。
- [x] 顺序错误时局部重排。
- [x] 无证据内容删除或转人工确认。
- [x] 仅重做受影响的SOP、测验、海报或视频片段；当前审计精确失效S07–S13、Q02和V07–V13，保留6个无关步骤、4道题和8个镜头，A4仅因单页布局依赖整页重建。

### SF-704 修订审计｜P0

- [x] 保存修改前、问题、证据、修改后、原因和时间；确认人字段待人工入口接入。
- [x] UI展示修订前后对比、问题证据和局部修订审计。

### SF-705 停止条件｜P0

- [x] 最多三轮修订。
- [x] Gold复检严重错误为零。
- [x] Gold必需步骤覆盖率达到100%。
- [x] Gold关键参数全部有证据；无依据短按时长已删除。

## 8. 后端与编排

### SF-800 工作流状态机｜P0

状态：`UPLOADED`、`INGESTING`、`EXTRACTING`、`PLANNING`、`CREATING`、`VERIFYING`、`REVISING`、`RENDERING`、`COMPLETED`、`FAILED`、`NEEDS_REVIEW`。

- [x] 定义状态迁移、严格Schema检查点、失败分类和恢复规则；可重试失败从原阶段恢复，不可重试失败必须由操作者修复后显式重跑。
- [x] 状态级与产物级重跑已在本机和DGX Python 3.12完成127项回归；DGX现场完整运行与渲染重跑均为 `COMPLETED`，七个复用阶段哈希保持一致。
- [x] 每阶段可单独重跑：八个Gold产物阶段接入统一执行器；完整运行重建全部阶段，指定阶段重跑先验证并复用上游SHA-256，只实际重建所选阶段及下游，完成发布以原子指针切换。

### SF-801 Agent与工具接口｜P0

- [x] 固化Perception、SOP、Creator、Verifier、Revision五类职责、禁止动作、授权工具和5次交接，严格保留 `Verifier → Revision → Verifier` 复检环。
- [x] 提供PDF页、关键帧、视频/录音区间、Evidence检索、SOP规划、保存、渲染、规则/语义质检、局部修订、选择性重建和人工确认13个可审计工具；14次实际调用绑定19个成果哈希，本机与DGX均完成145项回归、Web接口和路演断言复验。

### SF-802 日志与容错｜P0

- [x] 记录调用模型、时间、重试、状态迁移、修订记录和逐阶段资源统计；每阶段保存耗时、CPU用户/系统时间、进程累计峰值RSS、输出字节和外部模型调用数，旧清单保持兼容。
- [x] Web展示DGX视觉计算的5步Perception/Verifier决策、工具和结果轨迹，明确区分GPU数值计算与语义结论。
- [x] 处理非法JSON、超时、限流、引用失效和渲染失败；渲染超时故障注入确认失败版本隔离、当前稳定发布不变，依赖恢复后可从 `RENDERING` 单阶段重跑成功。
- [x] 结构化日志递归脱敏API Key、Bearer Token和完整Authorization Header；传输错误日志只保存分类、状态码、尝试次数和退避，不保存响应正文或凭证。

## 9. Web演示

### SF-900 上传与任务页｜P0

- [x] 上传视频、PDF和录音。
- [x] 配置受众、语言、时长和输出类型；Web显示新操作员、`zh-CN`、目标80秒、六类必需输出和6段私有低码率预览。
- [x] Web 展示三类来源候选数、粗细粒度处理、合并步骤和前8个合并组，可下载完整候选合并报告。

### SF-901 SOP与证据页｜P0

- [x] 支持审核会话内安全排序、步骤锁定、人工确认和单步确定性重建；重排强制校验前置依赖与锁定位置，单步重建不修改Gold且外部模型调用0。
- [x] 点击Evidence定位PDF页、视频时间点或录音时间点；视频仅显示已审核安全预览，原始来源URL固定为空。
- [x] 本机与DGX Python 3.12均完成112项回归；DGX回环服务实际验证Evidence、会话、单步重建、依赖拒绝和锁定确认，服务不依赖Docker。

### SF-902 质检对比页｜P0

- [x] 突出修改前、问题、证据和修改后。
- [x] 接入 N31 真实来源候选彩排，明确显示非Gold状态、7→0、证据定位和6项局部修订；本机真实进程及接口专项测试通过。
- [x] Web 接受并优先展示 `GOLD / FINAL` 真实评测结果，显示5→0、证据定位和4项局部修订；仍兼容候选彩排与模拟兜底。

### SF-903 成果页｜P0

- [x] 展示最终SOP、13项手机检查清单和5题培训测验。
- [x] 通过白名单下载最终SOP、检查清单、测验、A4海报、培训视频、视频生成清单、视频证据包、连续动作候选窗口、PDF结构报告和修订记录；不开放原始素材或任意路径。
- [x] 页面内播放并下载80秒培训视频；独立证据包将15镜头绑定25条去重Evidence及其定位，不嵌入原始媒体。

## 10. 评测

### SF-1000 指标实现｜P0

- [x] 必需步骤覆盖率。
- [x] 有证据结论比例。
- [x] 严重错误数量。
- [x] 质检前后错误变化。
- [x] 记录结构化Gold质检、局部修订与输出端到端耗时：本机和DGX均对直接Gold与Web现场重算执行2次预热、各20次正式测量；当前DGX中位数分别为78.619毫秒和179.826毫秒，40轮全部成功且唯一P0语义指纹为1。

### SF-1001 对比实验｜P1

- [x] 比较受控错误草稿与质检修订：Gold严重错误5→0、必要步骤与证据覆盖90%→100%。
- [x] 比较单一手册、专家口述、视频和多源输入：必要步骤覆盖分别为80%、90%、视觉部分可观察90%、至少两种来源100%。
- [x] 记录2026-07-20 DGX基准进程高水位RSS：93,044,736字节；明确不把它表述为整机或GPU显存占用。
- [x] 为40轮直接Gold/Web运行增加P0语义指纹稳定性门禁：单路径唯一指纹必须为1、两路径指纹必须相同、40轮必须全部成功且失败0次。
- [x] 统一README、案例说明、环境文档、路演脚本、运行单、征文和Pitch就绪报告的运行时口径；精确耗时集中在评测说明，并由自动测试绑定当前DGX JSON。
- [x] 运行运行时/Pitch/征文/最终录屏/发布冻结专项63项和本机全量381项，旧指标仅保留在带日期的2026-07-17历史验证快照。
- [x] 运行20项本机开发预检：18项通过、工作树和正式纯净检出2项按开发模式跳过、失败0项；人工门禁继续0/5。
- [x] 提交并推送 `e9a8eef`，完成正式纯净检出和20/20本机正式预检；分块同步DGX后清理不兼容的私有预览/测试缓存并重建技术包，最终DGX 380项通过、1项按设计跳过，20项部署预检失败0项，服务和SSH隧道烟测通过。
- [ ] 记录最终路演后的人工修改次数。

验收：所有路演数字都来自可复现测试。

- [x] 新增公开Git提交纯净检出复现：精确归档HEAD跟踪文件，拒绝私有路径、`.env`、链接、路径穿越和本机实际密钥值；无索引安装并阻断Socket后复现Gold 5→0、Web 200、20成员技术包和路演状态。

## 11. 部署与稳定性

### SF-1100 DGX环境｜P0

- [x] 验证免密SSH。
- [x] 盘点GPU、内存、磁盘和基础工具。
- [ ] 解决Docker daemon访问问题（已降为非阻塞项，仅本地GPU模型或VSS需要）。
- [x] 安装或确认FFmpeg（用户级 `~/skillforge/bin/ffmpeg`）。
- [ ] 验证GPU容器（已降为非阻塞项，仅本地GPU模型或VSS需要）。
- [x] 建立远端缓存、数据和输出目录。
- [x] 使用机器已有CUDA 13原生工具链编译 `sm_121` 内核，在GB10实际处理6段安全视频420帧；不依赖Docker。
- [x] 建立权限受限的 `n31_safe` 视频/帧目录并完成本机到DGX逐文件SHA-256校验；第三方与私有来源未上传。

### SF-1101 一键启动｜P0

- [x] 启动模型/依赖、后端和前端（外部模型无需本地常驻进程）。
- [x] 自动检查环境并输出访问地址。
- [x] 当前代码和Gold结果在 DGX Python 3.12 完成72项测试、Gold 5→0闭环、离线Web、视觉/多源/DGX计算/连续窗口/PDF结构接口、A4海报、培训视频、运行时基准、路演验收、用户服务和现场重跑烟测。
- [x] 将提交 `7e9a4bf` 的31文件安全增量同步到DGX，重建陈旧技术交付包后全量311项通过；三模式一致性、18项成果、部署预检、用户服务重启和SSH隧道烟测均通过。
- [x] 将提交 `6969cff` 的16个Gold-free实验文件以83,861字节安全增量同步DGX；两端归档SHA-256均为 `93c7ca1280d372ab26507d02f0546eeeb153bd7421247a0f6735160e6007c285`，一键样例、独立评测、专项15项和全量320项通过，既有N31用户服务与SSH隧道烟测保持通过。
- [x] 将官方表单材料包与11阶段收尾链安全同步DGX；补齐发布冻结版本漂移后，本机/DGX技术冻结摘要、37,185,168字节20成员技术包及其SHA-256完全一致，表单/提交专项42项、发布专项17项和全量328项通过，部署副本未创建任何私有表单文件。
- [x] DGX Web 进程停止后以新PID重新启动，`0.0.0.0:7860`监听、回环健康检查和Gold 5→0接口均恢复。
- [x] 安装用户级 `skillforge-demo.service`：仅监听回环地址、离线Gold模式、失败自动重启、无需root和Docker。
- [x] 用SIGKILL故障注入验证systemd恢复为新PID；恢复后健康检查、`GOLD / FINAL / COMPLETED`、5→0和4项修订全部通过。
- [x] 用户服务为 `enabled` 且 `Linger=yes`；具备开机随用户管理器恢复的配置。实际整机重启属于高风险操作，未执行。
- [x] 公网端口降为非阻塞项；本机SSH隧道烟测已通过，默认安全访问地址为 `127.0.0.1:17860`。

### SF-1102 演示降级｜P0

- [x] 现场运行模式：只用已审核结构化Gold重新执行质检与局部修订，不依赖外部模型或原始素材。
- [x] 使用 N31 预处理素材的一键快速模式；可选从隐私视频派生开始完整本地重跑。
- [x] 生成不含原始素材和凭证的离线Gold输出兜底，并提供一键启动。
- [x] 实际逐个启动现场、预处理和离线模式，三种入口均返回 `GOLD / FINAL`、5→0和4项局部修订。
- [x] 建立三模式结构化语义一致性门禁：现场模式重新计算、预处理模式可用严格离线OCR缓存重新摄取、离线模式核验冻结包；三者必须保持13步、5→0、覆盖率0.9→1.0、4项修订和完成态工作流一致，且网络请求/外部模型/自动人工确认均为0。
- [x] 生成九场景、178秒、1080p有声字幕候选兜底；截图、TTS缓存和成片保持私有且受Git忽略。
- [x] 建立最终录屏私有机器QA：内部175–180.5秒目标、1080p/30fps、H.264/AAC、响度/峰值、静音和黑屏检查；字幕、隐私、完整播放和音画同步继续由人工门禁确认。

## 12. 路演与提交

### SF-1200 三分钟演示｜P0

- [x] 20秒痛点、20秒输入、30秒提取、40秒质检修订、30秒成果、20秒指标、20秒平台价值；7段连续覆盖180秒。
- [x] 冻结逐段讲解词、切屏动作、证明点、故障兜底和超时裁剪顺序。
- [x] 自动验收时间轴、18项成果、五类Agent工具追踪、Gold指标、四场景无来源内容门禁、高推理语义复核、选择性重建边界、三种SOP视图、交互检查清单、三来源候选合并、DGX报告、连续动作候选窗口、PDF结构报告、运行时基准和三种演示入口；当前状态 `READY_WITH_HUMAN_GATES`。
- [x] 建立80秒成片私有完整观看审核表、安全QA和当前成片/生成清单哈希绑定；只接受本地审核记录，成片、清单或记录变化后旧确认自动失效。
- [x] 培训视频审核补充带时区的播放开始/结束时间和实际观看毫秒数；必须达到当前成片时长减2秒，短播、倒序时间、异常长时间或单调计时不一致均拒绝。
- [x] 当前本机已将成片观看和彩排私有模板安全初始化为 `PENDING_INPUT`；提交预检将合法空白模板识别为“待人工填写”而非技术失败，且不会生成QA或人工确认。
- [x] 参赛者实际从头到尾播放当前80秒成片，80.409秒观看记录、八项人工检查和匿名QA通过后已显式确认 `TRAINING_VIDEO_FULL_WATCH`。
- [x] 建立Git忽略的7段真人彩排记录、175–180秒内部计时核验、匿名QA和人工门禁哈希绑定；记录、运行单或策略变化后旧确认自动失效。
- [x] 彩排运行单七段口述文案与已确认路演口述稿同步（第1-2段PPT、第3-4段Web、第5-7段回PPT，全程两次切换；40轮回归与唯一P0语义指纹列入证明点）；`human_gates`、`demo_modes`、`required_artifacts` 未变，运行单哈希变化后按约定 `reset-stale` 显式重置三项已确认门禁密封（仅清空重密封，不伪造确认），待重新确认。
- [x] 观看类引导流程支持门禁失效后归档重做（时间戳归档、显式同意、不静默覆盖），状态提示与门禁存储实时交叉核对，成功后提示显式确认命令；重确认路径全链路可用。
- [ ] 按冻结运行单实际完成一次连续真人计时彩排，机器检查通过后再人工确认 `FINAL_STAGE_REHEARSAL`。
- [x] 自动生成有字幕有旁白候选，并用九个场景中点差异哈希验证最终时间线无漏场景或错序。
- [x] 在正式演示画面加入团队名“星星之火”，移除对外画面中的内部候选标签，并把第8场更新为13个Gold步骤、10个必要步骤和30次镜头证据引用；新178.021秒成片及机器QA通过。
- [x] 建立最终录屏私有完整观看审核表、匿名QA和人工门禁绑定；观看记录同时绑定MP4、机器QA、构建报告、故事板和策略，任一漂移后确认失效。
- [x] 本机空白最终录屏审核表已按当前178.021秒候选初始化为 `PENDING_INPUT`，11项人工检查均为false，未生成QA或人工确认。
- [x] 新增统一引导命令，支持80秒培训视频完整播放、7段真人彩排计时和178秒最终录屏完整播放；逐项询问人工事实，任一否定或时长不合格均不写入通过记录，生成QA后仍不自动确认门禁。
- [x] 三类引导流程的私有记录与匿名QA改为事务式写入；QA路径必须位于同一私有目录，校验或写盘异常时删除半成品QA并原子恢复原草稿字节，允许安全重试。
- [x] 将事务完整性修复的6个公开文件安全同步DGX；127,567字节增量包两端SHA-256一致，受影响103项、全量373项、19项部署预检、11阶段收尾和SSH隧道烟测通过，部署副本人工记录、QA与确认保持0。
- [x] 参赛者已完整观看加入“星星之火”标识的当前候选；178.591秒实际观看覆盖178.021秒成片，11项人工检查、匿名QA和全部关联哈希通过，`FINAL_RECORDING_REVIEW` 已由邱世乐显式确认。
- [x] 提供 `check_final_recording.sh` 一键检查和严格报告；QA通过只进入 `READY_FOR_HUMAN_REVIEW`，不能替代录制、观看或官方视频规则。

### SF-1201 路演材料｜P0

- [x] 8页PPT：定位、痛点、输入输出、架构、证据闭环、Demo、指标、落地；已将435处不兼容的 `PingFang SC` 统一替换为 `Microsoft YaHei`，PPTX结构检查通过，并在本机 Microsoft PowerPoint 实际打开逐页确认中文正常、无明显溢出。
- [x] 三分钟讲解词、现场/预处理/离线操作单和录屏故障恢复表。
- [x] 完成README、架构与数据边界图、运行说明、可粘贴参赛材料和已知限制。
- [x] 建立18项冻结成果的确定性公开技术交付ZIP和严格QA：精确20成员、固定元数据、来源/包内双重哈希、路径与密钥边界、篡改/重复/多余成员拒绝；明确不冒充官方上传格式。
- [x] 填写“星星之火”4名最终成员、单位和六项分工；严格名单QA通过，参赛者已确认与报名页面一致并关闭团队资格门禁。
- [x] 提供Git忽略的成员/单位/分工模板和严格检查；团队资格门禁一旦确认，缺失或无效的私有名单会阻止提交。

### SF-1202 提交检查｜P0

- [x] 对照赛事公开说明完成主题、能力、DGX和日程的对齐审计，见 `docs/赛事要求对齐矩阵.md`。
- [x] 新增提交预检，自动检查仓库实际密钥值泄漏、敏感路径、18项成果格式/哈希、12份文档、赛事征文事实QA、项目身份和全量测试命令；新增官方参考代码复现记录、赛事征文和最终录屏候选说明后同步纳入必需文档。
- [x] 将最终录屏完整观看私有状态纳入第19项提交预检；合法空白模板显示 `PENDING_INPUT`，孤立QA、提前确认、权限错误或绑定产物漂移均安全失败。
- [x] 将来源绑定的公开提交纯净检出纳入第20项提交预检；正式模式只接受当前 `main == origin/main` 的 `PASSED` 私有报告，开发模式和无Git部署明确跳过且不能冒充正式结论。
- [x] 将公开检出与20项预检安全同步DGX；长期部署收集378项测试、无Git集成项按设计跳过，另从与GitHub同哈希的离线Git bundle完成正式纯净复现，临时检出随后删除。
- [x] 新增私有人工门禁确认器；显式确认绑定运行单和证据哈希，支持状态、更新、撤销与过期重置，记录由Git忽略且不会把确认人或证据路径写入公开预检。
- [x] 建立3个公开入口的私有填写模板和匿名可访问性检查器；只保存网址哈希、状态码、内容类型和检查结果，不把真实网址写入Git或公开报告。
- [x] 提交预检和人工门禁接入私有团队名单：门禁未确认时允许保持 `ABSENT`；确认时绑定当前名单SHA-256和安全QA，名单变化后确认自动失效。
- [x] 建立官方规则私有六项审核表、来源文件/安全网址绑定、匿名QA和提交预检；规则正文、来源地址和私有备注不进入公开报告，任一绑定状态变化后旧确认自动失效。
- [x] 当前本机已初始化成片观看、彩排、团队名单、官方规则审核和官方表单材料包五份私有模板；均为目录0700、文件0600且受Git忽略，成员、规则结论、表单值、人工检查和来源保持待填，人工门禁仍为0/5。
- [x] 提交预检统一支持合法 `PENDING_INPUT` 中间态；模板Schema/权限错误、草稿旁出现QA、草稿被提前确认、非法JSON或已就绪证据漂移仍安全失败，并已在本机与无私有模板的DGX部署副本完成回归。
- [x] 2026-07-19以匿名只读方式复审训练营回放页和4个公开资料入口；3个入口均为技术课程讲义、1个为技术代码，未取得规则讲义或可用匿名回放，6项规则细节继续保持待官方材料，未登录、报名、提交表单或保存受限内容；本机与DGX均已复验新的受控快照。
- [x] 接收参赛者转交的统一提交表单入口和“跑通何老师参考代码”基础完赛标准；提交入口及带跟踪参数的下载地址不写入公开仓库，参考代码待放入独立投递区后审计。
- [x] 完整渲染并检查官方开幕PPT的12页；评分和主要提交要求进入规则快照，私有审核表绑定原PPTX并保持 `PENDING_INPUT`，原文件和正文不进入Git。
- [x] 新增官方参考材料静态审计器、严格Schema、一键脚本和4项测试；当前真实报告为 `WAITING_ON_RUNTIME_BUNDLE`，退出码2，明确基础完赛标准尚未达到。
- [x] 建立提交后私有回执审核表、截图/PDF复制、最终干净预检、18项发布冻结和提交后公开链接QA的交叉绑定；QA不复制截图、提交编号、网址、个人信息或路径。
- [x] 建立官方动态表单只读快照、8项必填/1项可选私有材料包和去标识化QA；严格检查2–5人名单、三个安全公开网址、团队照片解码/20MB限制和来源漂移，只允许人工复制，不自动填写或提交表单。
- [x] 将统一提交收尾协调器扩展为11阶段，按技术包、五项门禁、官方表单材料包、最终预检、上传、公开链接和回执的固定依赖顺序输出下一动作；只读、不联网、不自动确认，并明确区分正式提交待办与实现目标是否受阻。
- [ ] 上传后验证作品页、仓库和视频公开链接可访问。
- [x] 项目名称、案例编号、定位、截止日和本地成果引用一致。
- [x] 最终成员信息与报名资料一致。
- [x] 取得组织方对视频时长/格式、外部API和现场运行3项细则的明确答复并写入私有规则审核；六项QA通过，尚待参赛者显式确认规则门禁。
- [x] 经参赛者明确授权配置公开代码仓库并推送安全范围；108个历史提交密钥泄漏0处，公开仓库为 `https://github.com/sparklefire/SkillForge`。
- [ ] 准备团队合影。
- [x] 完成1958字赛事征文正文、17个来源和15项主张的确定性QA，并接入第18项提交预检；保持只允许人工发布，公开网址仍待参赛者填写。
- [x] 将赛事征文与第18项预检安全同步DGX；本机/DGX赛事征文专项、39项联合专项和344项全量测试全部通过，18项部署预检失败0项，服务和SSH隧道烟测通过。
- [x] 将最终录屏候选的14个公开构建文件安全同步DGX；本机/DGX增量包SHA-256一致，新增专项19项和全量352项通过，部署副本未同步截图、TTS缓存、候选MP4或 `.env`，服务和SSH隧道烟测继续通过。
- [x] 将最终录屏完整观看审核链的21个公开文件安全同步DGX；本机/DGX增量包SHA-256一致，相关专项63项和全量361项通过，19项部署预检失败0，部署副本未同步候选MP4、私有审核表、QA或 `.env`，服务重启和SSH隧道烟测通过。
- [x] 统一审计五项人工门禁证据链；培训视频和最终彩排确认现会重算当前原始记录并逐项比对匿名QA，拒绝记录/QA联动改写、过期产物绑定和语义不一致，且不自动生成任何人工结论。
- [x] 将五门禁证据加固的9个公开文件安全同步DGX；本机/DGX增量包SHA-256一致，受影响专项28项、全量361项、19项部署预检、11阶段收尾和SSH隧道烟测通过，部署副本私有记录与人工确认保持0。
- [x] 建立 `run_guided_human_review.sh` 本机人工复核入口及安全状态/准备操作；播放器不经Shell、环境白名单不传Step密钥，私有空白培训视频草稿完成兼容迁移，三份QA和五项人工确认继续为0。
- [x] 将引导式人工复核的15个公开文件安全同步DGX；150,557字节增量包两端SHA-256一致，受影响86项、全量369项、19项部署预检、11阶段收尾和SSH隧道烟测通过，部署副本人工记录、QA与确认保持0。
- [x] 参考Workshop实跑已从当前主线移除；仅当后续参赛策略明确要求时，才取得合法bundle并在DGX独立目录复现。
- [ ] 保存提交成功截图并复查作品页。
- [x] 最终录屏人工确认强制绑定同一MP4的机器QA、媒体哈希和当前内部策略哈希；媒体或策略变化、报告缺失或失效后确认自动失效。
- [x] `manage_human_gates.sh` 所有子命令在stderr增加人工可读门禁摘要（进度、逐项状态与下一步命令，STALE时提示reset-stale），stdout保持纯JSON不变；摘要不含确认人或证据内容，新增1项CLI回归，本机全量383项通过。
- [x] `check_submission_form_packet.sh` 私有输入未完成时报错同步输出待办指引（文件路径、空字段ID、照片绑定命令、改状态重跑步骤）；只暴露字段ID不暴露已填值，stdout保持纯JSON，新增1项回归。
- [x] `check_submission.sh` 增加stderr人工预检摘要（通过数/状态/逐项图标/待确认门禁与查看命令），stdout纯JSON不变，新增1项回归。
- [x] `check_submission_receipt.sh` 报错时按错误类型在stderr输出可执行待办（初始化、填写状态、绑定截图命令），不泄漏私有内容，stdout纯JSON不变，新增1项回归。

## 倒排里程碑

| 日期 | 里程碑 |
|---|---|
| 7月14日 | 场景、素材方案、Gold格式、API和DGX环境 |
| 7月15日 | 视频/PDF/录音解析与统一Schema |
| 7月16日 | SOP生成和证据绑定 |
| 7月17日 | 检查清单、测验和基础UI |
| 7月18日 | 质检Agent和自动修订 |
| 7月19日 | 海报、培训视频和成果页 |
| 7月20日 | 评测、对比实验和稳定性 |
| 7月21日 | 演示视频、PPT、README和兜底 |
| 7月22日 | 最终回归并提前提交 |

## 延期时的砍项顺序

依次砍掉：数字人 → 多语言 → AI生成镜头 → 多案例 → 海报。

不能砍：SOP、证据引用、错误检测、局部修订、修订前后对比和稳定Demo。
